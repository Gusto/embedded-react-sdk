import {
  useQuery,
  useSuspenseQuery
} from "./chunk-ZFEO5ARQ.js";
import {
  useGustoEmbeddedContext
} from "./chunk-NO5NKTIM.js";
import "./chunk-V4Y5HYFO.js";
import "./chunk-YWSHKGLW.js";
import {
  RFCDate
} from "./chunk-IM5PGYGQ.js";
import {
  NotFoundErrorObject$inboundSchema
} from "./chunk-6THOVZYA.js";
import {
  APIPromise,
  extractSecurity,
  fail,
  json,
  jsonErr,
  match,
  resolveGlobalSecurity
} from "./chunk-VMJMC35V.js";
import {
  encodeFormQuery,
  encodeSimple,
  matchStatusCode,
  pathToFunc
} from "./chunk-KLSXJR7W.js";
import "./chunk-F3XHLYOT.js";
import {
  HTTPMetadata$inboundSchema
} from "./chunk-T5UP3H22.js";
import "./chunk-TOSH4I53.js";
import {
  safeParse
} from "./chunk-NSEKISR4.js";
import {
  unwrapAsync
} from "./chunk-MLG4HZMY.js";
import "./chunk-UJZEUGVO.js";
import {
  combineSignals,
  compactMap,
  remap
} from "./chunk-HHBL53AV.js";
import {
  arrayType,
  nativeEnumType,
  objectType,
  stringType
} from "./chunk-GI7E5V64.js";
import "./chunk-PX6F3LHL.js";

// node_modules/@gusto/embedded-api/esm/models/components/minimumwage.js
var MinimumWage$inboundSchema = objectType({
  uuid: stringType(),
  wage: stringType(),
  wage_type: stringType(),
  effective_date: stringType().transform((v) => new RFCDate(v)),
  authority: stringType(),
  notes: stringType().optional()
}).transform((v) => {
  return remap(v, {
    "wage_type": "wageType",
    "effective_date": "effectiveDate"
  });
});

// node_modules/@gusto/embedded-api/esm/models/operations/getv1locationslocationuuidminimumwages.js
var GetV1LocationsLocationUuidMinimumWagesHeaderXGustoAPIVersion = {
  TwoThousandAndTwentySixMinus06Minus15: "2026-06-15"
};
var GetV1LocationsLocationUuidMinimumWagesHeaderXGustoAPIVersion$outboundSchema = nativeEnumType(GetV1LocationsLocationUuidMinimumWagesHeaderXGustoAPIVersion);
var GetV1LocationsLocationUuidMinimumWagesRequest$outboundSchema = objectType({
  xGustoAPIVersion: GetV1LocationsLocationUuidMinimumWagesHeaderXGustoAPIVersion$outboundSchema.default("2026-06-15"),
  locationUuid: stringType(),
  effectiveDate: stringType().optional()
}).transform((v) => {
  return remap(v, {
    xGustoAPIVersion: "X-Gusto-API-Version",
    locationUuid: "location_uuid",
    effectiveDate: "effective_date"
  });
});
var GetV1LocationsLocationUuidMinimumWagesResponse$inboundSchema = objectType({
  HttpMeta: HTTPMetadata$inboundSchema,
  "Minimum-Wage-List": arrayType(MinimumWage$inboundSchema).optional()
}).transform((v) => {
  return remap(v, {
    "HttpMeta": "httpMeta",
    "Minimum-Wage-List": "minimumWageList"
  });
});

// node_modules/@gusto/embedded-api/esm/funcs/locationsGetMinimumWages.js
function locationsGetMinimumWages(client, request, options) {
  return new APIPromise($do(client, request, options));
}
async function $do(client, request, options) {
  const parsed = safeParse(request, (value) => GetV1LocationsLocationUuidMinimumWagesRequest$outboundSchema.parse(value), "Input validation failed");
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;
  const pathParams = {
    location_uuid: encodeSimple("location_uuid", payload.location_uuid, {
      explode: false,
      charEncoding: "percent"
    })
  };
  const path = pathToFunc("/v1/locations/{location_uuid}/minimum_wages")(pathParams);
  const query = encodeFormQuery({
    "effective_date": payload.effective_date
  });
  const headers = new Headers(compactMap({
    Accept: "application/json",
    "X-Gusto-API-Version": encodeSimple("X-Gusto-API-Version", payload["X-Gusto-API-Version"], { explode: false, charEncoding: "none" })
  }));
  const secConfig = await extractSecurity(client._options.companyAccessAuth);
  const securityInput = secConfig == null ? {} : { companyAccessAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput, [0]);
  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "get-v1-locations-location_uuid-minimum_wages",
    oAuth2Scopes: null,
    resolvedSecurity: requestSecurity,
    securitySource: client._options.companyAccessAuth,
    retryConfig: options?.retries || client._options.retryConfig || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"]
  };
  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path,
    headers,
    query,
    body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;
  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode) => matchStatusCode({ status: statusCode }, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;
  const responseFields = {
    HttpMeta: { Response: response, Request: req }
  };
  const [result] = await match(json(200, GetV1LocationsLocationUuidMinimumWagesResponse$inboundSchema, {
    key: "Minimum-Wage-List"
  }), jsonErr(404, NotFoundErrorObject$inboundSchema), fail("4XX"), fail("5XX"))(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }
  return [result, { status: "complete", request: req, response }];
}

// node_modules/@gusto/embedded-api/esm/react-query/locationsGetMinimumWages.core.js
function prefetchLocationsGetMinimumWages(queryClient, client$, request, options) {
  return queryClient.prefetchQuery({
    ...buildLocationsGetMinimumWagesQuery(client$, request, options)
  });
}
function buildLocationsGetMinimumWagesQuery(client$, request, options) {
  return {
    queryKey: queryKeyLocationsGetMinimumWages(request.locationUuid, {
      xGustoAPIVersion: request.xGustoAPIVersion,
      effectiveDate: request.effectiveDate
    }),
    queryFn: async function locationsGetMinimumWagesQueryFn(ctx) {
      const sig = combineSignals(ctx.signal, options?.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig
      };
      return unwrapAsync(locationsGetMinimumWages(client$, request, mergedOptions));
    }
  };
}
function queryKeyLocationsGetMinimumWages(locationUuid, parameters) {
  return [
    "@gusto/embedded-api-v-2026-06-15",
    "Locations",
    "getMinimumWages",
    locationUuid,
    parameters
  ];
}

// node_modules/@gusto/embedded-api/esm/react-query/locationsGetMinimumWages.js
function useLocationsGetMinimumWages(request, options) {
  const client = useGustoEmbeddedContext();
  return useQuery({
    ...buildLocationsGetMinimumWagesQuery(client, request, options),
    ...options
  });
}
function useLocationsGetMinimumWagesSuspense(request, options) {
  const client = useGustoEmbeddedContext();
  return useSuspenseQuery({
    ...buildLocationsGetMinimumWagesQuery(client, request, options),
    ...options
  });
}
function setLocationsGetMinimumWagesData(client, queryKeyBase, data) {
  const key = queryKeyLocationsGetMinimumWages(...queryKeyBase);
  return client.setQueryData(key, data);
}
function invalidateLocationsGetMinimumWages(client, queryKeyBase, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gusto/embedded-api-v-2026-06-15",
      "Locations",
      "getMinimumWages",
      ...queryKeyBase
    ]
  });
}
function invalidateAllLocationsGetMinimumWages(client, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gusto/embedded-api-v-2026-06-15",
      "Locations",
      "getMinimumWages"
    ]
  });
}
export {
  buildLocationsGetMinimumWagesQuery,
  invalidateAllLocationsGetMinimumWages,
  invalidateLocationsGetMinimumWages,
  prefetchLocationsGetMinimumWages,
  queryKeyLocationsGetMinimumWages,
  setLocationsGetMinimumWagesData,
  useLocationsGetMinimumWages,
  useLocationsGetMinimumWagesSuspense
};
//# sourceMappingURL=@gusto_embedded-api_react-query_locationsGetMinimumWages.js.map
