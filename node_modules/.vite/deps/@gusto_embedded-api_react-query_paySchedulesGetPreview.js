import {
  useQuery,
  useSuspenseQuery
} from "./chunk-ZFEO5ARQ.js";
import {
  useGustoEmbeddedContext
} from "./chunk-NO5NKTIM.js";
import "./chunk-V4Y5HYFO.js";
import "./chunk-YWSHKGLW.js";
import {
  RFCDate
} from "./chunk-IM5PGYGQ.js";
import {
  UnprocessableEntityError$inboundSchema
} from "./chunk-MT6SOPTL.js";
import "./chunk-JVU7U7TD.js";
import {
  NotFoundErrorObject$inboundSchema
} from "./chunk-6THOVZYA.js";
import {
  APIPromise,
  extractSecurity,
  fail,
  json,
  jsonErr,
  match,
  resolveGlobalSecurity
} from "./chunk-VMJMC35V.js";
import {
  encodeFormQuery,
  encodeSimple,
  matchStatusCode,
  pathToFunc
} from "./chunk-KLSXJR7W.js";
import "./chunk-F3XHLYOT.js";
import {
  HTTPMetadata$inboundSchema
} from "./chunk-T5UP3H22.js";
import "./chunk-TOSH4I53.js";
import {
  safeParse
} from "./chunk-NSEKISR4.js";
import {
  unwrapAsync
} from "./chunk-MLG4HZMY.js";
import "./chunk-UJZEUGVO.js";
import {
  combineSignals,
  compactMap,
  remap
} from "./chunk-HHBL53AV.js";
import {
  arrayType,
  instanceOfType,
  nativeEnumType,
  numberType,
  objectType,
  stringType
} from "./chunk-GI7E5V64.js";
import "./chunk-PX6F3LHL.js";

// node_modules/@gusto/embedded-api/esm/models/components/payschedulepreviewpayperiod.js
var PaySchedulePreviewPayPeriod$inboundSchema = objectType({
  check_date: stringType().transform((v) => new RFCDate(v)),
  start_date: stringType().transform((v) => new RFCDate(v)),
  run_payroll_by: stringType().transform((v) => new RFCDate(v)),
  end_date: stringType().transform((v) => new RFCDate(v))
}).transform((v) => {
  return remap(v, {
    "check_date": "checkDate",
    "start_date": "startDate",
    "run_payroll_by": "runPayrollBy",
    "end_date": "endDate"
  });
});

// node_modules/@gusto/embedded-api/esm/models/components/payschedulepreview.js
var PaySchedulePreview$inboundSchema = objectType({
  pay_periods: arrayType(PaySchedulePreviewPayPeriod$inboundSchema).optional(),
  holidays: arrayType(stringType().transform((v) => new RFCDate(v))).optional()
}).transform((v) => {
  return remap(v, {
    "pay_periods": "payPeriods"
  });
});

// node_modules/@gusto/embedded-api/esm/models/operations/getv1companiescompanyidpayschedulespreview.js
var GetV1CompaniesCompanyIdPaySchedulesPreviewHeaderXGustoAPIVersion = {
  TwoThousandAndTwentySixMinus06Minus15: "2026-06-15"
};
var Frequency = {
  EveryWeek: "Every week",
  EveryOtherWeek: "Every other week",
  TwicePerMonth: "Twice per month",
  Monthly: "Monthly"
};
var GetV1CompaniesCompanyIdPaySchedulesPreviewHeaderXGustoAPIVersion$outboundSchema = nativeEnumType(GetV1CompaniesCompanyIdPaySchedulesPreviewHeaderXGustoAPIVersion);
var Frequency$outboundSchema = nativeEnumType(Frequency);
var GetV1CompaniesCompanyIdPaySchedulesPreviewRequest$outboundSchema = objectType({
  xGustoAPIVersion: GetV1CompaniesCompanyIdPaySchedulesPreviewHeaderXGustoAPIVersion$outboundSchema.default("2026-06-15"),
  companyId: stringType(),
  frequency: Frequency$outboundSchema,
  anchorPayDate: instanceOfType(RFCDate).transform((v) => v.toString()),
  anchorEndOfPayPeriod: instanceOfType(RFCDate).transform((v) => v.toString()),
  day1: numberType().int().optional(),
  day2: numberType().int().optional(),
  endDate: instanceOfType(RFCDate).transform((v) => v.toString()).optional(),
  payScheduleUuid: stringType().optional()
}).transform((v) => {
  return remap(v, {
    xGustoAPIVersion: "X-Gusto-API-Version",
    companyId: "company_id",
    anchorPayDate: "anchor_pay_date",
    anchorEndOfPayPeriod: "anchor_end_of_pay_period",
    day1: "day_1",
    day2: "day_2",
    endDate: "end_date",
    payScheduleUuid: "pay_schedule_uuid"
  });
});
var GetV1CompaniesCompanyIdPaySchedulesPreviewResponse$inboundSchema = objectType({
  HttpMeta: HTTPMetadata$inboundSchema,
  "Pay-Schedule-Preview": PaySchedulePreview$inboundSchema.optional()
}).transform((v) => {
  return remap(v, {
    "HttpMeta": "httpMeta",
    "Pay-Schedule-Preview": "paySchedulePreview"
  });
});

// node_modules/@gusto/embedded-api/esm/funcs/paySchedulesGetPreview.js
function paySchedulesGetPreview(client, request, options) {
  return new APIPromise($do(client, request, options));
}
async function $do(client, request, options) {
  const parsed = safeParse(request, (value) => GetV1CompaniesCompanyIdPaySchedulesPreviewRequest$outboundSchema.parse(value), "Input validation failed");
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;
  const pathParams = {
    company_id: encodeSimple("company_id", payload.company_id, {
      explode: false,
      charEncoding: "percent"
    })
  };
  const path = pathToFunc("/v1/companies/{company_id}/pay_schedules/preview")(pathParams);
  const query = encodeFormQuery({
    "anchor_end_of_pay_period": payload.anchor_end_of_pay_period,
    "anchor_pay_date": payload.anchor_pay_date,
    "day_1": payload.day_1,
    "day_2": payload.day_2,
    "end_date": payload.end_date,
    "frequency": payload.frequency,
    "pay_schedule_uuid": payload.pay_schedule_uuid
  });
  const headers = new Headers(compactMap({
    Accept: "application/json",
    "X-Gusto-API-Version": encodeSimple("X-Gusto-API-Version", payload["X-Gusto-API-Version"], { explode: false, charEncoding: "none" })
  }));
  const secConfig = await extractSecurity(client._options.companyAccessAuth);
  const securityInput = secConfig == null ? {} : { companyAccessAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput, [0]);
  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "get-v1-companies-company_id-pay_schedules-preview",
    oAuth2Scopes: null,
    resolvedSecurity: requestSecurity,
    securitySource: client._options.companyAccessAuth,
    retryConfig: options?.retries || client._options.retryConfig || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"]
  };
  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path,
    headers,
    query,
    body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;
  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode) => matchStatusCode({ status: statusCode }, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;
  const responseFields = {
    HttpMeta: { Response: response, Request: req }
  };
  const [result] = await match(json(200, GetV1CompaniesCompanyIdPaySchedulesPreviewResponse$inboundSchema, { key: "Pay-Schedule-Preview" }), jsonErr(404, NotFoundErrorObject$inboundSchema), jsonErr(422, UnprocessableEntityError$inboundSchema), fail("4XX"), fail("5XX"))(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }
  return [result, { status: "complete", request: req, response }];
}

// node_modules/@gusto/embedded-api/esm/react-query/paySchedulesGetPreview.core.js
function prefetchPaySchedulesGetPreview(queryClient, client$, request, options) {
  return queryClient.prefetchQuery({
    ...buildPaySchedulesGetPreviewQuery(client$, request, options)
  });
}
function buildPaySchedulesGetPreviewQuery(client$, request, options) {
  return {
    queryKey: queryKeyPaySchedulesGetPreview(request.companyId, {
      xGustoAPIVersion: request.xGustoAPIVersion,
      frequency: request.frequency,
      anchorPayDate: request.anchorPayDate,
      anchorEndOfPayPeriod: request.anchorEndOfPayPeriod,
      day1: request.day1,
      day2: request.day2,
      endDate: request.endDate,
      payScheduleUuid: request.payScheduleUuid
    }),
    queryFn: async function paySchedulesGetPreviewQueryFn(ctx) {
      const sig = combineSignals(ctx.signal, options?.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig
      };
      return unwrapAsync(paySchedulesGetPreview(client$, request, mergedOptions));
    }
  };
}
function queryKeyPaySchedulesGetPreview(companyId, parameters) {
  return [
    "@gusto/embedded-api-v-2026-06-15",
    "paySchedules",
    "getPreview",
    companyId,
    parameters
  ];
}

// node_modules/@gusto/embedded-api/esm/react-query/paySchedulesGetPreview.js
function usePaySchedulesGetPreview(request, options) {
  const client = useGustoEmbeddedContext();
  return useQuery({
    ...buildPaySchedulesGetPreviewQuery(client, request, options),
    ...options
  });
}
function usePaySchedulesGetPreviewSuspense(request, options) {
  const client = useGustoEmbeddedContext();
  return useSuspenseQuery({
    ...buildPaySchedulesGetPreviewQuery(client, request, options),
    ...options
  });
}
function setPaySchedulesGetPreviewData(client, queryKeyBase, data) {
  const key = queryKeyPaySchedulesGetPreview(...queryKeyBase);
  return client.setQueryData(key, data);
}
function invalidatePaySchedulesGetPreview(client, queryKeyBase, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gusto/embedded-api-v-2026-06-15",
      "paySchedules",
      "getPreview",
      ...queryKeyBase
    ]
  });
}
function invalidateAllPaySchedulesGetPreview(client, filters) {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gusto/embedded-api-v-2026-06-15",
      "paySchedules",
      "getPreview"
    ]
  });
}
export {
  buildPaySchedulesGetPreviewQuery,
  invalidateAllPaySchedulesGetPreview,
  invalidatePaySchedulesGetPreview,
  prefetchPaySchedulesGetPreview,
  queryKeyPaySchedulesGetPreview,
  setPaySchedulesGetPreviewData,
  usePaySchedulesGetPreview,
  usePaySchedulesGetPreviewSuspense
};
//# sourceMappingURL=@gusto_embedded-api_react-query_paySchedulesGetPreview.js.map
