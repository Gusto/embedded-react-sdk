//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJSMin = (cb, mod) => () => (mod || (cb((mod = { exports: {} }).exports, mod), cb = null), mod.exports);
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
		key = keys[i];
		if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
			get: ((k) => from[k]).bind(null, key),
			enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));
//#endregion
let _rollup_pluginutils = require("@rollup/pluginutils");
//#region src/constants.ts
const STYLELINT_SEVERITY = {
	ERROR: "error",
	WARNING: "warning"
};
const PLUGIN_NAME = "vite:stylelint";
const WS_RUNTIME_LOADED_EVENT = "vite:stylelint:runtime-loaded";
const WS_OVERLAY_EVENT = "vite:stylelint:overlay";
const RUNTIME_CLIENT_RUNTIME_PATH = "/@vite-plugin-stylelint-runtime";
const RUNTIME_CLIENT_ENTRY_PATH = "/@vite-plugin-stylelint-runtime-entry";
const PLUGIN_OPTION_KEYS = [
	"test",
	"dev",
	"build",
	"include",
	"exclude",
	"stylelintPath",
	"formatter",
	"lintInWorker",
	"lintOnStart",
	"lintDirtyOnly",
	"emitError",
	"emitErrorAsWarning",
	"emitWarning",
	"emitWarningAsError",
	"customOverlay"
];
const COLOR_MAPPING = {
	error: "red",
	warning: "yellow",
	plugin: "magenta"
};
//#endregion
//#region src/linter.ts
var import_picocolors = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	let p = process || {}, argv = p.argv || [], env = p.env || {};
	let isColorSupported = !(!!env.NO_COLOR || argv.includes("--no-color")) && (!!env.FORCE_COLOR || argv.includes("--color") || p.platform === "win32" || (p.stdout || {}).isTTY && env.TERM !== "dumb" || !!env.CI);
	let formatter = (open, close, replace = open) => (input) => {
		let string = "" + input, index = string.indexOf(close, open.length);
		return ~index ? open + replaceClose(string, close, replace, index) + close : open + string + close;
	};
	let replaceClose = (string, close, replace, index) => {
		let result = "", cursor = 0;
		do {
			result += string.substring(cursor, index) + replace;
			cursor = index + close.length;
			index = string.indexOf(close, cursor);
		} while (~index);
		return result + string.substring(cursor);
	};
	let createColors = (enabled = isColorSupported) => {
		let f = enabled ? formatter : () => String;
		return {
			isColorSupported: enabled,
			reset: f("\x1B[0m", "\x1B[0m"),
			bold: f("\x1B[1m", "\x1B[22m", "\x1B[22m\x1B[1m"),
			dim: f("\x1B[2m", "\x1B[22m", "\x1B[22m\x1B[2m"),
			italic: f("\x1B[3m", "\x1B[23m"),
			underline: f("\x1B[4m", "\x1B[24m"),
			inverse: f("\x1B[7m", "\x1B[27m"),
			hidden: f("\x1B[8m", "\x1B[28m"),
			strikethrough: f("\x1B[9m", "\x1B[29m"),
			black: f("\x1B[30m", "\x1B[39m"),
			red: f("\x1B[31m", "\x1B[39m"),
			green: f("\x1B[32m", "\x1B[39m"),
			yellow: f("\x1B[33m", "\x1B[39m"),
			blue: f("\x1B[34m", "\x1B[39m"),
			magenta: f("\x1B[35m", "\x1B[39m"),
			cyan: f("\x1B[36m", "\x1B[39m"),
			white: f("\x1B[37m", "\x1B[39m"),
			gray: f("\x1B[90m", "\x1B[39m"),
			bgBlack: f("\x1B[40m", "\x1B[49m"),
			bgRed: f("\x1B[41m", "\x1B[49m"),
			bgGreen: f("\x1B[42m", "\x1B[49m"),
			bgYellow: f("\x1B[43m", "\x1B[49m"),
			bgBlue: f("\x1B[44m", "\x1B[49m"),
			bgMagenta: f("\x1B[45m", "\x1B[49m"),
			bgCyan: f("\x1B[46m", "\x1B[49m"),
			bgWhite: f("\x1B[47m", "\x1B[49m"),
			blackBright: f("\x1B[90m", "\x1B[39m"),
			redBright: f("\x1B[91m", "\x1B[39m"),
			greenBright: f("\x1B[92m", "\x1B[39m"),
			yellowBright: f("\x1B[93m", "\x1B[39m"),
			blueBright: f("\x1B[94m", "\x1B[39m"),
			magentaBright: f("\x1B[95m", "\x1B[39m"),
			cyanBright: f("\x1B[96m", "\x1B[39m"),
			whiteBright: f("\x1B[97m", "\x1B[39m"),
			bgBlackBright: f("\x1B[100m", "\x1B[49m"),
			bgRedBright: f("\x1B[101m", "\x1B[49m"),
			bgGreenBright: f("\x1B[102m", "\x1B[49m"),
			bgYellowBright: f("\x1B[103m", "\x1B[49m"),
			bgBlueBright: f("\x1B[104m", "\x1B[49m"),
			bgMagentaBright: f("\x1B[105m", "\x1B[49m"),
			bgCyanBright: f("\x1B[106m", "\x1B[49m"),
			bgWhiteBright: f("\x1B[107m", "\x1B[49m")
		};
	};
	module.exports = createColors();
	module.exports.createColors = createColors;
})))(), 1);
const getFilter = (options) => (0, _rollup_pluginutils.createFilter)(options.include, options.exclude);
const getStylelintLinterOptions = (options) => ({
	...Object.fromEntries(Object.entries(options).filter(([key]) => !PLUGIN_OPTION_KEYS.includes(key))),
	allowEmptyInput: true
});
const initializeStylelint = async (options) => {
	const { stylelintPath, formatter } = options;
	try {
		const module = await import(stylelintPath);
		const stylelintInstance = module?.default ?? module;
		return {
			stylelintInstance,
			formatter: typeof formatter === "string" ? await stylelintInstance.formatters[formatter] : formatter
		};
	} catch (error) {
		throw new Error(`Failed to initialize Stylelint. Have you installed and configured correctly? ${error}`);
	}
};
const isVirtualModule = (id) => id.startsWith("virtual:") || id[0] === "\0" || !id.includes("/");
const getFilePath = (id) => (0, _rollup_pluginutils.normalizePath)(id).split("?")[0];
const shouldIgnoreModule = (id, filter) => {
	if (isVirtualModule(id)) return true;
	if (!filter(id)) return true;
	const filePath = getFilePath(id);
	if ([".vue", ".svelte"].some((extname) => filePath.endsWith(extname))) return !(id.includes("?") && id.includes("type=style"));
	return false;
};
const colorize = (text, textType) => import_picocolors.default[COLOR_MAPPING[textType]](text);
const log = (text, textType, context) => {
	console.log("");
	if (context) {
		if (textType === "error") context.error(text);
		else if (textType === "warning") context.warn(text);
	} else console.log(`${text}  Plugin: ${colorize(PLUGIN_NAME, "plugin")}\r\n`);
};
const filterResults = (linterResult, options) => {
	let results = linterResult.results.filter((item) => !item.ignored);
	if (!options.emitError) {
		results = results.map((item) => ({
			...item,
			warnings: item.warnings.filter((warning) => warning.severity !== STYLELINT_SEVERITY.ERROR)
		}));
		linterResult.errored = false;
	}
	if (!options.emitWarning) results = results.map((item) => ({
		...item,
		warnings: item.warnings.filter((warning) => warning.severity !== STYLELINT_SEVERITY.WARNING)
	}));
	results = results.filter((item) => item.warnings.length > 0);
	return {
		results,
		textType: deriveTextType(linterResult.errored, options)
	};
};
const deriveTextType = (errored, options) => {
	if (errored) return options.emitErrorAsWarning ? "warning" : "error";
	return options.emitWarningAsError ? "error" : "warning";
};
const report = (results, linterResult, formatter, textType, context, emit) => {
	linterResult.results = results;
	const formattedText = formatter(results, linterResult);
	if (emit) emit({
		formattedText,
		textType,
		context
	});
	else log(formattedText, textType, context);
};
/**
* Build a structured overlay payload from filtered results.
*
* Severity follows the *post-filter* textType so `emitErrorAsWarning` /
* `emitWarningAsError` are reflected in the overlay, matching the terminal
* channel. Each result retains only the fields the runtime needs to render.
* Field mapping adapts Stylelint's shape to the wire-stable OverlayPayload:
* `source` → `filePath`, `warning.rule` → `ruleId`, `warning.text` → `message`.
*
* Exported for testing.
*/
const buildOverlayPayload = (results, textType) => ({ results: results.map((result) => ({
	filePath: result.source ?? "",
	messages: result.warnings.map((warning) => ({
		line: warning.line,
		column: warning.column,
		severity: textType === "error" ? "error" : "warning",
		ruleId: warning.rule,
		message: warning.text
	}))
})) });
/**
* Accumulates per-file overlay payloads so the panel shows all current problems
* across the project, not just the last-linted file. Files with no remaining
* messages are dropped; once empty, `snapshot` returns `undefined` so the caller
* can clear the overlay.
*/
var OverlayManager = class {
	files = /* @__PURE__ */ new Map();
	upsert(payload) {
		for (const result of payload.results) if (result.messages.length === 0) this.files.delete(result.filePath);
		else this.files.set(result.filePath, result);
		return this.snapshot();
	}
	snapshot() {
		if (this.files.size === 0) return;
		return { results: [...this.files.values()] };
	}
};
function createLinter(options, adapters) {
	const filter = getFilter(options);
	let stylelintInstance;
	let formatter;
	const ready = initializeStylelint(options).then((result) => {
		stylelintInstance = result.stylelintInstance;
		formatter = result.formatter;
	});
	const overlayManager = adapters?.onOverlayPayload ? new OverlayManager() : void 0;
	const lintFiles = async (files, context) => {
		await ready;
		const linterResult = await stylelintInstance.lint({
			...getStylelintLinterOptions(options),
			files
		});
		if (!linterResult || linterResult.results.length === 0) return;
		const { results, textType } = filterResults(linterResult, options);
		if (results.length === 0) {
			if (overlayManager && adapters?.onOverlayPayload) {
				overlayManager.upsert({ results: linterResult.results.map((result) => ({
					filePath: result.source ?? "",
					messages: []
				})) });
				adapters.onOverlayPayload(overlayManager.snapshot());
			}
			return;
		}
		if (overlayManager && adapters?.onOverlayPayload) {
			const payload = overlayManager.upsert(buildOverlayPayload(results, textType));
			adapters.onOverlayPayload(payload);
		}
		return report(results, linterResult, formatter, textType, context, adapters?.emit);
	};
	return {
		lint: async (ids, context) => {
			await ready;
			const idList = Array.isArray(ids) ? ids : [ids];
			if (options.lintDirtyOnly) {
				const filePaths = idList.filter((id) => !shouldIgnoreModule(id, filter)).map((id) => getFilePath(id));
				if (filePaths.length === 0) return;
				return lintFiles(filePaths, context);
			}
			if (!idList.some((id) => !shouldIgnoreModule(id, filter))) return;
			return lintFiles(options.include, context);
		},
		lintAll: async (context) => lintFiles(options.include, context)
	};
}
//#endregion
Object.defineProperty(exports, "PLUGIN_NAME", {
	enumerable: true,
	get: function() {
		return PLUGIN_NAME;
	}
});
Object.defineProperty(exports, "RUNTIME_CLIENT_ENTRY_PATH", {
	enumerable: true,
	get: function() {
		return RUNTIME_CLIENT_ENTRY_PATH;
	}
});
Object.defineProperty(exports, "RUNTIME_CLIENT_RUNTIME_PATH", {
	enumerable: true,
	get: function() {
		return RUNTIME_CLIENT_RUNTIME_PATH;
	}
});
Object.defineProperty(exports, "WS_OVERLAY_EVENT", {
	enumerable: true,
	get: function() {
		return WS_OVERLAY_EVENT;
	}
});
Object.defineProperty(exports, "WS_RUNTIME_LOADED_EVENT", {
	enumerable: true,
	get: function() {
		return WS_RUNTIME_LOADED_EVENT;
	}
});
Object.defineProperty(exports, "__toESM", {
	enumerable: true,
	get: function() {
		return __toESM;
	}
});
Object.defineProperty(exports, "createLinter", {
	enumerable: true,
	get: function() {
		return createLinter;
	}
});
