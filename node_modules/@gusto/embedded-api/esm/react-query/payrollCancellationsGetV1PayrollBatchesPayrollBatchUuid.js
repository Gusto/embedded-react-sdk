/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuidQuery, prefetchPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid, queryKeyPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid, } from "./payrollCancellationsGetV1PayrollBatchesPayrollBatchUuid.core.js";
export { buildPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuidQuery, prefetchPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid, queryKeyPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid, };
/**
 * Get a payroll cancellation batch
 *
 * @remarks
 * Returns the status and per-payroll results of a payroll cancellation batch.
 *
 * Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once terminal, the response includes the `results` array (one entry per authorized payroll, each with its own per-payroll `status` — `success` or `failed`) and the `exclusions` array (one entry per payroll that could not be processed). A cancel is atomic, so a per-payroll result is only ever `success` or `failed` — never `partial_success`.
 *
 * Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is the request lifecycle, distinct from the per-payroll `status` inside `results[]`. A `completed` batch does not imply every payroll was cancelled — inspect the array for per-payroll outcomes.
 *
 * Results are stored in Redis with a limited TTL after completion. If the partner polls after results have expired, this endpoint returns 410 Gone — partners should re-submit a new batch.
 *
 * 📘 System Access Authentication
 *
 * This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
 *
 * scope: `payroll_batches:read`
 */
export function usePayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid(security, request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuidQuery(client, security, request, options),
        ...options,
    });
}
/**
 * Get a payroll cancellation batch
 *
 * @remarks
 * Returns the status and per-payroll results of a payroll cancellation batch.
 *
 * Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once terminal, the response includes the `results` array (one entry per authorized payroll, each with its own per-payroll `status` — `success` or `failed`) and the `exclusions` array (one entry per payroll that could not be processed). A cancel is atomic, so a per-payroll result is only ever `success` or `failed` — never `partial_success`.
 *
 * Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is the request lifecycle, distinct from the per-payroll `status` inside `results[]`. A `completed` batch does not imply every payroll was cancelled — inspect the array for per-payroll outcomes.
 *
 * Results are stored in Redis with a limited TTL after completion. If the partner polls after results have expired, this endpoint returns 410 Gone — partners should re-submit a new batch.
 *
 * 📘 System Access Authentication
 *
 * This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
 *
 * scope: `payroll_batches:read`
 */
export function usePayrollCancellationsGetV1PayrollBatchesPayrollBatchUuidSuspense(security, request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuidQuery(client, security, request, options),
        ...options,
    });
}
export function setPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuidData(client, queryKeyBase, data) {
    const key = queryKeyPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidatePayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api-v-2026-06-15",
            "Payroll Cancellations",
            "getV1PayrollBatchesPayrollBatchUuid",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllPayrollCancellationsGetV1PayrollBatchesPayrollBatchUuid(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api-v-2026-06-15",
            "Payroll Cancellations",
            "getV1PayrollBatchesPayrollBatchUuid",
        ],
    });
}
//# sourceMappingURL=payrollCancellationsGetV1PayrollBatchesPayrollBatchUuid.js.map