/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildPayrollDigestsGetV1PayrollDigestsPayrollDigestUuidQuery, prefetchPayrollDigestsGetV1PayrollDigestsPayrollDigestUuid, queryKeyPayrollDigestsGetV1PayrollDigestsPayrollDigestUuid, } from "./payrollDigestsGetV1PayrollDigestsPayrollDigestUuid.core.js";
export { buildPayrollDigestsGetV1PayrollDigestsPayrollDigestUuidQuery, prefetchPayrollDigestsGetV1PayrollDigestsPayrollDigestUuid, queryKeyPayrollDigestsGetV1PayrollDigestsPayrollDigestUuid, };
/**
 * Get a payroll digest batch
 *
 * @remarks
 * Returns the status and results of a payroll digest batch.
 *
 * Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once terminal, the response includes the full `results` array (one entry per attempted company, each with its own per-company `status` — `success`, `partial_success`, or `failed`) and the `exclusions` array (one entry per company that could not be looked up or processed).
 *
 * Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`. A `completed` batch does not imply every company succeeded — inspect the arrays for per-company outcomes.
 *
 * Results are stored in Redis with a short TTL after completion. If the partner polls after results have expired, this endpoint returns 410 Gone — partners should re-submit a new batch to fetch fresh data.
 *
 * 📘 System Access Authentication
 *
 * This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
 *
 * scope: `payroll_digests:read`
 */
export function usePayrollDigestsGetV1PayrollDigestsPayrollDigestUuid(security, request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildPayrollDigestsGetV1PayrollDigestsPayrollDigestUuidQuery(client, security, request, options),
        ...options,
    });
}
/**
 * Get a payroll digest batch
 *
 * @remarks
 * Returns the status and results of a payroll digest batch.
 *
 * Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once terminal, the response includes the full `results` array (one entry per attempted company, each with its own per-company `status` — `success`, `partial_success`, or `failed`) and the `exclusions` array (one entry per company that could not be looked up or processed).
 *
 * Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`. A `completed` batch does not imply every company succeeded — inspect the arrays for per-company outcomes.
 *
 * Results are stored in Redis with a short TTL after completion. If the partner polls after results have expired, this endpoint returns 410 Gone — partners should re-submit a new batch to fetch fresh data.
 *
 * 📘 System Access Authentication
 *
 * This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
 *
 * scope: `payroll_digests:read`
 */
export function usePayrollDigestsGetV1PayrollDigestsPayrollDigestUuidSuspense(security, request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildPayrollDigestsGetV1PayrollDigestsPayrollDigestUuidQuery(client, security, request, options),
        ...options,
    });
}
export function setPayrollDigestsGetV1PayrollDigestsPayrollDigestUuidData(client, queryKeyBase, data) {
    const key = queryKeyPayrollDigestsGetV1PayrollDigestsPayrollDigestUuid(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidatePayrollDigestsGetV1PayrollDigestsPayrollDigestUuid(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api-v-2026-06-15",
            "Payroll Digests",
            "getV1PayrollDigestsPayrollDigestUuid",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllPayrollDigestsGetV1PayrollDigestsPayrollDigestUuid(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api-v-2026-06-15",
            "Payroll Digests",
            "getV1PayrollDigestsPayrollDigestUuid",
        ],
    });
}
//# sourceMappingURL=payrollDigestsGetV1PayrollDigestsPayrollDigestUuid.js.map