/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { companiesMigrate } from "../funcs/companiesMigrate.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Migrate company to embedded payroll
 *
 * @remarks
 * Migrate an existing Gusto customer to your embedded payroll product.
 *
 * ### Prerequisites
 * Before calling this endpoint:
 * 1. The customer must connect their Gusto account to your application using [OAuth2](doc:oauth2)
 * 2. The customer must view and [accept the Embedded Payroll Terms of Service](ref:post-partner-managed-companies-company_uuid-accept_terms_of_service)
 *
 * ### Related guides
 * - [Migrate an existing company](doc:migrate-existing-company)
 *
 * scope: `partner_managed_companies:write`
 */
export function useCompaniesMigrateMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildCompaniesMigrateMutation(client, options),
        ...options,
    });
}
export function mutationKeyCompaniesMigrate() {
    return ["@gusto/embedded-api-v-2026-06-15", "Companies", "migrate"];
}
export function buildCompaniesMigrateMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyCompaniesMigrate(),
        mutationFn: function companiesMigrateMutationFn({ request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(companiesMigrate(client$, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=companiesMigrate.js.map