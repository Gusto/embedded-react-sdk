/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildTaxRequirementsGetQuery, prefetchTaxRequirementsGet, queryKeyTaxRequirementsGet, } from "./taxRequirementsGet.core.js";
export { buildTaxRequirementsGetQuery, prefetchTaxRequirementsGet, queryKeyTaxRequirementsGet, };
/**
 * Get tax requirements for a state
 *
 * @remarks
 * Retrieves the detailed tax requirements for a specific state. The response includes requirement sets grouped by
 * category (e.g., registrations, tax rates, deposit schedules), each containing individual requirements with their
 * current values, labels, and metadata describing the expected input format.
 *
 * Use this to build dynamic UIs for tax setup or to read the current tax configuration for a state.
 *
 * scope: `company_tax_requirements:read`
 */
export function useTaxRequirementsGet(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildTaxRequirementsGetQuery(client, request, options),
        ...options,
    });
}
/**
 * Get tax requirements for a state
 *
 * @remarks
 * Retrieves the detailed tax requirements for a specific state. The response includes requirement sets grouped by
 * category (e.g., registrations, tax rates, deposit schedules), each containing individual requirements with their
 * current values, labels, and metadata describing the expected input format.
 *
 * Use this to build dynamic UIs for tax setup or to read the current tax configuration for a state.
 *
 * scope: `company_tax_requirements:read`
 */
export function useTaxRequirementsGetSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildTaxRequirementsGetQuery(client, request, options),
        ...options,
    });
}
export function setTaxRequirementsGetData(client, queryKeyBase, data) {
    const key = queryKeyTaxRequirementsGet(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateTaxRequirementsGet(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api-v-2026-06-15",
            "taxRequirements",
            "get",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllTaxRequirementsGet(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api-v-2026-06-15", "taxRequirements", "get"],
    });
}
//# sourceMappingURL=taxRequirementsGet.js.map