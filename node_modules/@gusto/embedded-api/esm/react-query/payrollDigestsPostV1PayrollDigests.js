/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { payrollDigestsPostV1PayrollDigests } from "../funcs/payrollDigestsPostV1PayrollDigests.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Create a payroll digest batch
 *
 * @remarks
 * Triggers an asynchronous computation of payroll digest data (statuses, blockers, pay periods, totals) across up to 25 companies that the partner is mapped to.
 *
 * The batch is processed asynchronously. Use the returned batch UUID to poll `GET /v1/payroll_digests/{payroll_digest_uuid}` for status and results.
 *
 * Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate computation occurs.
 *
 * 📘 System Access Authentication
 *
 * This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
 *
 * scope: `payroll_digests:write`
 */
export function usePayrollDigestsPostV1PayrollDigestsMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildPayrollDigestsPostV1PayrollDigestsMutation(client, options),
        ...options,
    });
}
export function mutationKeyPayrollDigestsPostV1PayrollDigests() {
    return [
        "@gusto/embedded-api-v-2026-06-15",
        "Payroll Digests",
        "postV1PayrollDigests",
    ];
}
export function buildPayrollDigestsPostV1PayrollDigestsMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyPayrollDigestsPostV1PayrollDigests(),
        mutationFn: function payrollDigestsPostV1PayrollDigestsMutationFn({ security, request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(payrollDigestsPostV1PayrollDigests(client$, security, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=payrollDigestsPostV1PayrollDigests.js.map