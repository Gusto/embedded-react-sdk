import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
/**
 * The action being performed on the batch.
 */
export declare const PayrollDigestBatchAction: {
    readonly Create: "create";
};
/**
 * The action being performed on the batch.
 */
export type PayrollDigestBatchAction = ClosedEnum<typeof PayrollDigestBatchAction>;
/**
 * The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`.
 */
export declare const PayrollDigestStatus: {
    readonly Pending: "pending";
    readonly Processing: "processing";
    readonly Completed: "completed";
    readonly Failed: "failed";
};
/**
 * The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`.
 */
export type PayrollDigestStatus = ClosedEnum<typeof PayrollDigestStatus>;
/**
 * A payroll digest batch request.
 */
export type PayrollDigest = {
    /**
     * The unique identifier of the payroll digest batch.
     */
    uuid: string;
    /**
     * The idempotency key provided when creating the batch.
     */
    idempotencyKey: string;
    /**
     * The action being performed on the batch.
     */
    batchAction: PayrollDigestBatchAction;
    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`.
     */
    status: PayrollDigestStatus;
};
/** @internal */
export declare const PayrollDigestBatchAction$inboundSchema: z.ZodNativeEnum<typeof PayrollDigestBatchAction>;
/** @internal */
export declare const PayrollDigestStatus$inboundSchema: z.ZodNativeEnum<typeof PayrollDigestStatus>;
/** @internal */
export declare const PayrollDigest$inboundSchema: z.ZodType<PayrollDigest, z.ZodTypeDef, unknown>;
export declare function payrollDigestFromJSON(jsonString: string): SafeParseResult<PayrollDigest, SDKValidationError>;
//# sourceMappingURL=payrolldigest.d.ts.map