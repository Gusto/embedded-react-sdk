import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { BulkReportItemResult } from "./bulkreportitemresult.js";
/**
 * This company's overall status across its `reports`:
 *
 * @remarks
 * - `success`: every report succeeded
 * - `partial_success`: some succeeded, some failed
 * - `failed`: every report failed
 * - `pending`: at least one report is still being generated
 */
export declare const BulkReportCompanyStatus: {
    readonly Pending: "pending";
    readonly Success: "success";
    readonly PartialSuccess: "partial_success";
    readonly Failed: "failed";
};
/**
 * This company's overall status across its `reports`:
 *
 * @remarks
 * - `success`: every report succeeded
 * - `partial_success`: some succeeded, some failed
 * - `failed`: every report failed
 * - `pending`: at least one report is still being generated
 */
export type BulkReportCompanyStatus = ClosedEnum<typeof BulkReportCompanyStatus>;
/**
 * Results for a single company in a bulk report batch.
 */
export type BulkReportCompany = {
    /**
     * UUID of the company.
     */
    companyUuid: string;
    /**
     * This company's overall status across its `reports`:
     *
     * @remarks
     * - `success`: every report succeeded
     * - `partial_success`: some succeeded, some failed
     * - `failed`: every report failed
     * - `pending`: at least one report is still being generated
     */
    status: BulkReportCompanyStatus;
    reports: Array<BulkReportItemResult>;
};
/** @internal */
export declare const BulkReportCompanyStatus$inboundSchema: z.ZodNativeEnum<typeof BulkReportCompanyStatus>;
/** @internal */
export declare const BulkReportCompany$inboundSchema: z.ZodType<BulkReportCompany, z.ZodTypeDef, unknown>;
export declare function bulkReportCompanyFromJSON(jsonString: string): SafeParseResult<BulkReportCompany, SDKValidationError>;
//# sourceMappingURL=bulkreportcompany.d.ts.map