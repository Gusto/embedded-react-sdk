/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
/**
 * The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch crashed at the system level; can be retried). This is distinct from the per-payroll `status` returned inside `results[]`. A `completed` batch does not imply every payroll was cancelled.
 */
export const PayrollBatchResultsStatus = {
    Pending: "pending",
    Processing: "processing",
    Completed: "completed",
    Failed: "failed",
};
/**
 * The outcome of cancelling this payroll. A cancel is atomic — there is no per-payroll `partial_success`.
 *
 * @remarks
 * - `success`: the payroll was cancelled, or required no action (already cancelled / never run)
 * - `failed`: the payroll could not be cancelled; see `errors`
 */
export const PayrollBatchResultsResultsStatus = {
    Success: "success",
    Failed: "failed",
};
/**
 * Machine-readable reason the cancellation failed.
 *
 * @remarks
 * - `not_cancellable`: the payroll is past the point where it can be cancelled
 * - `internal_error`: an unexpected error occurred; the request can be retried
 */
export const PayrollBatchResultsResultsCategory = {
    NotCancellable: "not_cancellable",
    InternalError: "internal_error",
};
/**
 * The type of entity this exclusion represents.
 */
export const PayrollBatchResultsEntityType = {
    Payroll: "payroll",
};
/**
 * Always `failed` for an excluded payroll.
 */
export const PayrollBatchResultsExclusionsStatus = {
    Failed: "failed",
};
/**
 * Machine-readable category for why the payroll was excluded.
 *
 * @remarks
 * - `not_found`: the payroll does not exist, or is not associated with a company the partner is mapped to
 * - `duplicate_operation`: the same payroll UUID appeared more than once in the request; only the first occurrence is processed
 */
export const PayrollBatchResultsCategory = {
    NotFound: "not_found",
    DuplicateOperation: "duplicate_operation",
};
/** @internal */
export const PayrollBatchResultsStatus$inboundSchema = z.nativeEnum(PayrollBatchResultsStatus);
/** @internal */
export const PayrollBatchResultsResultsStatus$inboundSchema = z.nativeEnum(PayrollBatchResultsResultsStatus);
/** @internal */
export const PayrollBatchResultsResultsCategory$inboundSchema = z.nativeEnum(PayrollBatchResultsResultsCategory);
/** @internal */
export const PayrollBatchResultsErrors$inboundSchema = z.object({
    error_key: z.string().optional(),
    category: PayrollBatchResultsResultsCategory$inboundSchema.optional(),
    message: z.string().optional(),
}).transform((v) => {
    return remap$(v, {
        "error_key": "errorKey",
    });
});
export function payrollBatchResultsErrorsFromJSON(jsonString) {
    return safeParse(jsonString, (x) => PayrollBatchResultsErrors$inboundSchema.parse(JSON.parse(x)), `Failed to parse 'PayrollBatchResultsErrors' from JSON`);
}
/** @internal */
export const Results$inboundSchema = z.object({
    idx: z.number().int().optional(),
    uuid: z.string().optional(),
    status: PayrollBatchResultsResultsStatus$inboundSchema.optional(),
    errors: z.array(z.lazy(() => PayrollBatchResultsErrors$inboundSchema))
        .optional(),
});
export function resultsFromJSON(jsonString) {
    return safeParse(jsonString, (x) => Results$inboundSchema.parse(JSON.parse(x)), `Failed to parse 'Results' from JSON`);
}
/** @internal */
export const PayrollBatchResultsEntityType$inboundSchema = z.nativeEnum(PayrollBatchResultsEntityType);
/** @internal */
export const PayrollBatchResultsExclusionsStatus$inboundSchema = z.nativeEnum(PayrollBatchResultsExclusionsStatus);
/** @internal */
export const PayrollBatchResultsCategory$inboundSchema = z.nativeEnum(PayrollBatchResultsCategory);
/** @internal */
export const Exclusions$inboundSchema = z.object({
    idx: z.number().int().optional(),
    entity_type: PayrollBatchResultsEntityType$inboundSchema.optional(),
    uuid: z.string().optional(),
    company_uuid: z.string().optional(),
    status: PayrollBatchResultsExclusionsStatus$inboundSchema.optional(),
    category: PayrollBatchResultsCategory$inboundSchema.optional(),
    message: z.string().optional(),
}).transform((v) => {
    return remap$(v, {
        "entity_type": "entityType",
        "company_uuid": "companyUuid",
    });
});
export function exclusionsFromJSON(jsonString) {
    return safeParse(jsonString, (x) => Exclusions$inboundSchema.parse(JSON.parse(x)), `Failed to parse 'Exclusions' from JSON`);
}
/** @internal */
export const PayrollBatchResults$inboundSchema = z.object({
    uuid: z.string(),
    idempotency_key: z.string(),
    status: PayrollBatchResultsStatus$inboundSchema,
    submitted_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
    completed_at: z.nullable(z.string().datetime({ offset: true }).transform(v => new Date(v))).optional(),
    submitted_items: z.nullable(z.number().int()).optional(),
    processed_items: z.number().int().optional(),
    excluded_items: z.number().int().optional(),
    results: z.array(z.lazy(() => Results$inboundSchema)).optional(),
    exclusions: z.array(z.lazy(() => Exclusions$inboundSchema)).optional(),
}).transform((v) => {
    return remap$(v, {
        "idempotency_key": "idempotencyKey",
        "submitted_at": "submittedAt",
        "completed_at": "completedAt",
        "submitted_items": "submittedItems",
        "processed_items": "processedItems",
        "excluded_items": "excludedItems",
    });
});
export function payrollBatchResultsFromJSON(jsonString) {
    return safeParse(jsonString, (x) => PayrollBatchResults$inboundSchema.parse(JSON.parse(x)), `Failed to parse 'PayrollBatchResults' from JSON`);
}
//# sourceMappingURL=payrollbatchresults.js.map