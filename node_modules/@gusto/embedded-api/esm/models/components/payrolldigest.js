/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
/**
 * The action being performed on the batch.
 */
export const PayrollDigestBatchAction = {
    Create: "create",
};
/**
 * The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`.
 */
export const PayrollDigestStatus = {
    Pending: "pending",
    Processing: "processing",
    Completed: "completed",
    Failed: "failed",
};
/** @internal */
export const PayrollDigestBatchAction$inboundSchema = z.nativeEnum(PayrollDigestBatchAction);
/** @internal */
export const PayrollDigestStatus$inboundSchema = z.nativeEnum(PayrollDigestStatus);
/** @internal */
export const PayrollDigest$inboundSchema = z.object({
    uuid: z.string(),
    idempotency_key: z.string(),
    batch_action: PayrollDigestBatchAction$inboundSchema,
    status: PayrollDigestStatus$inboundSchema,
}).transform((v) => {
    return remap$(v, {
        "idempotency_key": "idempotencyKey",
        "batch_action": "batchAction",
    });
});
export function payrollDigestFromJSON(jsonString) {
    return safeParse(jsonString, (x) => PayrollDigest$inboundSchema.parse(JSON.parse(x)), `Failed to parse 'PayrollDigest' from JSON`);
}
//# sourceMappingURL=payrolldigest.js.map