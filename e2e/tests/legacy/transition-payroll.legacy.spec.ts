import { test } from '../../utils/localTestFixture'

test.describe('TransitionFlow — Legacy', () => {
  test.skip(true, 'Migrating to scenario-driven tests')

  test('placeholder for creation-to-execution tests', async () => {
    // Will be migrated once post-schedule-change scenario supports full flow
  })

  test('placeholder for full flow test', async () => {
    // Will be migrated once post-schedule-change scenario supports full flow
  })
})
