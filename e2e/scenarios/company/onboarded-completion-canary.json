{
  "$schema": "../../schema/scenario.schema.json",
  "name": "Company onboarded completion canary",
  "description": "Already-onboarded company demo. Loading /?flow=company-onboarding against it should land on the completed overview ('Nice! We'll take it from here') with a Done button — the terminal success state of the wizard. requireOnboardedCompany guards against the non-deterministic demo factory occasionally returning a not-actually-onboarded company.",
  "domain": "company",
  "baseDemo": "react_sdk_demo_company_onboarded",
  "decorations": {},
  "expectedContext": ["companyId"],
  "requireOnboardedCompany": true
}
