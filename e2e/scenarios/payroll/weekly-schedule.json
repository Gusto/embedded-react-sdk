{
  "$schema": "../../schema/scenario.schema.json",
  "name": "Weekly pay schedule",
  "description": "Onboarded company with a weekly pay schedule, used by weekly-cadence.spec.ts to verify the payroll landing renders correctly under weekly cadence (date math, period boundaries) — distinct from the biweekly default. The base demo provides the onboarded employees needed to populate the landing, so no decorated employees are needed here.",
  "domain": "payroll",
  "baseDemo": "react_sdk_demo_company_onboarded",
  "decorations": {
    "paySchedule": {
      "frequency": "Every week",
      "anchor_pay_date": "{{relative:+7d:Friday}}",
      "anchor_end_of_pay_period": "{{relative:+6d}}"
    }
  },
  "expectedContext": ["companyId", "paySchedule.uuid"]
}
