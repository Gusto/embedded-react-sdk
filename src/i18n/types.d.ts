// This file is autogenerated
import 'i18next'

declare module 'i18next' {
  interface CustomTypeOptions {
    defaultNS: 'common'
    resources: Resources
  }
}

/**
 * Every i18n resource namespace mapped to its keys. `keyof Resources` yields the
 * namespace names; `Resources['Company.Addresses']` (or {@link Translations.CompanyAddresses})
 * yields that namespace's keys. Backs i18next `t()` typing and `ResourceDictionary`.
 *
 * @public
 * @group Utility types
 */
export interface Resources {
  'Company.Addresses': Translations.CompanyAddresses
  'Company.AssignSignatory': Translations.CompanyAssignSignatory
  'Company.BankAccount': Translations.CompanyBankAccount
  'Company.DocumentList': Translations.CompanyDocumentList
  'Company.FederalTaxes': Translations.CompanyFederalTaxes
  'Company.Industry': Translations.CompanyIndustry
  'Company.Locations': Translations.CompanyLocations
  'Company.OnboardingOverview': Translations.CompanyOnboardingOverview
  'Company.PaySchedule': Translations.CompanyPaySchedule
  'Company.SignatureForm': Translations.CompanySignatureForm
  'Company.StateTaxes': Translations.CompanyStateTaxes
  'Company.TimeOff.CreateTimeOffPolicy': Translations.CompanyTimeOffCreateTimeOffPolicy
  'Company.TimeOff.EmployeeTable': Translations.CompanyTimeOffEmployeeTable
  'Company.TimeOff.HolidayPolicy': Translations.CompanyTimeOffHolidayPolicy
  'Company.TimeOff.PolicyDetail': Translations.CompanyTimeOffPolicyDetail
  'Company.TimeOff.SelectEmployees': Translations.CompanyTimeOffSelectEmployees
  'Company.TimeOff.SelectPolicyType': Translations.CompanyTimeOffSelectPolicyType
  'Company.TimeOff.TimeOffPolicies': Translations.CompanyTimeOffTimeOffPolicies
  'Company.TimeOff.TimeOffPolicyDetails': Translations.CompanyTimeOffTimeOffPolicyDetails
  'Company.TimeOff.TimeOffRequests': Translations.CompanyTimeOffTimeOffRequests
  'Contractor.Address': Translations.ContractorAddress
  'Contractor.ContractorList': Translations.ContractorContractorList
  'Contractor.DocumentsList': Translations.ContractorDocumentsList
  'Contractor.Landing': Translations.ContractorLanding
  'Contractor.ManagementContractorList': Translations.ContractorManagementContractorList
  'Contractor.NewHireReport': Translations.ContractorNewHireReport
  'Contractor.OnboardingSummary': Translations.ContractorOnboardingSummary
  'Contractor.PaymentMethod': Translations.ContractorPaymentMethod
  'Contractor.Payments.CreatePayment': Translations.ContractorPaymentsCreatePayment
  'Contractor.Payments.PaymentHistory': Translations.ContractorPaymentsPaymentHistory
  'Contractor.Payments.PaymentStatement': Translations.ContractorPaymentsPaymentStatement
  'Contractor.Payments.PaymentSummary': Translations.ContractorPaymentsPaymentSummary
  'Contractor.Payments.PaymentsList': Translations.ContractorPaymentsPaymentsList
  'Contractor.Profile': Translations.ContractorProfile
  'Contractor.SignatureForm': Translations.ContractorSignatureForm
  'Contractor.Submit': Translations.ContractorSubmit
  'Employee.BankAccount': Translations.EmployeeBankAccount
  'Employee.BankFormBody': Translations.EmployeeBankFormBody
  'Employee.Compensation': Translations.EmployeeCompensation
  'Employee.Dashboard': Translations.EmployeeDashboard
  'Employee.Deductions': Translations.EmployeeDeductions
  'Employee.DeductionsForm': Translations.EmployeeDeductionsForm
  'Employee.DocumentManager': Translations.EmployeeDocumentManager
  'Employee.DocumentSigner': Translations.EmployeeDocumentSigner
  'Employee.EmployeeDocuments': Translations.EmployeeEmployeeDocuments
  'Employee.EmployeeList': Translations.EmployeeEmployeeList
  'Employee.EmploymentEligibility': Translations.EmployeeEmploymentEligibility
  'Employee.FederalTaxes': Translations.EmployeeFederalTaxes
  'Employee.FederalTaxesView': Translations.EmployeeFederalTaxesView
  'Employee.HomeAddress': Translations.EmployeeHomeAddress
  'Employee.I9SignatureForm': Translations.EmployeeI9SignatureForm
  'Employee.Landing': Translations.EmployeeLanding
  'Employee.Management.Compensation': Translations.EmployeeManagementCompensation
  'Employee.Management.Deductions': Translations.EmployeeManagementDeductions
  'Employee.Management.Documents': Translations.EmployeeManagementDocuments
  'Employee.Management.FederalTaxes': Translations.EmployeeManagementFederalTaxes
  'Employee.Management.HomeAddress': Translations.EmployeeManagementHomeAddress
  'Employee.Management.PaymentMethod': Translations.EmployeeManagementPaymentMethod
  'Employee.Management.PaymentMethodBankForm': Translations.EmployeeManagementPaymentMethodBankForm
  'Employee.Management.PaymentMethodSplitForm': Translations.EmployeeManagementPaymentMethodSplitForm
  'Employee.Management.Paystubs': Translations.EmployeeManagementPaystubs
  'Employee.Management.Profile': Translations.EmployeeManagementProfile
  'Employee.Management.StateTaxes': Translations.EmployeeManagementStateTaxes
  'Employee.Management.WorkAddress': Translations.EmployeeManagementWorkAddress
  'Employee.ManagementEmployeeList': Translations.EmployeeManagementEmployeeList
  'Employee.OnboardingSummary': Translations.EmployeeOnboardingSummary
  'Employee.PaySchedules': Translations.EmployeePaySchedules
  'Employee.PaymentMethod': Translations.EmployeePaymentMethod
  'Employee.Profile': Translations.EmployeeProfile
  'Employee.SplitPaycheck': Translations.EmployeeSplitPaycheck
  'Employee.SplitPaymentsFormBody': Translations.EmployeeSplitPaymentsFormBody
  'Employee.StateTaxes': Translations.EmployeeStateTaxes
  'Employee.StateTaxesView': Translations.EmployeeStateTaxesView
  'Employee.Terminations.TerminateEmployee': Translations.EmployeeTerminationsTerminateEmployee
  'Employee.Terminations.TerminationFlow': Translations.EmployeeTerminationsTerminationFlow
  'Employee.Terminations.TerminationSummary': Translations.EmployeeTerminationsTerminationSummary
  'InformationRequests.InformationRequestForm': Translations.InformationRequestsInformationRequestForm
  'InformationRequests.InformationRequestList': Translations.InformationRequestsInformationRequestList
  InformationRequests: Translations.InformationRequests
  'Payroll.Common': Translations.PayrollCommon
  'Payroll.ConfirmWireDetailsBanner': Translations.PayrollConfirmWireDetailsBanner
  'Payroll.ConfirmWireDetailsForm': Translations.PayrollConfirmWireDetailsForm
  'Payroll.Dismissal': Translations.PayrollDismissal
  'Payroll.EmployeeSelection': Translations.PayrollEmployeeSelection
  'Payroll.GrossUpModal': Translations.PayrollGrossUpModal
  'Payroll.OffCycle': Translations.PayrollOffCycle
  'Payroll.OffCycleCreation': Translations.PayrollOffCycleCreation
  'Payroll.OffCycleDeductionsSetting': Translations.PayrollOffCycleDeductionsSetting
  'Payroll.OffCyclePayPeriodDateForm': Translations.PayrollOffCyclePayPeriodDateForm
  'Payroll.OffCycleReasonSelection': Translations.PayrollOffCycleReasonSelection
  'Payroll.OffCycleTaxWithholding': Translations.PayrollOffCycleTaxWithholding
  'Payroll.PayrollBlocker': Translations.PayrollPayrollBlocker
  'Payroll.PayrollConfiguration': Translations.PayrollPayrollConfiguration
  'Payroll.PayrollEditEmployee': Translations.PayrollPayrollEditEmployee
  'Payroll.PayrollFlow': Translations.PayrollPayrollFlow
  'Payroll.PayrollHistory': Translations.PayrollPayrollHistory
  'Payroll.PayrollLanding': Translations.PayrollPayrollLanding
  'Payroll.PayrollList': Translations.PayrollPayrollList
  'Payroll.PayrollOverview': Translations.PayrollPayrollOverview
  'Payroll.PayrollReceipts': Translations.PayrollPayrollReceipts
  'Payroll.RecoveryCasesList': Translations.PayrollRecoveryCasesList
  'Payroll.RecoveryCasesResubmit': Translations.PayrollRecoveryCasesResubmit
  'Payroll.Transition': Translations.PayrollTransition
  'Payroll.TransitionCreation': Translations.PayrollTransitionCreation
  'Payroll.TransitionPayrollAlert': Translations.PayrollTransitionPayrollAlert
  'Payroll.WireInstructions': Translations.PayrollWireInstructions
  common: Translations.common
}

/**
 * All user-facing text rendered by SDK components, organized by namespace.
 *
 * Every key includes its English default; pass overrides through a component's
 * `dictionary` prop or the global `GustoProvider` dictionary.
 *
 * @public
 */
export namespace Translations {
  /** Translation keys for the `Company.Addresses` i18n namespace. */
  export interface CompanyAddresses {
    /** @defaultValue `"Company addresses"` */
    title: string
    /** @defaultValue `"To automate your payroll filings, we need to have your company's accurate addresses. Please enter your mailing and filing addresses and all addresses where you have employees physically working in the United States."` */
    description: string
    /** @defaultValue `"Mailing Address"` */
    mailingLabel: string
    /** @defaultValue `"Filing Address"` */
    filingLabel: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Add address"` */
    addCta: string
    /** @defaultValue `"I've added all addresses"` */
    continueCta: string
  }
  /** Translation keys for the `Company.AssignSignatory` i18n namespace. */
  export interface CompanyAssignSignatory {
    /** @defaultValue `"Assign a company signatory"` */
    title: string
    /** @defaultValue `"The company signatory is responsible for electronically signing all government forms Gusto creates and is generally a member of the business or partnership. Each time a signatory is updated, we'll file a new Form 8655 with the IRS to give Gusto the \"Reporting Agent Authorization\" we need to file and pay your taxes. Your signatory must be authorized by the IRS."` */
    description: string
    signingOptions: {
      /** @defaultValue `"Signing options"` */
      label: string
      /** @defaultValue `"I'm authorized to sign official documents for the company."` */
      selfSign: string
      /** @defaultValue `"I'll appoint another person to sign official documents."` */
      appointOther: string
    }
    signatoryDetails: {
      /** @defaultValue `"Signatory person details"` */
      title: string
      /** @defaultValue `"Make sure this information is accurate. We'll use it to submit IRS Form 8655 so we can file and pay your federal taxes. If you don't know who your authorized signatory is, please call the IRS at (800) 829-4933."` */
      description: string
      /** @defaultValue `"First name"` */
      firstName: string
      /** @defaultValue `"Last name"` */
      lastName: string
      /** @defaultValue `"Email address"` */
      email: string
      /** @defaultValue `"Phone number"` */
      phone: string
      /** @defaultValue `"Social security number"` */
      ssn: string
      /** @defaultValue `"Date of birth"` */
      birthday: string
      titleSelect: {
        /** @defaultValue `"Title"` */
        label: string
        /** @defaultValue `"Select title..."` */
        placeholder: string
      }
    }
    address: {
      /** @defaultValue `"Signatory home address"` */
      title: string
      /** @defaultValue `"To verify your identity, please enter your home address. If you're unsure what address to use, use the one listed on your driver's license."` */
      description: string
      /** @defaultValue `"Street 1"` */
      street1: string
      /** @defaultValue `"Street 2"` */
      street2: string
      /** @defaultValue `"City"` */
      city: string
      /** @defaultValue `"State"` */
      state: string
      /** @defaultValue `"Select state..."` */
      statePlaceholder: string
      /** @defaultValue `"Zip"` */
      zip: string
    }
    inviteSignatory: {
      /** @defaultValue `"Invite a signatory"` */
      title: string
      /** @defaultValue `"Make sure this information is accurate. We'll use it to submit IRS Form 8655 so we can file and pay your federal taxes. If you don't know who your authorized signatory is, please call the IRS at (800) 829-4933."` */
      description: string
      /** @defaultValue `"Signatory email"` */
      signatoryEmail: string
      /** @defaultValue `"Confirm signatory email"` */
      confirmEmail: string
      /** @defaultValue `"First name"` */
      firstName: string
      /** @defaultValue `"Last name"` */
      lastName: string
    }
    buttons: {
      /** @defaultValue `"Sign documents"` */
      signDocuments: string
      /** @defaultValue `"Invite signatory"` */
      inviteSignatory: string
    }
    validations: {
      /** @defaultValue `"Date of birth is required"` */
      dob: string
      /** @defaultValue `"Email address is required and must be valid"` */
      email: string
      /** @defaultValue `"Email addresses must match"` */
      emailMismatch: string
      /** @defaultValue `"First name is required"` */
      firstName: string
      /** @defaultValue `"Last name is required"` */
      lastName: string
      /** @defaultValue `"Phone number must be 10 digits"` */
      phone: string
      /** @defaultValue `"Title is required"` */
      title: string
      address: {
        /** @defaultValue `"Street address is required"` */
        street1: string
        /** @defaultValue `"City is required"` */
        city: string
        /** @defaultValue `"State is required"` */
        state: string
        /** @defaultValue `"ZIP code must be 5 digits"` */
        zip: string
      }
    }
  }
  /** Translation keys for the `Company.BankAccount` i18n namespace. */
  export interface CompanyBankAccount {
    /** @defaultValue `"Company bank account"` */
    addBankAccountTitle: string
    /** @defaultValue `"We’ll use your checking account info to debit for wages and taxes. Your account must be linked to a checking bank account. Credit payments, credit cards, and savings accounts are not accepted."` */
    addBankAccountDescription: string
    /** @defaultValue `"Routing number"` */
    routingNumberLabel: string
    /** @defaultValue `"Account"` */
    accountNumberLabel: string
    verificationAlert: {
      awaiting_deposits: {
        /** @defaultValue `"Verification pending"` */
        label: string
        /** @defaultValue `"We're sending two test deposits to the bank account below. You should expect to see them in 1-2 business days. After receiving the deposits, please add them on the Bank verification step."` */
        description: string
      }
      ready_for_verification: {
        /** @defaultValue `"Verify your bank account"` */
        label: string
        /** @defaultValue `"Two test deposits were successfully sent to account ending in {{number}}."` */
        description: string
      }
      verified: {
        /** @defaultValue `"Your bank account has been verified!"` */
        label: string
      }
    }
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Change bank account"` */
    changeBankAccountCta: string
    /** @defaultValue `"Verify bank account"` */
    verifyBankAccountCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    form: {
      /** @defaultValue `"Routing number"` */
      routingNumberLabel: string
      /** @defaultValue `"Enter your 9-digit routing number"` */
      routingNumberDescription: string
      /** @defaultValue `"Account number"` */
      accountNumberLabel: string
    }
    validations: {
      /** @defaultValue `"Routing number is required"` */
      routingNumber: string
      /** @defaultValue `"Account number is required"` */
      accountNumber: string
      /** @defaultValue `"Deposit 1 is required"` */
      deposit1: string
      /** @defaultValue `"Deposit 2 is required"` */
      deposit2: string
    }
    /** @defaultValue `"Verify bank account"` */
    verifyBankAccountTitle: string
    /** @defaultValue `"Find the two small deposits Gusto made into your company bank account. Input the values below and click “Verify deposits” to continue setup."` */
    verifyBankAccountDescription: string
    /** @defaultValue `"Test deposit #1"` */
    deposit1Label: string
    /** @defaultValue `"Enter first deposit amount in dollars, e.g. 0.02 for 2 cents"` */
    deposit1Description: string
    /** @defaultValue `"Test deposit #2"` */
    deposit2Label: string
    /** @defaultValue `"Enter second deposit amount in dollars, e.g. 0.02 for 2 cents"` */
    deposit2Description: string
    /** @defaultValue `"Verify deposits"` */
    verifyCta: string
  }
  /** Translation keys for the `Company.DocumentList` i18n namespace. */
  export interface CompanyDocumentList {
    /** @defaultValue `"Documents"` */
    documentListTitle: string
    /** @defaultValue `"Documents"` */
    documentListLabel: string
    /** @defaultValue `"Form"` */
    formColumnLabel: string
    /** @defaultValue `"Action"` */
    actionColumnLabel: string
    /** @defaultValue `"Please note, only the signatory can sign documents."` */
    selfSignatoryTitle: string
    /** @defaultValue `"Only the signatory can sign documents."` */
    otherSignatoryTitle: string
    /** @defaultValue `"You are the assigned signatory."` */
    selfSignatoryDescription: string
    /** @defaultValue `"Your signatory is {{signatory}}, {{title}}."` */
    otherSignatoryDescription: string
    /** @defaultValue `"A signatory has not yet been assigned"` */
    noSignatoryDescription: string
    /** @defaultValue `"Change signatory"` */
    changeSignatoryCta: string
    /** @defaultValue `"Assign signatory"` */
    assignSignatoryCta: string
    /** @defaultValue `"Sign document"` */
    signDocumentCta: string
    /** @defaultValue `"Complete"` */
    signDocumentComplete: string
    /** @defaultValue `"Not signed"` */
    notSigned: string
    /** @defaultValue `"Could not load your documents, try again later."` */
    documentListError: string
    /** @defaultValue `"No documents found"` */
    emptyTableTitle: string
    /** @defaultValue `"Continue"` */
    continueCta: string
  }
  /** Translation keys for the `Company.FederalTaxes` i18n namespace. */
  export interface CompanyFederalTaxes {
    /** @defaultValue `"Federal Tax Information"` */
    pageTitle: string
    /** @defaultValue `"Enter your entity type and the legal name of your company. You can find this info on your <einLink>FEIN assignment form (Form CP575)</einLink>. We need this to file and pay your taxes correctly."` */
    entityTypeAndLegalNameIntro: string
    /** @defaultValue `"Federal EIN"` */
    federalEinLabel: string
    /** @defaultValue `"Your company's Federal Employer Identification Number (EIN). If you don't have one, please <applyLink>apply online.</applyLink>"` */
    federalEinDescription: string
    /** @defaultValue `"Taxpayer type"` */
    taxpayerTypeLabel: string
    /** @defaultValue `"Some common types are Sole Prop, LLC, and S-Corp."` */
    taxpayerTypeDescription: string
    /** @defaultValue `"Select taxpayer type..."` */
    taxpayerTypePlaceholder: string
    /** @defaultValue `"Federal filing form"` */
    federalFilingFormLabel: string
    /** @defaultValue `"To learn more about the different Federal Tax Form filings for payroll, please review the <irsLink>IRS website.</irsLink>"` */
    federalFilingFormDescription: string
    /** @defaultValue `"Select filing form..."` */
    federalFilingFormPlaceholder: string
    /** @defaultValue `"Legal entity name"` */
    legalEntityNameLabel: string
    /** @defaultValue `"Make sure this is the legal name of the company, not your DBA."` */
    legalEntityNameDescription: string
    /** @defaultValue `"Legal entity name is required"` */
    legalEntityNameError: string
    taxPayerType: {
      /** @defaultValue `"C-Corporation"` */
      'C-Corporation': string
      /** @defaultValue `"S-Corporation"` */
      'S-Corporation': string
      /** @defaultValue `"Sole proprietor"` */
      'Sole proprietor': string
      /** @defaultValue `"LLC"` */
      LLC: string
      /** @defaultValue `"LLP"` */
      LLP: string
      /** @defaultValue `"Limited partnership"` */
      'Limited partnership': string
      /** @defaultValue `"Co-ownership"` */
      'Co-ownership': string
      /** @defaultValue `"Association"` */
      Association: string
      /** @defaultValue `"Trusteeship"` */
      Trusteeship: string
      /** @defaultValue `"General partnership"` */
      'General partnership': string
      /** @defaultValue `"Joint venture"` */
      'Joint venture': string
      /** @defaultValue `"Non-Profit"` */
      'Non-Profit': string
    }
    filingForm: {
      /** @defaultValue `"941 - Employer's Quarterly Federal Tax Return"` */
      '941': string
      /** @defaultValue `"944 - Employer's Annual Federal Tax Return"` */
      '944': string
    }
    /** @defaultValue `"Continue"` */
    continueCta: string
  }
  /** Translation keys for the `Company.Industry` i18n namespace. */
  export interface CompanyIndustry {
    /** @defaultValue `"Industry"` */
    title: string
    /** @defaultValue `"Select the industry that most closely matches your company's. Think you fall between industries? Choose the one that best represents your primary business activity (this is usually the activity that generates the most income for your company)."` */
    description: string
    /** @defaultValue `"Select your company's industry"` */
    label: string
    /** @defaultValue `"Type a few letters to search for your industry"` */
    placeholder: string
    /** @defaultValue `"Continue"` */
    submitCta: string
  }
  /** Translation keys for the `Company.Locations` i18n namespace. */
  export interface CompanyLocations {
    /** @defaultValue `"Company addresses"` */
    locationsListTitle: string
    /** @defaultValue `"To automate your payroll filings, we need to have your company's accurate addresses. Please enter your mailing and filing addresses and all addresses where you have employees physically working in the United States."` */
    locationsListDescription: string
    /** @defaultValue `"List of company addresses"` */
    locationListLabel: string
    /** @defaultValue `"Addresses"` */
    locationListCol1: string
    /** @defaultValue `"Address status"` */
    locationListCol2: string
    /** @defaultValue `"Filing Address"` */
    filingAddress: string
    /** @defaultValue `"Mailing Address"` */
    mailingAddress: string
    /** @defaultValue `"Location actions"` */
    hamburgerTitle: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"No addresses"` */
    emptyTableTitle: string
    /** @defaultValue `"Once you’ve added addresses they will appear here"` */
    emptyTableDescription: string
    /** @defaultValue `"+ Add another address"` */
    addLocationCta: string
    /** @defaultValue `"+ Add an address"` */
    addFirstLocationCta: string
    /** @defaultValue `"Add company address"` */
    locationFormTitle: string
    /** @defaultValue `"We will need to collect and add any employee’s physical working address in the US including remote employees and employees who work from home"` */
    locationFormDescription: string
    /** @defaultValue `"Street 1"` */
    street1Label: string
    /** @defaultValue `"Street 2"` */
    street2Label: string
    /** @defaultValue `"City"` */
    cityLabel: string
    /** @defaultValue `"State"` */
    stateLabel: string
    /** @defaultValue `"Select state..."` */
    statePlaceholder: string
    /** @defaultValue `"Zip"` */
    zipLabel: string
    /** @defaultValue `"Phone number"` */
    phoneNumberLabel: string
    /** @defaultValue `"Address types"` */
    addressTypeLabel: string
    /** @defaultValue `"Mailing address"` */
    mailingAddressLabel: string
    /** @defaultValue `"This is where you'd like to receive mail. It's usually the same as your filing address."` */
    mailingAddressDescription: string
    /** @defaultValue `"This is your company's mailing address. To change it, designate a different location as the mailing address."` */
    mailingAddressDescriptionLocked: string
    /** @defaultValue `"Filing address"` */
    filingAddressLabel: string
    /** @defaultValue `"This is your primary physical place of business. It can't be a P.O. Box. The filing address should match the address you have on file with the IRS, which you can find on your federal EIN assignment form (Form CP575). We'll use it for all local, state, and federal filings."` */
    filingAddressDescription: string
    /** @defaultValue `"This is your company's filing address. To change it, designate a different location as the filing address."` */
    filingAddressDescriptionLocked: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    validations: {
      /** @defaultValue `"Street address is required"` */
      street1: string
      /** @defaultValue `"Please provide valid city name"` */
      city: string
      /** @defaultValue `"Please select a state"` */
      state: string
      /** @defaultValue `"Please provide valid zip code"` */
      zip: string
      /** @defaultValue `"Please provide valid phone number"` */
      phone: string
    }
  }
  /** Translation keys for the `Company.OnboardingOverview` i18n namespace. */
  export interface CompanyOnboardingOverview {
    /** @defaultValue `"Nice! We’ll take it from here."` */
    onboardingCompletedTitle: string
    /** @defaultValue `"We’ll review the details to make sure we have everything we need to pay your team. Look out for an email once your payroll is approved."` */
    onboardingCompletedDescription: string
    /** @defaultValue `"Done"` */
    onboardingCompletedCta: string
    /** @defaultValue `"We need a few more details"` */
    missingRequirementsTitle: string
    /** @defaultValue `"We need a bit more information to get your company ready for payroll."` */
    missingRequirementsDescription: string
    /** @defaultValue `"Let’s get started"` */
    initialSetupTitle: string
    /** @defaultValue `"We need some information to get your company ready for payroll."` */
    initialSetupDescription: string
    /** @defaultValue `"Continue onboarding"` */
    missingRequirementsCta: string
    /** @defaultValue `"Start onboarding"` */
    initialSetupCta: string
    stepTitles: {
      /** @defaultValue `"Add company addresses"` */
      add_addresses: string
      /** @defaultValue `"Company federal tax information"` */
      federal_tax_setup: string
      /** @defaultValue `"Select industry"` */
      select_industry: string
      /** @defaultValue `"Payroll account"` */
      add_bank_info: string
      /** @defaultValue `"Add employees"` */
      add_employees: string
      /** @defaultValue `"State tax"` */
      state_setup: string
      /** @defaultValue `"Select a Pay Schedule"` */
      payroll_schedule: string
      /** @defaultValue `"Sign documents"` */
      sign_all_forms: string
      /** @defaultValue `"Verify bank account"` */
      verify_bank_info: string
      /** @defaultValue `"External payroll"` */
      external_payroll: string
    }
    stepDescriptions: {
      /** @defaultValue `"Your mailing and filing addresses and all addresses where you have employees physically working in the United States."` */
      add_addresses: string
      /** @defaultValue `"Enter your federal tax account information."` */
      federal_tax_setup: string
      /** @defaultValue `"Set the industry of your company"` */
      select_industry: string
      /** @defaultValue `"The bank account you will use for payroll."` */
      add_bank_info: string
      /** @defaultValue `"Add your employees to run payroll."` */
      add_employees: string
      /** @defaultValue `"Enter state tax details for the states where your employees will be physically working."` */
      state_setup: string
      /** @defaultValue `"Select a Pay Schedule"` */
      payroll_schedule: string
      /** @defaultValue `"Sign required documents to complete setup."` */
      sign_all_forms: string
      /** @defaultValue `"Verify your bank account information."` */
      verify_bank_info: string
      /** @defaultValue `"External payroll"` */
      external_payroll: string
    }
  }
  /** Translation keys for the `Company.PaySchedule` i18n namespace. */
  export interface CompanyPaySchedule {
    /** @defaultValue `"Pick what frequency you'd like to run payroll. If you need help, you can read more about <ScheduleLink href=\"https://gusto.com/resources/articles/payroll/best-payroll-schedule-small-business\" target=\"_blank\">how to choose a pay schedule.</ScheduleLink>"` */
    listDescription: string
    /** @defaultValue `"Why do we need to ask for this? We need to know when to pay your employees. Some states have <PaymentLawLink href=\"https://www.dol.gov/agencies/whd/state/payday\" target=\"_blank\">laws around when you must pay your employees.</PaymentLawLink> Please choose pay schedules that are legal for your employees."` */
    listDescription2: string
    /** @defaultValue `"+ Add another pay schedule"` */
    addAnotherPayScheduleCta: string
    /** @defaultValue `"Save & continue"` */
    saveAndContinueCta: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Please make sure to verify this information is accurate. If this information isn't correct, it can delay when your team will be paid."` */
    pleaseVerify: string
    payScheduleList: {
      /** @defaultValue `"Name"` */
      name: string
      /** @defaultValue `"Actions"` */
      actions: string
      /** @defaultValue `"Active"` */
      active: string
      /** @defaultValue `"Inactive"` */
      inactive: string
      /** @defaultValue `"Edit"` */
      edit: string
    }
    /** @defaultValue `"Pay schedules"` */
    payScheduleListLabel: string
    headings: {
      /** @defaultValue `"Add pay schedule"` */
      addPaySchedule: string
      /** @defaultValue `"Edit pay schedule"` */
      editPaySchedule: string
      /** @defaultValue `"Set up pay schedule"` */
      pageTitle: string
    }
    labels: {
      /** @defaultValue `"Name"` */
      name: string
      /** @defaultValue `"Pay frequency"` */
      payfrequency: string
      /** @defaultValue `"Frequency"` */
      frequency: string
      /** @defaultValue `"Select frequency..."` */
      frequencyPlaceholder: string
      /** @defaultValue `"Deadline to run payroll"` */
      deadline: string
      /** @defaultValue `"Frequency Options"` */
      frequencyOptions: string
      /** @defaultValue `"First pay date"` */
      firstPayDate: string
      /** @defaultValue `"First pay period end date"` */
      firstPayPeriodEndDate: string
      /** @defaultValue `"First pay day of the month"` */
      firstPayDayOfTheMonth: string
      /** @defaultValue `"Last pay day of the month"` */
      lastPayDayOfTheMonth: string
      /** @defaultValue `"Preview"` */
      preview: string
      /** @defaultValue `"Legend"` */
      legend: string
    }
    /** @defaultValue `"Loading..."` */
    loading: string
    descriptions: {
      /** @defaultValue `"Select the pay days for the month."` */
      frequencyOptionsDescription: string
      /** @defaultValue `"Please account for the {{count}} day it will take to process payroll."` */
      anchorPayDateDescription_one: string
      /** @defaultValue `"Please account for the {{count}} days it will take to process payroll."` */
      anchorPayDateDescription_other: string
      /** @defaultValue `"The last date of the first pay period to help calculate future pay periods. This can be the same date as the first pay date."` */
      anchorEndOfPayPeriodDescription: string
    }
    payPreview: {
      /** @defaultValue `"Pay period"` */
      payPeriod: string
      /** @defaultValue `"Payday"` */
      payday: string
      /** @defaultValue `"Run payroll by 4:00PM (PDT) on "` */
      payrollDeadline: string
    }
    actions: {
      /** @defaultValue `"Cancel"` */
      cancel: string
      /** @defaultValue `"Save"` */
      save: string
    }
    frequencies: {
      /** @defaultValue `"Every week"` */
      everyWeek: string
      /** @defaultValue `"Every other week"` */
      everyOtherWeek: string
      /** @defaultValue `"Twice per month"` */
      twicePerMonth: string
      /** @defaultValue `"Monthly"` */
      monthly: string
    }
    frequencyOptions: {
      /** @defaultValue `"15th and Last day of the month"` */
      '15thAndLast': string
      /** @defaultValue `"Custom"` */
      custom: string
    }
    validations: {
      /** @defaultValue `"Pay schedule name is required"` */
      name: string
      /** @defaultValue `"Pay frequency is required"` */
      frequency: string
      /** @defaultValue `"First pay date is required"` */
      firstPayDate: string
      /** @defaultValue `"First pay period end date is required"` */
      firstPayPeriodEndDate: string
      /** @defaultValue `"First pay day of the month is required"` */
      firstPayDayOfTheMonth: string
      /** @defaultValue `"Last pay day of the month is required"` */
      lastPayDayOfTheMonth: string
      /** @defaultValue `"Must be between 1 and 31"` */
      dayRange: string
    }
    previewAlert: {
      /** @defaultValue `"Pay Schedule Preview"` */
      title: string
      /** @defaultValue `"Complete all the required fields to see a preview of your pay schedule."` */
      description: string
    }
  }
  /** Translation keys for the `Company.SignatureForm` i18n namespace. */
  export interface CompanySignatureForm {
    /** @defaultValue `"Signature required for {{formTitle}}"` */
    signatureFormTitle: string
    /** @defaultValue `"Please review the document. When you're finished, sign below."` */
    downloadInstructions: string
    /** @defaultValue `"View document"` */
    viewDocumentCta: string
    /** @defaultValue `"Signature"` */
    signatureLabel: string
    /** @defaultValue `"Type your full, legal name."` */
    signatureDescription: string
    /** @defaultValue `"Signature is required"` */
    signatureError: string
    /** @defaultValue `"I am the signatory and I agree to sign electronically"` */
    confirmationLabel: string
    /** @defaultValue `"You must agree to sign electronically"` */
    confirmationError: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Sign form"` */
    submitCta: string
    /** @defaultValue `"{{description}} You may also <downloadLink>download this document</downloadLink>."` */
    downloadPrompt: string
  }
  /** Translation keys for the `Company.StateTaxes` i18n namespace. */
  export interface CompanyStateTaxes {
    list: {
      /** @defaultValue `"Self-onboarding employees can change your company tax requirements"` */
      selfOnboardingWarningLabel: string
      /** @defaultValue `"If your employees are self-onboarding and enter details that change your company's state tax requirements (such as a home address in a new state), you may need to complete state tax setup again."` */
      selfOnboardingWarningDescription: string
      /** @defaultValue `"Continue"` */
      continueCta: string
      /** @defaultValue `"Edit"` */
      editStateTaxCta: string
      /** @defaultValue `"Continue setup"` */
      continueStateTaxSetupCta: string
      /** @defaultValue `"Manage tax rates"` */
      manageRatesCta: string
      /** @defaultValue `"Actions for {{state}}"` */
      hamburgerTitle: string
      /** @defaultValue `"State"` */
      requirementsListCol1: string
      /** @defaultValue `"Status"` */
      requirementsListCol2: string
      /** @defaultValue `"Complete"` */
      completeBadge: string
      /** @defaultValue `"Missing information"` */
      incompleteBadge: string
      /** @defaultValue `"Not started"` */
      notStartedBadge: string
      /** @defaultValue `"In progress"` */
      inProgressBadge: string
      /** @defaultValue `"Start setup"` */
      startStateTaxSetupCta: string
      /** @defaultValue `"Default tax rates applied"` */
      defaultRatesAppliedText: string
      /** @defaultValue `"Ready to run payroll"` */
      readyToRunPayrollBadge: string
      /** @defaultValue `"State tax requirements"` */
      requirementsListLabel: string
      /** @defaultValue `"No state tax requirements"` */
      emptyTableTitle: string
      /** @defaultValue `"Add company location or an employee to get started."` */
      emptyTableDescription: string
    }
    form: {
      /** @defaultValue `"Save"` */
      saveCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
      /** @defaultValue `"{{state}} tax requirements"` */
      title: string
      /** @defaultValue `"Effective: {{date}}"` */
      effectiveFromLabel: string
      validations: {
        /** @defaultValue `"Minimum value is {{min}}%"` */
        minValue: string
        /** @defaultValue `"Maximum value is {{max}}%"` */
        maxValue: string
        /** @defaultValue `"Value must be one of: {{values}}"` */
        oneOf: string
        /** @defaultValue `"This field is required"` */
        required: string
      }
    }
    manageRates: {
      /** @defaultValue `"Tax rates for {{state}}"` */
      title: string
      /** @defaultValue `"Effective-dated tax rate history and scheduled future rates."` */
      subtitle: string
      /** @defaultValue `"Back to states"` */
      backCta: string
      /** @defaultValue `"Add tax rate"` */
      addRateCta: string
      /** @defaultValue `"Effective date"` */
      effectiveDateLabel: string
      /** @defaultValue `"Select an effective date"` */
      effectiveDatePlaceholder: string
      /** @defaultValue `"Effective date"` */
      effectiveDateColumnLabel: string
      /** @defaultValue `"Current"` */
      currentBadge: string
      /** @defaultValue `"Scheduled"` */
      scheduledBadge: string
      /** @defaultValue `"Historical"` */
      historicalBadge: string
      /** @defaultValue `"No effective-dated rates yet"` */
      emptyHistoryTitle: string
      /** @defaultValue `"Add a tax rate to schedule a future-dated configuration."` */
      emptyHistoryDescription: string
      /** @defaultValue `"Add tax rate for {{state}}"` */
      addRateDialogTitle: string
      /** @defaultValue `"Schedule a new tax configuration to take effect on a future date."` */
      addRateDialogDescription: string
      /** @defaultValue `"Save tax rate"` */
      saveRateCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
      /** @defaultValue `"No editable fields"` */
      noEditableFieldsTitle: string
      /** @defaultValue `"This state does not expose editable effective-dated tax requirements."` */
      noEditableFieldsDescription: string
    }
  }
  /** Translation keys for the `Company.TimeOff.CreateTimeOffPolicy` i18n namespace. */
  export interface CompanyTimeOffCreateTimeOffPolicy {
    policyDetails: {
      /** @defaultValue `"Policy details"` */
      createTitle: string
      /** @defaultValue `"Edit {{name}}"` */
      editTitle: string
      /** @defaultValue `"Policy name"` */
      policyNameLabel: string
      /** @defaultValue `"Accrual method"` */
      accrualMethodLabel: string
      /** @defaultValue `"The rate at which employees will accrue time paid time off."` */
      accrualMethodHint: string
      /** @defaultValue `"Based on hours worked"` */
      perHourPaidLabel: string
      /** @defaultValue `"Employees earn time off based on hours worked (e.g., 1 hour for every 40 hours worked)."` */
      perHourPaidHint: string
      /** @defaultValue `"Fixed amount per year"` */
      perYearLabel: string
      /** @defaultValue `"Employees earn a set amount of time off each year. (e.g., 120 hours per year)."` */
      perYearHint: string
      /** @defaultValue `"Unlimited"` */
      unlimitedLabel: string
      /** @defaultValue `"Employees can take flexible time off with no set limit."` */
      unlimitedHint: string
      /** @defaultValue `"You must provide a valid base rate."` */
      accrualRateAndUnitError: string
      /** @defaultValue `"Employees will accrue"` */
      employeesWillAccrueLabel: string
      /** @defaultValue `"For every"` */
      forEveryLabel: string
      /** @defaultValue `"hours"` */
      hoursUnit: string
      /** @defaultValue `"hours worked"` */
      hoursWorkedUnit: string
      /** @defaultValue `"Additional options"` */
      additionalOptionsLabel: string
      /** @defaultValue `"Include overtime hours"` */
      includeOvertimeLabel: string
      /** @defaultValue `"Include all paid hours"` */
      allPaidHoursLabel: string
      /** @defaultValue `"Total hours per year"` */
      fixedAccrualRateLabel: string
      /** @defaultValue `"The total number of hours employees earn."` */
      fixedAccrualRateHint: string
      /** @defaultValue `"Accrual rate"` */
      accrualMethodFixed: string
      /** @defaultValue `"The rate at which employees will accrue paid time off."` */
      accrualMethodFixedHint: string
      /** @defaultValue `"Each pay period"` */
      perPayPeriodLabel: string
      /** @defaultValue `"Employees earn time off gradually with each paycheck."` */
      perPayPeriodHint: string
      /** @defaultValue `"All at once"` */
      allAtOnceLabel: string
      /** @defaultValue `"Employees receive their full annual time off balance upfront."` */
      allAtOnceHint: string
      /** @defaultValue `"Reset date"` */
      policyResetDateType: string
      /** @defaultValue `"The accrual period will reset on this date each year."` */
      policyResetDateTypeHint: string
      /** @defaultValue `"Each employee's start date"` */
      perAnniversaryYearLabel: string
      /** @defaultValue `"Custom date"` */
      perCalendarYearLabel: string
      /** @defaultValue `"Month"` */
      monthLabel: string
      /** @defaultValue `"Day"` */
      dayLabel: string
      validations: {
        /** @defaultValue `"Policy name is required."` */
        policyName: string
        /** @defaultValue `"Please select an accrual method."` */
        accrualMethod: string
        /** @defaultValue `"Accrual rate is required."` */
        accrualRate: string
        /** @defaultValue `"Hours worked is required."` */
        accrualRateUnit: string
        /** @defaultValue `"Please select an accrual rate."` */
        accrualMethodFixed: string
        /** @defaultValue `"Please select a reset date."` */
        resetDateType: string
      }
    }
    policySettings: {
      /** @defaultValue `"Policy settings"` */
      createTitle: string
      /** @defaultValue `"{{name}} settings"` */
      editTitle: string
      /** @defaultValue `"hours"` */
      hoursUnit: string
      /** @defaultValue `"days"` */
      daysUnit: string
      /** @defaultValue `"Accrual maximum"` */
      accrualMaximumLabel: string
      /** @defaultValue `"The maximum balance an employee can accrue per period. Accrual will stop when this limit is reached."` */
      accrualMaximumHint: string
      /** @defaultValue `"Balance maximum"` */
      balanceMaximumLabel: string
      /** @defaultValue `"The maximum balance an employee can hold at once. Accrual stops when this limit is reached."` */
      balanceMaximumHint: string
      /** @defaultValue `"Carry over limit"` */
      carryOverLimitLabel: string
      /** @defaultValue `"The maximum hours allowed to carryover from one accrual period to the next."` */
      carryOverLimitHint: string
      /** @defaultValue `"Waiting period"` */
      waitingPeriodLabel: string
      /** @defaultValue `"The number of days employees must work before they begin accruing time off."` */
      waitingPeriodHint: string
      /** @defaultValue `"Payout on dismissal"` */
      paidOutOnTerminationLabel: string
      /** @defaultValue `"Choose if unused time off is paid out when an employee leaves. Some states require it by law."` */
      paidOutOnTerminationHint: string
      /** @defaultValue `"0"` */
      numberOfHoursPlaceholder: string
      /** @defaultValue `"0"` */
      numberOfDaysPlaceholder: string
      /** @defaultValue `"Save"` */
      continueCta: string
      errors: {
        /** @defaultValue `"The balance maximum cannot be lower than an employee's current balance. Reduce the employee's balance first, or increase the maximum."` */
        balanceExceedsMaximum: string
        /** @defaultValue `"Waiting period must be a whole number of days"` */
        waitingPeriodMustBeWholeNumber: string
      }
    }
    startingBalances: {
      /** @defaultValue `"Starting balances"` */
      title: string
      /** @defaultValue `"If your employees have an existing balance, please enter those hours here. Employees moving to this policy from another will carry over their accrued hours."` */
      description: string
      /** @defaultValue `"You haven't added any employees to this policy yet, so there aren't any starting balances to add."` */
      noEmployees: string
      /** @defaultValue `"The following employees' balances need review"` */
      alertHeader: string
      /** @defaultValue `"Create policy"` */
      createPolicyCta: string
    }
    balanceTable: {
      /** @defaultValue `"Balance (hrs)"` */
      balance: string
      /** @defaultValue `"{{count}} employee"` */
      employeeCount_one: string
      /** @defaultValue `"{{count}} employees"` */
      employeeCount_other: string
    }
    /** @defaultValue `"Save & continue"` */
    continueCta: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    errors: {
      /** @defaultValue `"Unable to update policy settings. {{details}}"` */
      updatePolicySettingsFailed: string
    }
    flash: {
      /** @defaultValue `"Details for this policy have been updated."` */
      policyUpdated: string
      /** @defaultValue `"Your time off policy is active."` */
      policyActive: string
      /** @defaultValue `"{{count}} employee has been added to this policy."` */
      employeesAdded_one: string
      /** @defaultValue `"{{count}} employees have been added to this policy."` */
      employeesAdded_other: string
      /** @defaultValue `"{{name}}'s time off balance has been updated."` */
      balanceUpdated: string
      /** @defaultValue `"Details for this policy have been updated."` */
      settingsUpdated: string
    }
  }
  /** Translation keys for the `Company.TimeOff.EmployeeTable` i18n namespace. */
  export interface CompanyTimeOffEmployeeTable {
    /** @defaultValue `"Name"` */
    name: string
    /** @defaultValue `"Job title"` */
    jobTitle: string
    /** @defaultValue `"Department"` */
    department: string
    /** @defaultValue `"of {{numAll}} employees selected"` */
    selected: string
    /** @defaultValue `"Select all employees"` */
    selectAllEmployees: string
    /** @defaultValue `"Select all"` */
    selectAll: string
    /** @defaultValue `"Clear selected employees"` */
    clearAriaLabel: string
    /** @defaultValue `"Clear"` */
    clear: string
    /** @defaultValue `"Search by name or department"` */
    searchPlaceholder: string
    /** @defaultValue `"Search by name or department"` */
    searchLabel: string
    /** @defaultValue `"Employees"` */
    tableLabel: string
    /** @defaultValue `"No employees found"` */
    noSearchResults: string
    /** @defaultValue `"Clear search"` */
    clearSearch: string
  }
  /** Translation keys for the `Company.TimeOff.HolidayPolicy` i18n namespace. */
  export interface CompanyTimeOffHolidayPolicy {
    /** @defaultValue `"Choose your company holidays"` */
    title: string
    /** @defaultValue `"Choose which holidays your company observes and we'll automatically add those hours when you run payroll."` */
    description: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    tableHeaders: {
      /** @defaultValue `"Holiday"` */
      holidayName: string
      /** @defaultValue `"Observed date"` */
      observedDate: string
      /** @defaultValue `"Next observation"` */
      nextObservation: string
    }
    /** @defaultValue `"Company holidays"` */
    tableLabel: string
    editEmployees: {
      /** @defaultValue `"Add employees to your policy"` */
      title: string
    }
    show: {
      /** @defaultValue `"Holiday pay policy"` */
      title: string
      /** @defaultValue `"Add employees"` */
      addEmployeesCta: string
      /** @defaultValue `"Edit policy"` */
      editPolicyCta: string
    }
    tabs: {
      /** @defaultValue `"Holidays"` */
      holidays: string
    }
    holidayScheduleTable: {
      /** @defaultValue `"Holiday schedule"` */
      title: string
      /** @defaultValue `"Edit"` */
      editCta: string
    }
    holidays: {
      newYearsDay: {
        /** @defaultValue `"New Year's Day"` */
        name: string
        /** @defaultValue `"January 1"` */
        observedDate: string
      }
      mlkDay: {
        /** @defaultValue `"Martin Luther King, Jr. Day"` */
        name: string
        /** @defaultValue `"Third Monday in January"` */
        observedDate: string
      }
      presidentsDay: {
        /** @defaultValue `"Presidents' Day"` */
        name: string
        /** @defaultValue `"Third Monday in February"` */
        observedDate: string
      }
      memorialDay: {
        /** @defaultValue `"Memorial Day"` */
        name: string
        /** @defaultValue `"Last Monday in May"` */
        observedDate: string
      }
      juneteenth: {
        /** @defaultValue `"Juneteenth"` */
        name: string
        /** @defaultValue `"June 19"` */
        observedDate: string
      }
      independenceDay: {
        /** @defaultValue `"Independence Day"` */
        name: string
        /** @defaultValue `"July 4"` */
        observedDate: string
      }
      laborDay: {
        /** @defaultValue `"Labor Day"` */
        name: string
        /** @defaultValue `"First Monday in September"` */
        observedDate: string
      }
      columbusDay: {
        /** @defaultValue `"Columbus Day (Indigenous Peoples' Day)"` */
        name: string
        /** @defaultValue `"Second Monday in October"` */
        observedDate: string
      }
      veteransDay: {
        /** @defaultValue `"Veterans Day"` */
        name: string
        /** @defaultValue `"November 11"` */
        observedDate: string
      }
      thanksgiving: {
        /** @defaultValue `"Thanksgiving"` */
        name: string
        /** @defaultValue `"Fourth Thursday in November"` */
        observedDate: string
      }
      christmasDay: {
        /** @defaultValue `"Christmas Day"` */
        name: string
        /** @defaultValue `"December 25"` */
        observedDate: string
      }
    }
    flash: {
      /** @defaultValue `"Holiday pay policy deleted successfully"` */
      deleted: string
      /** @defaultValue `"{{count}} employee has been added to this policy."` */
      employeesAdded_one: string
      /** @defaultValue `"{{count}} employees have been added to this policy."` */
      employeesAdded_other: string
      /** @defaultValue `"{{name}} has been removed from the policy."` */
      employeeRemoved: string
      /** @defaultValue `"{{count}} employee has been removed from the policy."` */
      employeesRemoved_one: string
      /** @defaultValue `"{{count}} employees have been removed from the policy."` */
      employeesRemoved_other: string
    }
  }
  /** Translation keys for the `Company.TimeOff.PolicyDetail` i18n namespace. */
  export interface CompanyTimeOffPolicyDetail {
    tabs: {
      /** @defaultValue `"Employees"` */
      employees: string
    }
    /** @defaultValue `"Back to policies"` */
    backLabel: string
    /** @defaultValue `"Actions for {{name}}"` */
    employeeActions: string
    emptyEmployees: {
      /** @defaultValue `"No employees have been added to this policy"` */
      title: string
      /** @defaultValue `"Add employee"` */
      addEmployeeCta: string
    }
    removeEmployeeDialog: {
      /** @defaultValue `"Remove {{name}}"` */
      title: string
      /** @defaultValue `"Are you sure you want to remove {{name}} from this policy?"` */
      description: string
      /** @defaultValue `"Remove"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
  }
  /** Translation keys for the `Company.TimeOff.SelectEmployees` i18n namespace. */
  export interface CompanyTimeOffSelectEmployees {
    /** @defaultValue `"Add employees to policy"` */
    title: string
    /** @defaultValue `"Select the employees you want to add to this policy."` */
    description: string
    /** @defaultValue `"Select the employees you want to add to this holiday pay policy."` */
    holidayDescription: string
    /** @defaultValue `"Any employees currently assigned to another paid time off policy will be moved to this policy."` */
    reassignmentWarning: string
    /** @defaultValue `"Department"` */
    departmentColumn: string
    /** @defaultValue `"Starting balance (hrs)"` */
    startingBalanceColumn: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"All eligible employees have already been added to this policy."` */
    emptyState: string
    errors: {
      /** @defaultValue `"Unable to complete this policy. {{details}}"` */
      completePolicyFailed: string
    }
  }
  /** Translation keys for the `Company.TimeOff.SelectPolicyType` i18n namespace. */
  export interface CompanyTimeOffSelectPolicyType {
    /** @defaultValue `"Select policy type"` */
    title: string
    /** @defaultValue `"Policy type"` */
    policyTypeLabel: string
    /** @defaultValue `"Select the type of time off policy you want to set up for your team."` */
    policyTypeHint: string
    /** @defaultValue `"Holiday pay"` */
    holidayLabel: string
    /** @defaultValue `"Give employees time off for company holidays. Choose which holidays to observe."` */
    holidayHint: string
    /** @defaultValue `"Time off"` */
    timeOffLabel: string
    /** @defaultValue `"Use for paid leave or combine vacation and sick time into one PTO policy."` */
    timeOffHint: string
    /** @defaultValue `"Sick leave"` */
    sickLeaveLabel: string
    /** @defaultValue `"Paid time employees can use when they're sick or caring for their health."` */
    sickLeaveHint: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    validations: {
      /** @defaultValue `"Please select a policy type"` */
      policyTypeRequired: string
    }
  }
  /** Translation keys for the `Company.TimeOff.TimeOffPolicies` i18n namespace. */
  export interface CompanyTimeOffTimeOffPolicies {
    /** @defaultValue `"Time Off Policies"` */
    pageTitle: string
    /** @defaultValue `"Create policy"` */
    createPolicyCta: string
    tableHeaders: {
      /** @defaultValue `"Name"` */
      name: string
      /** @defaultValue `"Enrolled"` */
      enrolled: string
    }
    /** @defaultValue `"Time off policies"` */
    tableLabel: string
    actions: {
      /** @defaultValue `"View policy"` */
      viewPolicy: string
      /** @defaultValue `"Delete policy"` */
      deletePolicy: string
      /** @defaultValue `"Actions for {{name}}"` */
      menuTrigger: string
      /** @defaultValue `"Actions for {{name}}"` */
      menuFor: string
    }
    /** @defaultValue `"Finish setup"` */
    finishSetupCta: string
    /** @defaultValue `"All employees"` */
    allEmployeesLabel: string
    /** @defaultValue `"–"` */
    enrolledDash: string
    /** @defaultValue `"{{count}} employee"` */
    employeeCount_one: string
    /** @defaultValue `"{{count}} employees"` */
    employeeCount_other: string
    /** @defaultValue `"Incomplete"` */
    incompleteBadge: string
    /** @defaultValue `"Holiday pay policy"` */
    holidayPayPolicy: string
    deletePolicyDialog: {
      /** @defaultValue `"Are you sure you want to delete the policy \"{{name}}\"?"` */
      title: string
      /** @defaultValue `"This will delete the policy \"{{name}}\" and all associated time off requests."` */
      description: string
      /** @defaultValue `"Delete policy"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    deleteHolidayDialog: {
      /** @defaultValue `"Are you sure you want to delete the company holiday pay policy?"` */
      title: string
      /** @defaultValue `"This will delete the company holiday pay policy."` */
      description: string
    }
    emptyState: {
      /** @defaultValue `"You don't have any time off policies"` */
      heading: string
      /** @defaultValue `"Manage employee time off by creating a policy."` */
      body: string
    }
    flash: {
      /** @defaultValue `"Policy \"{{name}}\" deleted successfully"` */
      policyDeleted: string
      /** @defaultValue `"Holiday pay policy deleted successfully"` */
      holidayDeleted: string
      /** @defaultValue `"Please select a valid policy type."` */
      invalidPolicyType: string
    }
    errors: {
      /** @defaultValue `"\"{{name}}\" has pending or approved time off requests. Please decline or cancel those requests before deleting this policy."` */
      pendingRequestsBlockDeletion: string
      /** @defaultValue `"Unable to delete this policy. Please try again."` */
      deleteFailed: string
    }
  }
  /** Translation keys for the `Company.TimeOff.TimeOffPolicyDetails` i18n namespace. */
  export interface CompanyTimeOffTimeOffPolicyDetails {
    /** @defaultValue `"Time off policies"` */
    breadcrumb: string
    /** @defaultValue `"Add employee"` */
    addEmployeeCta: string
    subtitle: {
      /** @defaultValue `"Paid time off policy"` */
      vacation: string
      /** @defaultValue `"Sick leave policy"` */
      sick: string
    }
    tabs: {
      /** @defaultValue `"Employees"` */
      employees: string
      /** @defaultValue `"Policy details"` */
      policyDetails: string
    }
    /** @defaultValue `"Details"` */
    details: string
    /** @defaultValue `"Edit policy"` */
    editPolicyCta: string
    policyType: {
      /** @defaultValue `"Policy type"` */
      label: string
      /** @defaultValue `"Paid time off"` */
      vacation: string
      /** @defaultValue `"Sick leave"` */
      sick: string
      /** @defaultValue `"Custom policy"` */
      custom: string
      /** @defaultValue `"Parental leave"` */
      parentalLeave: string
      /** @defaultValue `"Weather policy"` */
      weather: string
      /** @defaultValue `"Volunteer policy"` */
      volunteer: string
      /** @defaultValue `"Personal day"` */
      personalDay: string
      /** @defaultValue `"Learning and development"` */
      learningAndDevelopment: string
      /** @defaultValue `"Jury duty"` */
      juryDuty: string
      /** @defaultValue `"Floating holiday"` */
      floatingHoliday: string
      /** @defaultValue `"Bereavement"` */
      bereavement: string
    }
    /** @defaultValue `"Policy name"` */
    policyName: string
    /** @defaultValue `"Accrual rate"` */
    accrualRateTitle: string
    accrualMethod: {
      /** @defaultValue `"Accrual type"` */
      label: string
      /** @defaultValue `"Unlimited"` */
      unlimited: string
      /** @defaultValue `"Fixed"` */
      perPayPeriod: string
      /** @defaultValue `"Fixed"` */
      perCalendarYear: string
      /** @defaultValue `"Fixed"` */
      perAnniversaryYear: string
      /** @defaultValue `"Based on hours worked"` */
      perHourWorked: string
      /** @defaultValue `"Based on hours worked"` */
      perHourWorkedNoOvertime: string
      /** @defaultValue `"Based on hours worked"` */
      perHourPaid: string
      /** @defaultValue `"Based on hours worked"` */
      perHourPaidNoOvertime: string
    }
    accrualRate: {
      /** @defaultValue `"Accrual rate"` */
      label: string
      /** @defaultValue `"-"` */
      unlimited: string
      /** @defaultValue `"{{accrualRate}} hour(s) per pay period"` */
      perPayPeriod: string
      /** @defaultValue `"{{accrualRate}} hour(s) per calendar year"` */
      perCalendarYear: string
      /** @defaultValue `"{{accrualRate}} hour(s) per anniversary year"` */
      perAnniversaryYear: string
      /** @defaultValue `"{{accrualRate}} hour(s) for every {{accrualRateUnit}} hour(s) worked, including overtime"` */
      perHourWorked: string
      /** @defaultValue `"{{accrualRate}} hour(s) for every {{accrualRateUnit}} hour(s) worked, excluding overtime"` */
      perHourWorkedNoOvertime: string
      /** @defaultValue `"{{accrualRate}} hour(s) for every {{accrualRateUnit}} hour(s) including overtime and all paid hours"` */
      perHourPaid: string
      /** @defaultValue `"{{accrualRate}} hour(s) for every {{accrualRateUnit}} hour(s) worked and all paid hours, excluding overtime"` */
      perHourPaidNoOvertime: string
    }
    /** @defaultValue `"Reset date"` */
    resetDate: string
    /** @defaultValue `"Policy settings"` */
    policySettingsTitle: string
    /** @defaultValue `"Change"` */
    changeSettingsCta: string
    maxAccrualHoursPerYear: {
      /** @defaultValue `"Accrual maximum"` */
      label: string
      /** @defaultValue `"No maximum"` */
      noMaximum: string
      /** @defaultValue `"{{count}} hour(s) per year"` */
      withMaximum: string
    }
    maxHours: {
      /** @defaultValue `"Balance maximum"` */
      label: string
      /** @defaultValue `"No maximum"` */
      noMaximum: string
      /** @defaultValue `"{{count}} hour(s)"` */
      withMaximum: string
    }
    carryoverLimitHours: {
      /** @defaultValue `"Carry over limit"` */
      label: string
      /** @defaultValue `"No carry over limit"` */
      noLimit: string
      /** @defaultValue `"{{count}} hour(s)"` */
      withLimit: string
    }
    accrualWaitingPeriodDays: {
      /** @defaultValue `"Waiting period"` */
      label: string
      /** @defaultValue `"No waiting period"` */
      noPeriod: string
      /** @defaultValue `"{{count}} day(s)"` */
      withPeriod: string
    }
    paidOutOnTermination: {
      /** @defaultValue `"Paid out on termination"` */
      label: string
      /** @defaultValue `"Yes, remaining time off balances for this policy are paid out when an employee is dismissed."` */
      yes: string
      /** @defaultValue `"No, remaining time off balances for this policy are not paid out when an employee is dismissed."` */
      no: string
    }
    employeeTable: {
      /** @defaultValue `"Balance (hrs)"` */
      balance: string
      /** @defaultValue `"Actions"` */
      actions: string
      /** @defaultValue `"Edit balance"` */
      editBalance: string
      /** @defaultValue `"Remove employee"` */
      removeEmployee: string
    }
    addEmployeeModal: {
      /** @defaultValue `"Add employee to this policy"` */
      title: string
      /** @defaultValue `"Select the employees to add to this policy. Employees not shown are either already included or not eligible."` */
      description: string
    }
    editBalanceModal: {
      /** @defaultValue `"Edit {{name}} time off balance"` */
      title: string
      /** @defaultValue `"Balance (hrs)"` */
      balanceLabel: string
      /** @defaultValue `"Current balance"` */
      currentBalance: string
      /** @defaultValue `"hours"` */
      hoursUnit: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
      /** @defaultValue `"Update balance"` */
      updateCta: string
      errors: {
        /** @defaultValue `"Balance cannot exceed the policy maximum of {{max}} hours. Reduce the balance or increase the policy's balance maximum."` */
        balanceExceedsMax: string
        /** @defaultValue `"Unable to update balance. Please try again."` */
        updateFailed: string
      }
    }
    removeEmployeeModal: {
      /** @defaultValue `"Remove {{name}} from policy?"` */
      title: string
      /** @defaultValue `"When you remove an employee from a policy, any existing balance is removed."` */
      alert: string
      /** @defaultValue `"Remove employee"` */
      removeCta: string
    }
    removeEmployeesModal: {
      /** @defaultValue `"Remove {{count}} employee(s) from policy?"` */
      title: string
      /** @defaultValue `"When you remove an employee from a policy, any existing balance is removed."` */
      alert: string
      /** @defaultValue `"Remove employees"` */
      removeCta: string
    }
    flash: {
      /** @defaultValue `"{{name}} has been removed from the policy."` */
      employeeRemoved: string
      /** @defaultValue `"{{count}} employee has been added to this policy."` */
      employeesAdded_one: string
      /** @defaultValue `"{{count}} employees have been added to this policy."` */
      employeesAdded_other: string
      /** @defaultValue `"{{name}}'s time off balance has been updated."` */
      balanceUpdated: string
    }
  }
  /** Translation keys for the `Company.TimeOff.TimeOffRequests` i18n namespace. */
  export interface CompanyTimeOffTimeOffRequests {
    index: {
      /** @defaultValue `"Time off requests"` */
      title: string
      /** @defaultValue `"Add time off"` */
      addTimeOff: string
      tabs: {
        /** @defaultValue `"Needs review"` */
        needsReview: string
        /** @defaultValue `"Approved"` */
        approved: string
        /** @defaultValue `"Completed"` */
        completed: string
        /** @defaultValue `"Declined"` */
        declined: string
      }
      emptyState: {
        needsReview: {
          /** @defaultValue `"No requests to review"` */
          heading: string
          /** @defaultValue `"New time off requests will appear here."` */
          body: string
        }
        approved: {
          /** @defaultValue `"No approved requests yet"` */
          heading: string
          /** @defaultValue `"Once time off is approved, it will appear here."` */
          body: string
        }
        completed: {
          /** @defaultValue `"No completed time off yet"` */
          heading: string
          /** @defaultValue `"Once approved time off is completed, it will appear here."` */
          body: string
        }
        declined: {
          /** @defaultValue `"No declined requests"` */
          heading: string
          /** @defaultValue `"If a time off request is declined, it will appear here."` */
          body: string
        }
      }
    }
    requestsTable: {
      /** @defaultValue `"Employee"` */
      employee: string
      /** @defaultValue `"Policy"` */
      policy: string
      /** @defaultValue `"Date(s)"` */
      dates: string
      /** @defaultValue `"Requested"` */
      requested: string
      /** @defaultValue `"Remaining"` */
      remaining: string
      /** @defaultValue `"Actions"` */
      actions: string
      /** @defaultValue `"View"` */
      view: string
      /** @defaultValue `"{{count}} request"` */
      requestsCount_one: string
      /** @defaultValue `"{{count}} requests"` */
      requestsCount_other: string
      /** @defaultValue `"hrs"` */
      hoursUnit: string
    }
    policyType: {
      /** @defaultValue `"Vacation"` */
      vacation: string
      /** @defaultValue `"Sick leave"` */
      sick: string
      /** @defaultValue `"Bereavement"` */
      bereavement: string
      /** @defaultValue `"Floating holiday"` */
      floatingHoliday: string
      /** @defaultValue `"Jury duty"` */
      juryDuty: string
      /** @defaultValue `"Learning and development"` */
      learningAndDevelopment: string
      /** @defaultValue `"Personal day"` */
      personalDay: string
      /** @defaultValue `"Volunteer"` */
      volunteer: string
      /** @defaultValue `"Weather"` */
      weather: string
      /** @defaultValue `"Parental leave"` */
      parentalLeave: string
      /** @defaultValue `"Custom"` */
      custom: string
    }
    show: {
      /** @defaultValue `"{{policyType}} request"` */
      title: string
      /** @defaultValue `"Time off requests"` */
      breadcrumb: string
      /** @defaultValue `"Details"` */
      details: string
      /** @defaultValue `"Employee"` */
      employee: string
      /** @defaultValue `"Date(s) requested"` */
      datesRequested: string
      /** @defaultValue `"Time requested"` */
      timeRequested: string
      /** @defaultValue `"View breakdown by day"` */
      viewBreakdown: string
      /** @defaultValue `"Date"` */
      breakdownDate: string
      /** @defaultValue `"Time requested"` */
      breakdownTimeRequested: string
      /** @defaultValue `"Employee note"` */
      employeeNote: string
      /** @defaultValue `"Policy name"` */
      policyName: string
      /** @defaultValue `"Policy type"` */
      policyType: string
      /** @defaultValue `"Requested on"` */
      requestedOn: string
      /** @defaultValue `"Note to employee"` */
      noteToEmployee: string
      /** @defaultValue `"Notes"` */
      notes: string
      /** @defaultValue `"hrs"` */
      hoursUnit: string
      /** @defaultValue `"Needs review"` */
      statusNeedsReview: string
      /** @defaultValue `"Approved"` */
      statusApproved: string
      /** @defaultValue `"Completed"` */
      statusCompleted: string
      /** @defaultValue `"Declined"` */
      statusDeclined: string
      /** @defaultValue `"Approve request"` */
      approveRequestCta: string
      /** @defaultValue `"Decline request"` */
      declineRequestCta: string
      /** @defaultValue `"Decline approved request"` */
      declineApprovedRequestCta: string
      /** @defaultValue `"Done"` */
      doneCta: string
      /** @defaultValue `"A note to the employee is required when declining a request"` */
      employerNoteRequired: string
    }
    new: {
      /** @defaultValue `"Add time off"` */
      title: string
      /** @defaultValue `"Back"` */
      breadcrumb: string
      /** @defaultValue `"Time off you record for an employee is approved automatically."` */
      subtitle: string
      /** @defaultValue `"Employee"` */
      employeeLabel: string
      /** @defaultValue `"Select..."` */
      selectEmployee: string
      /** @defaultValue `"Policy"` */
      policyLabel: string
      /** @defaultValue `"Select..."` */
      selectPolicy: string
      /** @defaultValue `"Note"` */
      noteLabel: string
      /** @defaultValue `"(Optional)"` */
      optionalLabel: string
      /** @defaultValue `"Save"` */
      saveCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    dateHoursFields: {
      /** @defaultValue `"Start date"` */
      startDate: string
      /** @defaultValue `"End date"` */
      endDate: string
      /** @defaultValue `"Time requested"` */
      timeRequested: string
      /** @defaultValue `"hrs"` */
      hoursUnit: string
      /** @defaultValue `"End date must be on or after start date."` */
      invalidDateRange: string
      duration: {
        /** @defaultValue `"Duration"` */
        title: string
        /** @defaultValue `"Choose how many hours of time off to record for the selected dates."` */
        description: string
        /** @defaultValue `"Full days"` */
        fullDays: string
        /** @defaultValue `"Automatically counts 8 hours for each weekday (Monday - Friday). Weekends are excluded."` */
        fullDaysDescription: string
        /** @defaultValue `"Partial days"` */
        partialDays: string
        /** @defaultValue `"Use this option to specify the exact number of hours for each day."` */
        partialDaysDescription: string
        /** @defaultValue `"Date"` */
        dateHeader: string
        /** @defaultValue `"Time requested"` */
        timeRequestedHeader: string
      }
    }
    balancePreview: {
      /** @defaultValue `"Time off balance for this policy"` */
      title: string
      /** @defaultValue `"Available balance"` */
      availableBalance: string
      /** @defaultValue `"The amount of time off this employee has available today to use on this policy."` */
      availableBalanceHint: string
      /** @defaultValue `"Other requested time off"` */
      otherRequested: string
      /** @defaultValue `"Any other approved future time off and pending time off under this policy. Excludes this request."` */
      otherRequestedHint: string
      /** @defaultValue `"This request"` */
      thisRequest: string
      /** @defaultValue `"Remaining available balance"` */
      remainingBalance: string
      /** @defaultValue `"TBD hrs"` */
      placeholder: string
      /** @defaultValue `"If you approve this request, the employee will end up with a negative time off balance."` */
      negativeBalanceWarning: string
    }
    flash: {
      /** @defaultValue `"Time off request created successfully"` */
      createSuccess: string
      /** @defaultValue `"Time off request approved"` */
      approveSuccess: string
      /** @defaultValue `"Time off request declined"` */
      declineSuccess: string
      /** @defaultValue `"A note is required when declining a request"` */
      employerNoteRequired: string
    }
  }
  /** Translation keys for the `Contractor.Address` i18n namespace. */
  export interface ContractorAddress {
    /** @defaultValue `"Business address"` */
    businessAddressTitle: string
    /** @defaultValue `"Contractor's business address, within the United States."` */
    businessAddressDescription: string
    /** @defaultValue `"Home address"` */
    homeAddressTitle: string
    /** @defaultValue `"Contractor's home mailing address, within the United States."` */
    homeAddressDescription: string
    w9EditWarning: {
      /** @defaultValue `"Changes will require an updated Form W-9"` */
      label: string
      /** @defaultValue `"This contractor has already signed a form W-9. If you are making corrections, you’re also required to update and retain a new signed version of Form W-9 reflecting the corrected information."` */
      body: string
    }
    /** @defaultValue `"Street 1"` */
    street1: string
    /** @defaultValue `"Street 2"` */
    street2: string
    /** @defaultValue `"City"` */
    city: string
    /** @defaultValue `"State"` */
    state: string
    /** @defaultValue `"Select state..."` */
    statePlaceholder: string
    /** @defaultValue `"Zip"` */
    zip: string
    /** @defaultValue `"Continue"` */
    submit: string
    /** @defaultValue `"Saving…"` */
    submitting: string
    validations: {
      /** @defaultValue `"Street address is required"` */
      street1: string
      /** @defaultValue `"Please provide valid city name"` */
      city: string
      /** @defaultValue `"Please select a state"` */
      state: string
      /** @defaultValue `"Please provide valid zip code"` */
      zip: string
      /** @defaultValue `"Please enter a valid ZIP code"` */
      zipInvalid: string
    }
  }
  /** Translation keys for the `Contractor.ContractorList` i18n namespace. */
  export interface ContractorContractorList {
    /** @defaultValue `"Add a contractor"` */
    addContractorCta: string
    /** @defaultValue `"+ Add another contractor"` */
    addAnotherCta: string
    /** @defaultValue `"A list of contractors"` */
    contractorListLabel: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Review"` */
    reviewCta: string
    /** @defaultValue `"Delete"` */
    deleteCta: string
    /** @defaultValue `"Cancel self-onboarding"` */
    cancelSelfOnboardingCta: string
    /** @defaultValue `"Add contractors to get them setup for payroll."` */
    emptyTableDescription: string
    /** @defaultValue `"You haven't added any contractors yet"` */
    emptyTableTitle: string
    listHeaders: {
      /** @defaultValue `"Name"` */
      name: string
      /** @defaultValue `"Status"` */
      status: string
    }
    /** @defaultValue `"Contractors"` */
    title: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Back to contractors"` */
    progressBarCta: string
    deleteDialog: {
      /** @defaultValue `"Delete contractor?"` */
      title: string
      /** @defaultValue `"This will permanently delete this contractor from your account. This action cannot be undone."` */
      description: string
      /** @defaultValue `"Delete contractor"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
  }
  /** Translation keys for the `Contractor.DocumentsList` i18n namespace. */
  export interface ContractorDocumentsList {
    /** @defaultValue `"Documents"` */
    title: string
    /** @defaultValue `"Please review and sign your documents."` */
    subtitle: string
    /** @defaultValue `"Documents"` */
    documentListLabel: string
    /** @defaultValue `"Name"` */
    documentColumnLabel: string
    /** @defaultValue `"Status"` */
    statusColumnLabel: string
    /** @defaultValue `"Sign document"` */
    signDocumentCta: string
    /** @defaultValue `"Not signed"` */
    notSigned: string
    /** @defaultValue `"Complete"` */
    signed: string
    /** @defaultValue `"W-9 isn't ready to sign yet"` */
    notReadyTitle: string
    /** @defaultValue `"The W-9 form hasn't been fully generated yet. Please check back later, or contact support if the problem continues."` */
    notReadyBody: string
    /** @defaultValue `"No documents found"` */
    emptyTitle: string
    /** @defaultValue `"Could not load your documents, try again later."` */
    errorTitle: string
    /** @defaultValue `"Continue"` */
    continueCta: string
  }
  /** Translation keys for the `Contractor.Landing` i18n namespace. */
  export interface ContractorLanding {
    /** @defaultValue `"Welcome, {{contractorName}}! {{companyName}} has invited you to complete your onboarding."` */
    landingSubtitle: string
    /** @defaultValue `"We just need a few details from you to get set up."` */
    landingDescription: string
    /** @defaultValue `"Here's what you'll need to do:"` */
    stepsSubtitle: string
    steps: {
      /** @defaultValue `"Complete your profile and tax information"` */
      profile: string
      /** @defaultValue `"Add your mailing address"` */
      address: string
      /** @defaultValue `"Set up your payment method"` */
      paymentMethod: string
      /** @defaultValue `"Review and sign documents"` */
      documents: string
    }
    /** @defaultValue `"Get started"` */
    getStartedCta: string
    /** @defaultValue `"there"` */
    fallbackName: string
  }
  /** Translation keys for the `Contractor.ManagementContractorList` i18n namespace. */
  export interface ContractorManagementContractorList {
    /** @defaultValue `"Contractors"` */
    title: string
    /** @defaultValue `"Add contractor"` */
    addContractorCta: string
    /** @defaultValue `"Contractor status tabs"` */
    tabsLabel: string
    tabs: {
      /** @defaultValue `"Active"` */
      active: string
      /** @defaultValue `"Onboarding"` */
      onboarding: string
      /** @defaultValue `"Dismissed"` */
      dismissed: string
    }
    /** @defaultValue `"Contractor name"` */
    nameLabel: string
    /** @defaultValue `"Rate"` */
    rateLabel: string
    /** @defaultValue `"Onboarding status"` */
    onboardingStatusLabel: string
    /** @defaultValue `"Dismissal date"` */
    dismissalDateLabel: string
    /** @defaultValue `"Hourly — {{rate}}/hr"` */
    rateHourly: string
    /** @defaultValue `"Starts {{date}}"` */
    startsBadge: string
    /** @defaultValue `"Last day {{date}}"` */
    lastDayBadge: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Review"` */
    reviewCta: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"View details"` */
    viewDetailsCta: string
    /** @defaultValue `"Remove"` */
    removeCta: string
    /** @defaultValue `"Dismiss contractor"` */
    dismissCta: string
    /** @defaultValue `"Rehire contractor"` */
    rehireCta: string
    /** @defaultValue `"Cancel self-onboarding"` */
    cancelSelfOnboardingCta: string
    /** @defaultValue `"Cancel dismissal"` */
    cancelDismissalCta: string
    /** @defaultValue `"Cancel rehire"` */
    cancelRehireCta: string
    /** @defaultValue `"Contractor actions menu"` */
    hamburgerTitle: string
    /** @defaultValue `"List of contractors"` */
    contractorListLabel: string
    emptyState: {
      active: {
        /** @defaultValue `"There are no active contractors"` */
        title: string
        /** @defaultValue `"Contractors who have completed onboarding will appear here"` */
        description: string
      }
      onboarding: {
        /** @defaultValue `"There are no contractors onboarding"` */
        title: string
        /** @defaultValue `"Contractors currently being onboarded will appear here"` */
        description: string
      }
      dismissed: {
        /** @defaultValue `"There are no dismissed contractors"` */
        title: string
        /** @defaultValue `"Dismissed contractors will appear here"` */
        description: string
      }
    }
    removeDialog: {
      /** @defaultValue `"Remove contractor?"` */
      title: string
      /** @defaultValue `"This will permanently delete this contractor from your account. This action cannot be undone."` */
      description: string
      /** @defaultValue `"Remove contractor"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    cancelDismissalDialog: {
      /** @defaultValue `"Cancel dismissal?"` */
      title: string
      /** @defaultValue `"This contractor's scheduled dismissal will be removed and they will remain active."` */
      description: string
      /** @defaultValue `"Yes, cancel dismissal"` */
      confirmCta: string
      /** @defaultValue `"No, go back"` */
      cancelCta: string
    }
    cancelRehireDialog: {
      /** @defaultValue `"Cancel rehire?"` */
      title: string
      /** @defaultValue `"This contractor's scheduled rehire will be removed."` */
      description: string
      /** @defaultValue `"Yes, cancel rehire"` */
      confirmCta: string
      /** @defaultValue `"No, go back"` */
      cancelCta: string
    }
  }
  /** Translation keys for the `Contractor.NewHireReport` i18n namespace. */
  export interface ContractorNewHireReport {
    /** @defaultValue `"File new hire report?"` */
    title: string
    /** @defaultValue `"Most states require that employers file a new hire report for contractors within 20 days of their start date. Even if the report is overdue, it is generally better to file a late report than to risk penalties for being out of compliance."` */
    description: string
    /** @defaultValue `"Select whether or not you'd like us to file new hire report"` */
    doFileLegend: string
    /** @defaultValue `"Yes, file the report for me"` */
    yesOption: string
    /** @defaultValue `"No, I have already filed or will file the report myself"` */
    noOption: string
    /** @defaultValue `"Work state"` */
    stateSelectionLabel: string
    /** @defaultValue `"Select state..."` */
    statePlaceholder: string
    /** @defaultValue `"Continue"` */
    submitCta: string
    validations: {
      /** @defaultValue `"Please select work state"` */
      state: string
    }
  }
  /** Translation keys for the `Contractor.OnboardingSummary` i18n namespace. */
  export interface ContractorOnboardingSummary {
    /** @defaultValue `"You're all set!"` */
    subtitle: string
    /** @defaultValue `"Your onboarding information has been submitted. Your company will review your details and you'll be notified if anything else is needed."` */
    description: string
    /** @defaultValue `"Done"` */
    doneCta: string
  }
  /** Translation keys for the `Contractor.PaymentMethod` i18n namespace. */
  export interface ContractorPaymentMethod {
    /** @defaultValue `"Contractor payment details"` */
    title: string
    /** @defaultValue `"Select payment method"` */
    paymentFieldsetLegend: string
    /** @defaultValue `"Direct deposit"` */
    directDepositLabel: string
    /** @defaultValue `"We recommend direct deposit – we’ll deposit paychecks directly into your employee’s bank account."` */
    directDepositDescription: string
    /** @defaultValue `"Check"` */
    checkLabel: string
    /** @defaultValue `"If you select check as the payment method, you’ll need to write a physical check to this employee each payday."` */
    checkDescription: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Saving…"` */
    submittingCta: string
    bankAccountForm: {
      /** @defaultValue `"Account nickname"` */
      nameLabel: string
      /** @defaultValue `"Routing number"` */
      routingNumberLabel: string
      /** @defaultValue `"9-digits"` */
      routingNumberDescription: string
      /** @defaultValue `"Account number"` */
      accountNumberLabel: string
      /** @defaultValue `"Account type"` */
      accountTypeLabel: string
      /** @defaultValue `"Checking"` */
      accountTypeChecking: string
      /** @defaultValue `"Savings"` */
      accountTypeSavings: string
      validations: {
        /** @defaultValue `"Account nickname is required"` */
        accountName: string
        /** @defaultValue `"Routing number is required (9-digits)"` */
        routingNumber: string
        /** @defaultValue `"Please enter valid account number"` */
        accountNumber: string
      }
    }
  }
  /** Translation keys for the `Contractor.Payments.CreatePayment` i18n namespace. */
  export interface ContractorPaymentsCreatePayment {
    /** @defaultValue `"Pay contractors"` */
    title: string
    /** @defaultValue `"No contractors available for payment"` */
    emptyTableTitle: string
    /** @defaultValue `"There are no active contractors with completed onboarding. Add and onboard contractors before creating payments."` */
    emptyTableDescription: string
    /** @defaultValue `"Pay contractors"` */
    breadcrumbLabel: string
    /** @defaultValue `"Direct deposit payments submitted before 4pm PT on a business day will take {{count}} business day to complete"` */
    paymentSpeedNotice_one: string
    /** @defaultValue `"Direct deposit payments submitted before 4pm PT on a business day will take {{count}} business days to complete"` */
    paymentSpeedNotice_other: string
    /** @defaultValue `"Payment date"` */
    dateLabel: string
    /** @defaultValue `"Hours and payments"` */
    hoursAndPaymentsLabel: string
    contractorTableHeaders: {
      /** @defaultValue `"Contractor"` */
      contractor: string
      /** @defaultValue `"Wage"` */
      wageType: string
      /** @defaultValue `"Payment method"` */
      paymentMethod: string
      /** @defaultValue `"Hours"` */
      hours: string
      /** @defaultValue `"Fixed amount"` */
      wage: string
      /** @defaultValue `"Bonus"` */
      bonus: string
      /** @defaultValue `"Reimbursement"` */
      reimbursement: string
      /** @defaultValue `"Total"` */
      total: string
    }
    /** @defaultValue `"N/A"` */
    na: string
    /** @defaultValue `"Totals"` */
    totalsLabel: string
    /** @defaultValue `"Back"` */
    backButton: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Edit contractor payment"` */
    editContractor: string
    /** @defaultValue `"/hr"` */
    perHour: string
    wageTypes: {
      /** @defaultValue `"Fixed"` */
      fixed: string
      /** @defaultValue `"Hourly"` */
      hourly: string
    }
    paymentMethods: {
      /** @defaultValue `"Direct Deposit"` */
      directDeposit: string
      /** @defaultValue `"Check"` */
      check: string
      /** @defaultValue `"Historical Payment"` */
      historicalPayment: string
    }
    alerts: {
      /** @defaultValue `"Pay updated for {{contractorName}}"` */
      contractorPaymentUpdated: string
      /** @defaultValue `"Please add at least one contractor payment to continue."` */
      noContractorPayments: string
    }
    editContractorPayment: {
      /** @defaultValue `"Edit contractor pay"` */
      title: string
      /** @defaultValue `"Edit contractor's hours, additional earnings, and reimbursements. Inputs not applicable to this contractor are disabled. Please click \"Done\" to apply the change."` */
      subtitle: string
      /** @defaultValue `"Hours"` */
      hoursLabel: string
      /** @defaultValue `"hrs"` */
      hoursAdornment: string
      /** @defaultValue `"{{rate}}/hr × hours = {{total}}"` */
      hoursPayDescription: string
      /** @defaultValue `"Fixed amount"` */
      wageLabel: string
      /** @defaultValue `"Bonus"` */
      bonusLabel: string
      /** @defaultValue `"Reimbursement"` */
      reimbursementLabel: string
      /** @defaultValue `"Payment Method"` */
      paymentMethodLabel: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
      /** @defaultValue `"Done"` */
      saveCta: string
      paymentMethods: {
        /** @defaultValue `"Check"` */
        check: string
        /** @defaultValue `"Direct deposit"` */
        directDeposit: string
        /** @defaultValue `"Historical payment"` */
        historicalPayment: string
      }
      errors: {
        /** @defaultValue `"Direct Deposit is not available for contractors set up for Check payments"` */
        directDepositNotAvailable: string
        /** @defaultValue `"This payment method is not supported. Please select Check or Direct Deposit."` */
        unsupportedPaymentMethod: string
      }
    }
    wireDetailsBanner: {
      /** @defaultValue `"To make payment, wire funds by {{time}} on {{date}}"` */
      title: string
      /** @defaultValue `"Wire transfer details required"` */
      titleFallback: string
      /** @defaultValue `"We can't pay your contractors until we get your wire. It may take time to get from your bank to ours, so we recommend sending it as soon as you can."` */
      description: string
      /** @defaultValue `"Start your wire transfer"` */
      startWireTransferCta: string
      /** @defaultValue `"We'll notify you once we've received your wire transfer."` */
      confirmationDescription: string
    }
    successState: {
      /** @defaultValue `"Payment group created successfully"` */
      title: string
      /** @defaultValue `"{{count}} contractor payment has been successfully scheduled."` */
      message: string
      /** @defaultValue `"{{count}} contractor payments have been successfully scheduled."` */
      message_other: string
      /** @defaultValue `"Done"` */
      doneCta: string
      /** @defaultValue `"Wire details submitted"` */
      wireDetailsSubmittedTitle: string
    }
    previewPresentation: {
      /** @defaultValue `"Submit"` */
      breadcrumbLabel: string
      /** @defaultValue `"Review and submit"` */
      reviewAndSubmitTitle: string
      /** @defaultValue `"We'll debit funds on {{debitDate}}"` */
      reviewSubtitle: string
      summaryTableHeaders: {
        /** @defaultValue `"Total amount"` */
        totalAmount: string
        /** @defaultValue `"Debit amount"` */
        debitAmount: string
        /** @defaultValue `"Debit account"` */
        debitAccount: string
        /** @defaultValue `"Debit date"` */
        debitDate: string
        /** @defaultValue `"Contractor pay date"` */
        contractorPayDate: string
      }
      /** @defaultValue `"Payment summary"` */
      paymentSummaryTitle: string
      /** @defaultValue `"What your company pays"` */
      whatYourCompanyPays: string
      contractorTableHeaders: {
        /** @defaultValue `"Contractor"` */
        contractor: string
        /** @defaultValue `"Wage"` */
        wageType: string
        /** @defaultValue `"Payment method"` */
        paymentMethod: string
        /** @defaultValue `"Hours"` */
        hours: string
        /** @defaultValue `"Fixed amount"` */
        wage: string
        /** @defaultValue `"Bonus"` */
        bonus: string
        /** @defaultValue `"Reimbursement"` */
        reimbursement: string
        /** @defaultValue `"Total"` */
        total: string
      }
      /** @defaultValue `"Totals"` */
      totalsLabel: string
      /** @defaultValue `"Edit"` */
      editButton: string
      /** @defaultValue `"Submit"` */
      submitButton: string
      /** @defaultValue `"/hr"` */
      perHour: string
      /** @defaultValue `"N/A"` */
      na: string
      wageTypes: {
        /** @defaultValue `"Fixed"` */
        fixed: string
        /** @defaultValue `"Hourly"` */
        hourly: string
      }
      paymentMethods: {
        /** @defaultValue `"Direct Deposit"` */
        directDeposit: string
        /** @defaultValue `"Check"` */
        check: string
        /** @defaultValue `"Historical Payment"` */
        historicalPayment: string
      }
      alerts: {
        /** @defaultValue `"To pay your contractors by {{checkDate}}, submit payments by {{debitDate}}."` */
        submitPaymentsDeadline: string
      }
      /** @defaultValue `"N/A"` */
      naDebitAccount: string
      submissionBlockers: {
        /** @defaultValue `"Submission blocked"` */
        genericBlockerTitle: string
        /** @defaultValue `"This payment cannot be submitted. Please contact support for assistance."` */
        genericBlockerMessage: string
        fastAchOptions: {
          /** @defaultValue `"Payment can still be funded by selecting one of the options below. The selected funding method will only be used for this payment and will not apply to future payments."` */
          description: string
          /** @defaultValue `"Funding options"` */
          fundingOptionsLabel: string
          /** @defaultValue `"Wire funds"` */
          wireLabel: string
          /** @defaultValue `"Fastest"` */
          wireFastestBadge: string
          /** @defaultValue `"Pay your contractors on time by sending a wire transfer. We'll provide instructions on the next step."` */
          wireDescription: string
          /** @defaultValue `"Switch to 4-day direct deposit"` */
          directDepositLabel: string
          /** @defaultValue `"Delay contractor pay date by four days and process using regular debits."` */
          directDepositDescription: string
          /** @defaultValue `"Contractor pay date: {{date}}"` */
          contractorPayDate: string
        }
        fast_ach_threshold_exceeded: {
          /** @defaultValue `"You have exceeded the limit at which you can process {{days}} contractor payments."` */
          title: string
        }
        needs_earned_access_for_fast_ach: {
          /** @defaultValue `"You have not yet earned access to faster contractor payments."` */
          title: string
        }
      }
    }
  }
  /** Translation keys for the `Contractor.Payments.PaymentHistory` i18n namespace. */
  export interface ContractorPaymentsPaymentHistory {
    /** @defaultValue `"Contractor payment history"` */
    title: string
    /** @defaultValue `"Payments debited on <strong>{{date}}</strong>"` */
    subtitle: string
    /** @defaultValue `"Payments"` */
    paymentsSection: string
    /** @defaultValue `"Payment history"` */
    breadcrumbLabel: string
    /** @defaultValue `"No payments found"` */
    noPaymentsFound: string
    /** @defaultValue `"There are no payments for this date."` */
    noPaymentsDescription: string
    /** @defaultValue `"/hr"` */
    perHour: string
    /** @defaultValue `"N/A"` */
    na: string
    tableHeaders: {
      /** @defaultValue `"Contractor"` */
      contractor: string
      /** @defaultValue `"Wage"` */
      wageType: string
      /** @defaultValue `"Payment method"` */
      paymentMethod: string
      /** @defaultValue `"Hours"` */
      hours: string
      /** @defaultValue `"Fixed amount"` */
      wage: string
      /** @defaultValue `"Bonus"` */
      bonus: string
      /** @defaultValue `"Reimbursements"` */
      reimbursements: string
      /** @defaultValue `"Total"` */
      total: string
      /** @defaultValue `"Action"` */
      action: string
    }
    actions: {
      /** @defaultValue `"View"` */
      view: string
      /** @defaultValue `"Cancel payment"` */
      cancel: string
    }
    errors: {
      /** @defaultValue `"Contractor payment group not found"` */
      paymentGroupNotFound: string
    }
  }
  /** Translation keys for the `Contractor.Payments.PaymentStatement` i18n namespace. */
  export interface ContractorPaymentsPaymentStatement {
    /** @defaultValue `"Payment statement for {{contractorName}}"` */
    title: string
    /** @defaultValue `"Payment statement for {{contractorName}}"` */
    breadcrumbLabel: string
    /** @defaultValue `"Debited"` */
    debitedColumn: string
    /** @defaultValue `"Amount"` */
    amountColumn: string
    /** @defaultValue `"Hours"` */
    hoursLabel: string
    /** @defaultValue `"{{hours}} hours at {{rate}}/hr"` */
    hoursAmount: string
    /** @defaultValue `"Wage"` */
    wageLabel: string
    /** @defaultValue `"Bonus"` */
    bonus: string
    /** @defaultValue `"Reimbursement"` */
    reimbursement: string
    receipt: {
      /** @defaultValue `"Total"` */
      totalLabel: string
      /** @defaultValue `"Receipt Details"` */
      detailsLabel: string
      /** @defaultValue `"From"` */
      from: string
      /** @defaultValue `"To"` */
      to: string
      /** @defaultValue `"Debit date"` */
      debitDate: string
      /** @defaultValue `"This receipt confirms funds have been electronically transferred to the recipient's bank account. Money transmission services are provided by Gusto, Inc. pursuant to its <licensesLink>licenses</licensesLink>."` */
      disclaimer: string
    }
    errors: {
      /** @defaultValue `"Contractor payment group not found"` */
      paymentGroupNotFound: string
      /** @defaultValue `"Payment not found"` */
      paymentNotFound: string
      /** @defaultValue `"Contractor not found"` */
      contractorNotFound: string
    }
  }
  /** Translation keys for the `Contractor.Payments.PaymentSummary` i18n namespace. */
  export interface ContractorPaymentsPaymentSummary {
    /** @defaultValue `"Payment Summary"` */
    breadcrumbLabel: string
    /** @defaultValue `"Payment group created successfully"` */
    successTitle: string
    /** @defaultValue `"{{count}} contractor payment has been successfully scheduled."` */
    successMessage_one: string
    /** @defaultValue `"{{count}} contractor payments have been successfully scheduled."` */
    successMessage_other: string
    /** @defaultValue `"Payment summary"` */
    summaryTitle: string
    /** @defaultValue `"Debit date: {{debitDate}}"` */
    summarySubtitle: string
    /** @defaultValue `"Done"` */
    doneCta: string
    /** @defaultValue `"Wire details submitted"` */
    wireDetailsSubmittedTitle: string
    alerts: {
      /** @defaultValue `"Wire details submitted"` */
      wireDetailsSubmitted: string
    }
    /** @defaultValue `"Payment Summary"` */
    paymentSummaryTitle: string
    /** @defaultValue `"Total Amount"` */
    totalAmount: string
    /** @defaultValue `"Debit Amount"` */
    debitAmount: string
    /** @defaultValue `"Debit Account"` */
    debitAccount: string
    /** @defaultValue `"Debit Date"` */
    debitDate: string
    /** @defaultValue `"Contractor Pay Date"` */
    contractorPayDate: string
    /** @defaultValue `"Contractor Payments"` */
    contractorPaymentsTitle: string
    /** @defaultValue `"Contractor"` */
    contractor: string
    /** @defaultValue `"Wage Type"` */
    wageType: string
    /** @defaultValue `"Payment Method"` */
    paymentMethod: string
    paymentMethods: {
      /** @defaultValue `"Direct Deposit"` */
      directDeposit: string
      /** @defaultValue `"Check"` */
      check: string
    }
    /** @defaultValue `"Hours"` */
    hours: string
    /** @defaultValue `"Wage"` */
    wage: string
    /** @defaultValue `"Bonus"` */
    bonus: string
    /** @defaultValue `"Reimbursement"` */
    reimbursement: string
    /** @defaultValue `"Total"` */
    total: string
    /** @defaultValue `"Totals"` */
    totalsLabel: string
    /** @defaultValue `"N/A"` */
    notAvailable: string
  }
  /** Translation keys for the `Contractor.Payments.PaymentsList` i18n namespace. */
  export interface ContractorPaymentsPaymentsList {
    /** @defaultValue `"Contractor payments"` */
    title: string
    /** @defaultValue `"Upcoming and past payments"` */
    subtitle: string
    /** @defaultValue `"Contractor payments"` */
    breadcrumbLabel: string
    /** @defaultValue `"New payment"` */
    createPaymentCta: string
    /** @defaultValue `"Start date"` */
    startDate: string
    /** @defaultValue `"Apply date"` */
    applyDate: string
    /** @defaultValue `"Payment date"` */
    paymentDateColumnLabel: string
    /** @defaultValue `"Reimbursement total"` */
    reimbursementTotalColumnLabel: string
    /** @defaultValue `"Wage total"` */
    wageTotalColumnLabel: string
    /** @defaultValue `"Actions"` */
    actionColumnLabel: string
    /** @defaultValue `"View payment"` */
    viewPaymentCta: string
    /** @defaultValue `"No payments found"` */
    noPaymentsFound: string
    /** @defaultValue `"No contractor payments have been created yet. Create your first payment to get started."` */
    noPaymentsDescription: string
    alerts: {
      /** @defaultValue `"Successfully created {{count}} contractor payment."` */
      paymentCreatedSuccessfully_one: string
      /** @defaultValue `"Successfully created {{count}} contractor payments."` */
      paymentCreatedSuccessfully_other: string
      /** @defaultValue `"Contractor payment cancelled successfully"` */
      paymentCancelledSuccessfully: string
      /** @defaultValue `"Wire details submitted"` */
      wireDetailsSubmitted: string
      /** @defaultValue `"Payments may be blocked: Provide more information to run payments"` */
      rfiPendingResponseTitle: string
      /** @defaultValue `"We need some more information about your business. Please respond as soon as possible to run payments on time."` */
      rfiPendingResponseDescription: string
      /** @defaultValue `"Payments may be blocked"` */
      rfiPendingReviewTitle: string
      /** @defaultValue `"We're reviewing information about your business. Payments may be blocked until review is complete."` */
      rfiPendingReviewDescription: string
      /** @defaultValue `"Respond"` */
      rfiRespondCta: string
    }
    dateRanges: {
      /** @defaultValue `"Last 3 months"` */
      last3Months: string
      /** @defaultValue `"Last 6 months"` */
      last6Months: string
      /** @defaultValue `"Last 12 months"` */
      last12Months: string
    }
  }
  /** Translation keys for the `Contractor.Profile` i18n namespace. */
  export interface ContractorProfile {
    /** @defaultValue `"Contractor profile"` */
    title: string
    /** @defaultValue `"This information will be used for payments and on tax documents, so double-check that it's accurate."` */
    subtitle: string
    w9EditWarning: {
      /** @defaultValue `"Changes will require an updated Form W-9"` */
      label: string
      /** @defaultValue `"This contractor has already signed a form W-9. If you are making corrections, you’re also required to update and retain a new signed version of Form W-9 reflecting the corrected information."` */
      body: string
    }
    selfOnboarding: {
      /** @defaultValue `"Complete your profile"` */
      title: string
      /** @defaultValue `"Please verify your name and provide your Social Security Number."` */
      individualDescription: string
      /** @defaultValue `"Please verify your business name and provide your EIN."` */
      businessDescription: string
      /** @defaultValue `"Continue"` */
      continue: string
      /** @defaultValue `"Saving…"` */
      submitting: string
    }
    fields: {
      selfOnboarding: {
        /** @defaultValue `"Invite this contractor to enter some of their own details online"` */
        label: string
        /** @defaultValue `"Your contractor will be invited to enter their information on their own time."` */
        description: string
      }
      email: {
        /** @defaultValue `"Contractor's email address"` */
        label: string
      }
      contractorType: {
        /** @defaultValue `"Type"` */
        label: string
      }
      firstName: {
        /** @defaultValue `"First Name"` */
        label: string
      }
      lastName: {
        /** @defaultValue `"Last Name"` */
        label: string
      }
      middleInitial: {
        /** @defaultValue `"Middle Initial"` */
        label: string
      }
      ssn: {
        /** @defaultValue `"Social Security Number"` */
        label: string
      }
      businessName: {
        /** @defaultValue `"Business Name"` */
        label: string
      }
      startDate: {
        /** @defaultValue `"Start Date"` */
        label: string
        /** @defaultValue `"Your contractor's first day of work at your company"` */
        description: string
      }
      wageType: {
        /** @defaultValue `"Wage Type"` */
        label: string
      }
      hourlyRate: {
        /** @defaultValue `"Hourly Rate"` */
        label: string
      }
      ein: {
        /** @defaultValue `"EIN"` */
        label: string
      }
    }
    validations: {
      /** @defaultValue `"Email is required when inviting contractor"` */
      email: string
      /** @defaultValue `"Email must be a valid email address"` */
      emailFormat: string
      /** @defaultValue `"First name is required for individual contractors"` */
      firstName: string
      /** @defaultValue `"First name is not valid"` */
      firstNameFormat: string
      /** @defaultValue `"Last name is required for individual contractors"` */
      lastName: string
      /** @defaultValue `"Last name is not valid"` */
      lastNameFormat: string
      /** @defaultValue `"SSN is required for individual contractors"` */
      ssn: string
      /** @defaultValue `"SSN must be valid format"` */
      ssnFormat: string
      /** @defaultValue `"Business name is required for business contractors"` */
      businessName: string
      /** @defaultValue `"EIN is required for business contractors"` */
      ein: string
      /** @defaultValue `"EIN must be valid format (XX-XXXXXXX)"` */
      einFormat: string
      /** @defaultValue `"Hourly rate is required for hourly contractors"` */
      hourlyRate: string
      /** @defaultValue `"Start date is required"` */
      startDate: string
    }
    buttons: {
      /** @defaultValue `"Cancel"` */
      cancel: string
      /** @defaultValue `"Create Contractor"` */
      create: string
      /** @defaultValue `"Continue"` */
      update: string
      /** @defaultValue `"Creating…"` */
      creating: string
      /** @defaultValue `"Saving…"` */
      updating: string
    }
  }
  /** Translation keys for the `Contractor.SignatureForm` i18n namespace. */
  export interface ContractorSignatureForm {
    /** @defaultValue `"Signature required"` */
    signatureRequired: string
    /** @defaultValue `"See the IRS Form W-9 for instructions on completing this form"` */
    instructions: string
    /** @defaultValue `"<downloadLink>Download document</downloadLink>"` */
    downloadPrompt: string
    sections: {
      /** @defaultValue `"Exemptions"` */
      exemptions: string
      /** @defaultValue `"Address"` */
      address: string
      /** @defaultValue `"Taxpayer Identification Number (TIN)"` */
      tin: string
      /** @defaultValue `"Certification"` */
      certification: string
    }
    sectionInstructions: {
      /** @defaultValue `"See section 4 of IRS Form W-9 for instructions."` */
      exemptions: string
      /** @defaultValue `"See sections 5-7 of IRS Form W-9 for instructions."` */
      address: string
      /** @defaultValue `"For individuals, this is generally your social security number (SSN). For other entities, this will be your employer identification number (EIN)."` */
      tin: string
      /** @defaultValue `"See Part 1 of the IRS Form W-9 for instructions."` */
      tinSecondary: string
      /** @defaultValue `"See Part 2 of the IRS Form W-9 for instructions."` */
      certification: string
    }
    /** @defaultValue `"Under penalties of perjury, I certify that:"` */
    certificationIntro: string
    certificationPoints: {
      /** @defaultValue `"The number shown on this form is my correct taxpayer identification number (or I am waiting for a number to be issued to me);"` */
      taxpayerId: string
      /** @defaultValue `"I am not subject to backup withholding because (a) I am exempt from backup withholding, or (b) I have not been notified by the Internal Revenue Service (IRS) that I am subject to backup withholding as a result of a failure to report all interest or dividends, or (c) the IRS has notified me that I am no longer subject to backup withholding;"` */
      backupWithholding: string
      /** @defaultValue `"I am a U.S. citizen or other U.S. person (defined below);"` */
      usPerson: string
      /** @defaultValue `"For federal tax purposes, you are considered a U.S. person if you are: an individual who is a U.S. citizen or U.S. resident alien; a partnership, corporation, company, or association created or organized in the United States or under the laws of the United States; an estate (other than a foreign estate); or a domestic trust (as defined in Regulations section 301.7701-7)."` */
      usPersonDefinition: string
      /** @defaultValue `"The FATCA code(s) entered on this form (if any) indicating that I am exempt from FATCA reporting is correct."` */
      fatca: string
    }
    fields: {
      name: {
        /** @defaultValue `"Entity or individual name (1)"` */
        label: string
        /** @defaultValue `"Your individual or business name. For a sole proprietor or disregarded entity, enter the owner's name here, and enter the business/disregarded entity's name in the Business name field below."` */
        description: string
      }
      business_name: {
        /** @defaultValue `"Business name (2)"` */
        label: string
        /** @defaultValue `"The name of your business, if different than above."` */
        description: string
      }
      taxClassification: {
        /** @defaultValue `"Federal tax classification (3)"` */
        label: string
        /** @defaultValue `"Select the type of business you are."` */
        description: string
      }
      llcClassificationCode: {
        /** @defaultValue `"LLC tax classification code"` */
        label: string
        /** @defaultValue `"Select the appropriate code for the LLC, unless it is a disregarded entity. A disregarded entity should instead check the appropriate selection for the tax classification of its owner."` */
        description: string
      }
      other_text: {
        /** @defaultValue `"Other"` */
        label: string
      }
      foreign_partners: {
        /** @defaultValue `"Foreign partners/owners/beneficiaries (3b)"` */
        label: string
        /** @defaultValue `"If you selected “Partnership”, “Trust/Estate” or checked “LLC” and entered “P” as the tax classification, and you are providing this form to a partnership, trust, or estate in which you have an ownership interest, check this box if you have any foreign partners, owners, or beneficiaries."` */
        description: string
      }
      exempt_payee_code: {
        /** @defaultValue `"Exempt payee code"` */
        label: string
      }
      exemption_from_FATCA: {
        /** @defaultValue `"Exemption from FATCA reporting code (4b)"` */
        label: string
        /** @defaultValue `"Enter the code (if any) indicating you are exempt from reporting under the Foreign Account Tax Compliance Act."` */
        description: string
      }
      home_address_street_1: {
        /** @defaultValue `"Street 1"` */
        label: string
      }
      home_address_street_2: {
        /** @defaultValue `"Street 2"` */
        label: string
      }
      home_address_city: {
        /** @defaultValue `"City"` */
        label: string
      }
      home_address_state: {
        /** @defaultValue `"State"` */
        label: string
      }
      home_address_zip: {
        /** @defaultValue `"Zip code"` */
        label: string
      }
      account_number: {
        /** @defaultValue `"Account numbers"` */
        label: string
      }
      company_name: {
        /** @defaultValue `"Requester's name and address"` */
        label: string
      }
      ssn: {
        /** @defaultValue `"Social Security Number (SSN)"` */
        label: string
        /** @defaultValue `"Change SSN"` */
        changeCta: string
      }
      ein: {
        /** @defaultValue `"Employer Identification Number (EIN)"` */
        label: string
        /** @defaultValue `"Change EIN"` */
        changeCta: string
      }
      signature_text: {
        /** @defaultValue `"Signature"` */
        label: string
      }
    }
    options: {
      taxClassification: {
        /** @defaultValue `"Individual/sole proprietor"` */
        individual_proprietor: string
        /** @defaultValue `"C-Corporation"` */
        c_corporation: string
        /** @defaultValue `"S-Corporation"` */
        s_corporation: string
        /** @defaultValue `"Partnership"` */
        partnership: string
        /** @defaultValue `"Trust/estate"` */
        trust_estate: string
        /** @defaultValue `"LLC"` */
        limited_liability_company: string
        /** @defaultValue `"Other"` */
        other: string
      }
      llcClassificationCode: {
        /** @defaultValue `"Select..."` */
        placeholder: string
        /** @defaultValue `"C = C-Corporation"` */
        c: string
        /** @defaultValue `"S = S-Corporation"` */
        s: string
        /** @defaultValue `"P = Partnership"` */
        p: string
      }
    }
    /** @defaultValue `"I agree to electronically sign this form."` */
    agreeLabel: string
    validation: {
      /** @defaultValue `"This field is required."` */
      required: string
      /** @defaultValue `"You must agree to electronically sign this form."` */
      agreeRequired: string
      /** @defaultValue `"Enter a valid Social Security Number."` */
      invalidSsn: string
      /** @defaultValue `"Enter a valid Employer Identification Number."` */
      invalidEin: string
    }
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Sign"` */
    signCta: string
    /** @defaultValue `"Signing…"` */
    signingCta: string
    /** @defaultValue `"Acknowledge"` */
    acknowledgeCta: string
    /** @defaultValue `"Acknowledging…"` */
    acknowledgingCta: string
  }
  /** Translation keys for the `Contractor.Submit` i18n namespace. */
  export interface ContractorSubmit {
    /** @defaultValue `"Submit contractor"` */
    heading: string
    /** @defaultValue `"Contractor Onboarded"` */
    doneTitle: string
    /** @defaultValue `"This contractor has been successfully onboarded."` */
    doneDescription: string
    /** @defaultValue `"Done"` */
    doneCta: string
    /** @defaultValue `"Submit and complete onboarding"` */
    submitCta: string
    /** @defaultValue `"Please note that after submitting"` */
    title: string
    documentRequirements: {
      /** @defaultValue `"Documents"` */
      title: string
      /** @defaultValue `"You will need to collect signed copies of the following documents from {{contractorName}}."` */
      description: string
      /** @defaultValue `"Download document"` */
      downloadCta: string
      documents: {
        taxpayer_identification_form_w_9: {
          /** @defaultValue `"Taxpayer Identification (Form W-9)"` */
          title: string
          /** @defaultValue `"Verifies your contractor's identity for tax purposes such as generating their Form 1099."` */
          description: string
        }
      }
      /** @defaultValue `"The government requires you to have Form W-9 completed and signed."` */
      alertLabel: string
    }
    warningItems: {
      /** @defaultValue `"The contractor type cannot be changed."` */
      '0': string
      /** @defaultValue `"The new hire report cannot be modified."` */
      '1': string
    }
    inviteContractor: {
      /** @defaultValue `"Review before sending invitation"` */
      title: string
      /** @defaultValue `"You are inviting a contractor to complete the onboarding flow. Please double-check that you've entered this contractor's details correctly."` */
      description: string
      /** @defaultValue `"Start date"` */
      startDateLabel: string
      /** @defaultValue `"Send invitation"` */
      inviteCta: string
      /** @defaultValue `"Contractor has been saved & invited to self-onboard!"` */
      successMessage: string
    }
    submitDone: {
      /** @defaultValue `"Contractor has been onboarded!"` */
      successMessage: string
    }
  }
  /** Translation keys for the `Employee.BankAccount` i18n namespace. */
  export interface EmployeeBankAccount {
    /** @defaultValue `"Account number"` */
    accountNumberLabel: string
    /** @defaultValue `"Checking"` */
    accountTypeChecking: string
    /** @defaultValue `"Account type"` */
    accountTypeLabel: string
    /** @defaultValue `"Savings"` */
    accountTypeSavings: string
    /** @defaultValue `"Select..."` */
    accountTypeSelect: string
    /** @defaultValue `"Back"` */
    cancelCta: string
    /** @defaultValue `"Image of the sample bank check showing location of routing and account numbers"` */
    checkImageAlt: string
    /** @defaultValue `"Enter the details of the bank account the employee wishes to be paid with. We’ll send a small test deposit to confirm the account."` */
    description: string
    /** @defaultValue `"Account Nickname"` */
    nameLabel: string
    /** @defaultValue `"Routing number"` */
    routingNumberLabel: string
    /** @defaultValue `"(9 digits)"` */
    routingNumberDescription: string
    /** @defaultValue `"Save & continue"` */
    submitCta: string
    /** @defaultValue `"Employee bank account"` */
    title: string
    validations: {
      /** @defaultValue `"Please select account type"` */
      accountType: string
    }
  }
  /** Translation keys for the `Employee.BankFormBody` i18n namespace. */
  export interface EmployeeBankFormBody {
    /** @defaultValue `"Account nickname"` */
    nameLabel: string
    /** @defaultValue `"Routing number"` */
    routingNumberLabel: string
    /** @defaultValue `"(9 digits)"` */
    routingNumberDescription: string
    /** @defaultValue `"Account number"` */
    accountNumberLabel: string
    /** @defaultValue `"Account type"` */
    accountTypeLabel: string
    /** @defaultValue `"Checking"` */
    accountTypeChecking: string
    /** @defaultValue `"Savings"` */
    accountTypeSavings: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    validations: {
      /** @defaultValue `"Account name is required"` */
      accountName: string
      /** @defaultValue `"Routing number should be a number (9 digits)"` */
      routingNumber: string
      /** @defaultValue `"Account number is a required field"` */
      accountNumber: string
      /** @defaultValue `"Account number should contain only digits (up to 17)"` */
      accountNumberFormat: string
    }
  }
  /** Translation keys for the `Employee.Compensation` i18n namespace. */
  export interface EmployeeCompensation {
    /** @defaultValue `"+ Add another job"` */
    addAnotherJobCta: string
    /** @defaultValue `"Add another job"` */
    addAnotherJobTitle: string
    /** @defaultValue `"Adjust for minimum wage"` */
    adjustForMinimumWage: string
    /** @defaultValue `"Determines whether the compensation should be adjusted for minimum wage. Only applies to Nonexempt employees."` */
    adjustForMinimumWageDescription: string
    /** @defaultValue `"Minimum wage"` */
    minimumWageLabel: string
    /** @defaultValue `"What minimum wage requirement should compensation be adjusted to"` */
    minimumWageDescription: string
    allCompensations: {
      /** @defaultValue `"Amount"` */
      amountColumn: string
      /** @defaultValue `"Delete"` */
      deleteCta: string
      /** @defaultValue `"Edit"` */
      editCta: string
      /** @defaultValue `"Job title"` */
      jobColumn: string
      /** @defaultValue `"Per"` */
      perColumn: string
      /** @defaultValue `"List of all jobs for the employee"` */
      tableLabel: string
      /** @defaultValue `"Pay type"` */
      typeColumn: string
    }
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Cancel"` */
    cancelNewJobCta: string
    /** @defaultValue `"<ClassificationLink href=\"https://support.gusto.com/team-management/team-payments/pay-rates/1001671771/Employee-classification-options.htm\" target=\"_blank\">Learn more about employee classifications.</ClassificationLink>"` */
    classificationLink: string
    commissionAlerts: {
      federalMinimumPay: {
        /** @defaultValue `"Commission-only employees must earn the federal minimum pay"` */
        label: string
        /** @defaultValue `"Federal laws say that employees not eligible for overtime should be paid at least $684 per week ($35,568 per year). You’ve classified this employee as not eligible for overtime—make sure they meet the Department of Labor’s definition of an exempt employee."` */
        body: string
      }
      minimumWage: {
        /** @defaultValue `"Commission-only employees must earn at least the minimum wage"` */
        label: string
        /** @defaultValue `"To stay compliant, <minimumWageLink>check your local regulations</minimumWageLink>."` */
        body: string
      }
      ownerSalary: {
        /** @defaultValue `"The IRS requires owners of S corps to pay themselves a reasonable salary similar to others in the same role, before taking any distributions."` */
        label: string
      }
    }
    /** @defaultValue `"Employee type"` */
    employeeClassification: string
    /** @defaultValue `"Select classification..."` */
    flsaStatusPlaceholder: string
    /** @defaultValue `"Select wage frequency..."` */
    paymentUnitPlaceholder: string
    /** @defaultValue `"Select minimum wage..."` */
    minimumWagePlaceholder: string
    flsaStatusLabels: {
      /** @defaultValue `"Commission Only/No Overtime"` */
      'Commission Only Exempt': string
      /** @defaultValue `"Commission Only/Eligible for overtime"` */
      'Commission Only Nonexempt': string
      /** @defaultValue `"Salary/No overtime"` */
      Exempt: string
      /** @defaultValue `"Paid by the hour"` */
      Nonexempt: string
      /** @defaultValue `"Owner's draw"` */
      Owner: string
      /** @defaultValue `"Salary/Eligible for overtime"` */
      'Salaried Nonexempt': string
    }
    /** @defaultValue `"Job actions"` */
    hamburgerTitle: string
    /** @defaultValue `"Job Title"` */
    jobTitle: string
    /** @defaultValue `"The period over which the compensation amount is tracked (e.g., hourly, daily, weekly, monthly, annually)."` */
    paymentUnitDescription: string
    /** @defaultValue `"Wage"` */
    wageLabel: string
    /** @defaultValue `"Wage frequency"` */
    wageFrequencyLabel: string
    paymentUnitOptions: {
      /** @defaultValue `"Hour"` */
      Hour: string
      /** @defaultValue `"Month"` */
      Month: string
      /** @defaultValue `"Paycheck"` */
      Paycheck: string
      /** @defaultValue `"Week"` */
      Week: string
      /** @defaultValue `"Year"` */
      Year: string
    }
    /** @defaultValue `"Effective date"` */
    effectiveDate: string
    /** @defaultValue `"Changes will take effect on this date."` */
    effectiveDateDescription: string
    /** @defaultValue `"Start date"` */
    hireDate: string
    /** @defaultValue `"Save job"` */
    saveNewJobCta: string
    /** @defaultValue `"Continue"` */
    submitCta: string
    /** @defaultValue `"Compensation"` */
    title: string
    /** @defaultValue `"Edit job"` */
    editTitle: string
    /** @defaultValue `"Add job"` */
    addTitle: string
    /** @defaultValue `"Effective date"` */
    effectiveDateLabel: string
    validations: {
      /** @defaultValue `"Effective date is a required field"` */
      effectiveDate: string
      /** @defaultValue `"Effective date cannot be before the employee's hire date."` */
      effectiveDateBeforeHire: string
      /** @defaultValue `"Start date is required"` */
      hireDate: string
      /** @defaultValue `"Changing this employee's classification will immediately delete their additional jobs."` */
      classificationChangeNotification: string
      /** @defaultValue `"Only employees that are paid by the hour can have multiple jobs."` */
      classificationChangeRemovesSecondaryJobs: string
      /** @defaultValue `"Most employees who make under {{limit}}/year should be eligible for overtime."` */
      exemptThreshold: string
      /** @defaultValue `"Payment unit must be one of Hour, Week, Month, or Year"` */
      paymentUnit: string
      /** @defaultValue `"Amount is a required field"` */
      rate: string
      /** @defaultValue `"Amount must be at least $1.00"` */
      nonZeroRate: string
      /** @defaultValue `"FLSA Exempt employees must meet salary threshold of {{limit}}/year"` */
      rateExemptThreshold: string
      /** @defaultValue `"Title is a required field"` */
      title: string
      /** @defaultValue `"Please select minimum wage for adjustment"` */
      minimumWage: string
      /** @defaultValue `"Please select a risk class code"` */
      stateWcClassCode: string
      /** @defaultValue `"Effective date must be in the future"` */
      effectiveDateBeforeMin: string
      /** @defaultValue `"Job title is a required field"` */
      jobTitleSentence: string
    }
    /** @defaultValue `"Workers' compensation coverage"` */
    stateWcCoveredLabel: string
    /** @defaultValue `"Washington administers <wcLink>workers’ compensation insurance</wcLink> to protect workers and employers from the financial impact of a work-related injury. Indicate here if this employee is exempt from the workers’ comp tax."` */
    stateWcCoveredDescription: string
    stateWcCoveredOptions: {
      /** @defaultValue `"Yes, this employee is covered"` */
      yes: string
      /** @defaultValue `"No, this employee is not covered"` */
      no: string
    }
    /** @defaultValue `"Risk class code"` */
    stateWcClassCodeLabel: string
    /** @defaultValue `"The risk class code associated with this employee’s job function. We need this to pay and file your taxes correctly."` */
    stateWcClassCodeDescription: string
    /** @defaultValue `"Select if employee is a 2% shareholder"` */
    twoPercentStakeholderLabel: string
  }
  /** Translation keys for the `Employee.Dashboard` i18n namespace. */
  export interface EmployeeDashboard {
    /** @defaultValue `"Employee"` */
    employeeRoleLabel: string
    /** @defaultValue `"Employee dashboard tabs"` */
    tabsLabel: string
    tabs: {
      /** @defaultValue `"Basic details"` */
      basicDetails: string
      /** @defaultValue `"Job and pay"` */
      jobAndPay: string
      /** @defaultValue `"Taxes"` */
      taxes: string
      /** @defaultValue `"Documents"` */
      documents: string
    }
    alerts: {
      /** @defaultValue `"Bank account successfully added."` */
      bankAccountAdded: string
      /** @defaultValue `"Bank account successfully deleted."` */
      bankAccountDeleted: string
      /** @defaultValue `"Split payment successfully updated."` */
      splitUpdated: string
      /** @defaultValue `"Deduction successfully added."` */
      deductionAdded: string
      /** @defaultValue `"Deduction successfully updated."` */
      deductionUpdated: string
      /** @defaultValue `"Deduction successfully deleted."` */
      deductionDeleted: string
      /** @defaultValue `"Job successfully added."` */
      jobAdded: string
      /** @defaultValue `"Profile updated"` */
      profileUpdated: string
      /** @defaultValue `"Federal tax settings successfully updated."` */
      federalTaxesUpdated: string
      /** @defaultValue `"Successfully updated state tax settings."` */
      stateTaxesUpdated: string
    }
  }
  /** Translation keys for the `Employee.Deductions` i18n namespace. */
  export interface EmployeeDeductions {
    /** @defaultValue `"Deductions"` */
    pageTitle: string
    /** @defaultValue `"Include deductions for this employee?"` */
    includeDeductionsFormLabel: string
    /** @defaultValue `"If you need to deduct money from this employee's paycheck (e.g. for Housing, Meals, or Transportation), you can choose either a one-time deduction or an ongoing deduction that occurs every pay period."` */
    includeDeductionsDescription: string
    /** @defaultValue `"If you need to deduct money from this employee's paycheck (e.g. for child support, housing, meals, or transportation), you can choose either a one-time deduction or an ongoing deduction that occurs every pay period."` */
    includeDeductionsDescriptionV2: string
    /** @defaultValue `"You haven't added any deductions yet"` */
    includeDeductionsEmptyState: string
    /** @defaultValue `"Yes"` */
    includeDeductionsYes: string
    /** @defaultValue `"No"` */
    includeDeductionsNo: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Add Deduction"` */
    addDeductionTitle: string
    /** @defaultValue `"Add deduction"` */
    addDeductionButtonCta: string
    /** @defaultValue `"Back to deductions"` */
    backToDeductionsCta: string
    /** @defaultValue `"External post tax deductions"` */
    externalPostTaxDeductions: string
    /** @defaultValue `"Amounts are withheld from the net pay of an employee and reported on the paystub and within payroll receipts. Note that these deductions are always post-tax and will start with the next pay period."` */
    externalPostTaxDeductionsDescription: string
    /** @defaultValue `"Garnishment (a court-ordered deduction)"` */
    garnishmentOption: string
    /** @defaultValue `"Garnishment type"` */
    garnishmentType: string
    /** @defaultValue `"Child support"` */
    childSupport: string
    /** @defaultValue `"Child Support"` */
    childSupportTitle: string
    /** @defaultValue `"Custom deduction"` */
    customDeductionTitle: string
    /** @defaultValue `"All counties"` */
    allCounties: string
    /** @defaultValue `"Agency"` */
    agency: string
    /** @defaultValue `"Select the appropriate state agency."` */
    agencyDescription: string
    /** @defaultValue `"County"` */
    county: string
    /** @defaultValue `"Select the appropriate state county"` */
    countyDescription: string
    /** @defaultValue `"CSE Case Number"` */
    caseNumber: string
    /** @defaultValue `"Carefully enter the Child Support Enforcement Case Number"` */
    caseNumberDescription: string
    /** @defaultValue `"Enter the unique Order Identifier or Order ID associated with this child support obligation."` */
    orderNumberDescription: string
    /** @defaultValue `"Carefully enter the Child Support Enforcement Remittance ID"` */
    remittanceNumberDescription: string
    /** @defaultValue `"Total amount withheld"` */
    totalAmountWithheld: string
    /** @defaultValue `"Enter the amount indicated in the letter from the child support agency"` */
    totalAmountWithheldDescription: string
    /** @defaultValue `"Max paycheck percentage"` */
    maxPaycheckPercentage: string
    /** @defaultValue `"Enter the maximum percentage of your employee's paycheck that we should withhold. You can find this info in the letter from the child support agency."` */
    maxPaycheckPercentageDescription: string
    /** @defaultValue `"This agency doesn't support electronic payments. You are responsible for paying the agency yourself."` */
    manualPaymentRequired: string
    /** @defaultValue `"Per"` */
    per: string
    /** @defaultValue `"Enter how often the agency collects the withholding amount"` */
    perDescription: string
    /** @defaultValue `"Every week"` */
    everyWeek: string
    /** @defaultValue `"Every other week"` */
    everyOtherWeek: string
    /** @defaultValue `"Twice per month"` */
    twicePerMonth: string
    /** @defaultValue `"Monthly"` */
    monthly: string
    /** @defaultValue `"Custom deduction (post-tax)"` */
    customDeductionOption: string
    /** @defaultValue `"A garnishment is a court-ordered wage reduction—we’ll handle the tax calculations based what you select. For other post-tax deductions, choose Custom Deduction."` */
    deductionTypeRadioLabel: string
    /** @defaultValue `"Edit Deduction"` */
    editDeductionTitle: string
    /** @defaultValue `"Deduction description"` */
    descriptionLabel: string
    /** @defaultValue `"Description"` */
    descriptionLabelV2: string
    /** @defaultValue `"Deduction frequency"` */
    frequencyLabel: string
    /** @defaultValue `"This deduction occurs every payroll"` */
    frequencyRecurringOption: string
    /** @defaultValue `"Recurring (every payroll)"` */
    frequencyRecurringOptionV2: string
    /** @defaultValue `"This is a one-time deduction and only applies to the next upcoming payroll"` */
    frequencyOneTimeOption: string
    /** @defaultValue `"One-time (next payroll only)"` */
    frequencyOneTimeOptionV2: string
    /** @defaultValue `"List of current deductions of an employee"` */
    deductionsTableLabel: string
    /** @defaultValue `"Deduction type"` */
    deductionTypeLabel: string
    /** @defaultValue `"Percentage or fixed"` */
    deductionTypeLabelV2: string
    /** @defaultValue `"Percentage"` */
    deductionTypePercentageOption: string
    /** @defaultValue `"Percentage of pay"` */
    deductionTypePercentageOptionV2: string
    /** @defaultValue `"Fixed dollar amount"` */
    deductionTypeFixedAmountOption: string
    /** @defaultValue `"Amount to withhold"` */
    deductionAmountLabel: string
    /** @defaultValue `"Enter the percentage of your employee’s wages to withhold."` */
    deductionAmountDescriptionPercentage: string
    /** @defaultValue `"Enter the amount of money to withhold each pay period from your employee’s wages."` */
    deductionAmountDescriptionFixed: string
    /** @defaultValue `"Pay period maximum"` */
    payPeriodMaximum: string
    /** @defaultValue `"Total amount owed"` */
    totalAmountLabel: string
    /** @defaultValue `"We will adjust the amount of the last payment and stop collecting once the total amount is reached."` */
    totalAmountDescription: string
    /** @defaultValue `"Annual maximum"` */
    annualMaxLabel: string
    /** @defaultValue `"The maximum annual amount you deduct from the employee's pay for this specific deduction. Leave this field blank if there is no maximum."` */
    annualMaxDescription: string
    /** @defaultValue `"This is a court-ordered deduction"` */
    courtOrderedLabel: string
    /** @defaultValue `"Deduction"` */
    nameColumn: string
    /** @defaultValue `"Frequency"` */
    frequencyColumn: string
    /** @defaultValue `"Withheld"` */
    withheldColumn: string
    /** @defaultValue `"Actions"` */
    actionsColumn: string
    /** @defaultValue `"Recurring"` */
    recurringText: string
    /** @defaultValue `"{{value}} per paycheck"` */
    recurringAmount: string
    /** @defaultValue `"One-time"` */
    nonRecurringText: string
    /** @defaultValue `"Once added, your deductions will appear here."` */
    emptyListMessage: string
    /** @defaultValue `"Deduction actions menu"` */
    hamburgerTitle: string
    /** @defaultValue `"Edit deduction"` */
    editCta: string
    /** @defaultValue `"Delete deduction"` */
    deleteCta: string
    deleteDeductionDialog: {
      /** @defaultValue `"Delete this deduction?"` */
      title: string
      /** @defaultValue `"{{deduction}} will no longer be deducted from this employee's paycheck."` */
      description: string
      /** @defaultValue `"Delete"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    /** @defaultValue `"Add another deduction"` */
    addDeductionCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    validations: {
      /** @defaultValue `"Description is required"` */
      description: string
      /** @defaultValue `"Amount must be a valid number"` */
      amount: string
    }
    /** @defaultValue `"Federal Tax Lien"` */
    federalTaxLien: string
    /** @defaultValue `"State Tax Lien"` */
    stateTaxLien: string
    /** @defaultValue `"Student Loan"` */
    studentLoan: string
    /** @defaultValue `"Creditor Garnishment"` */
    creditorGarnishment: string
    /** @defaultValue `"Federal Loan"` */
    federalLoan: string
    /** @defaultValue `"Other Garnishment"` */
    otherGarnishment: string
    /** @defaultValue `"Description is required"` */
    descriptionRequired: string
    /** @defaultValue `"Frequency is required"` */
    frequencyRequired: string
    /** @defaultValue `"Deduction type is required"` */
    deductionTypeRequired: string
    /** @defaultValue `"Amount is required"` */
    amountRequired: string
    /** @defaultValue `"Amount must be 0 or greater"` */
    amountNonNegative: string
    /** @defaultValue `"Agency is required"` */
    agencyRequired: string
    /** @defaultValue `"County is required"` */
    countyRequired: string
    /** @defaultValue `"Case number is required"` */
    caseNumberRequired: string
    /** @defaultValue `"Order number is required"` */
    orderNumberRequired: string
    /** @defaultValue `"Remittance number is required"` */
    remittanceNumberRequired: string
    /** @defaultValue `"Pay period maximum is required"` */
    payPeriodMaximumRequired: string
    /** @defaultValue `"Must be between 0 and 100"` */
    percentOutOfRange: string
    /** @defaultValue `"Payment period is required"` */
    paymentPeriodRequired: string
  }
  /** Translation keys for the `Employee.DeductionsForm` i18n namespace. */
  export interface EmployeeDeductionsForm {
    /** @defaultValue `"Add Deduction"` */
    addTitle: string
    /** @defaultValue `"Edit Deduction"` */
    editTitle: string
    /** @defaultValue `"Amounts are withheld from the net pay of an employee and reported on the paystub and within payroll receipts. Note that these deductions are always post-tax and will start with the next pay period."` */
    description: string
    /** @defaultValue `"Deduction type"` */
    variantLabel: string
    /** @defaultValue `"A garnishment is a court-ordered wage reduction—we’ll handle the tax calculations based what you select. For other post-tax deductions, choose Custom Deduction."` */
    variantDescription: string
    /** @defaultValue `"Garnishment (a court-ordered deduction)"` */
    garnishmentOption: string
    /** @defaultValue `"Custom deduction (post-tax)"` */
    customOption: string
    /** @defaultValue `"Garnishment type"` */
    garnishmentTypeLabel: string
    types: {
      /** @defaultValue `"Child Support"` */
      childSupport: string
      /** @defaultValue `"Federal Tax Lien"` */
      federalTaxLien: string
      /** @defaultValue `"State Tax Lien"` */
      stateTaxLien: string
      /** @defaultValue `"Student Loan"` */
      studentLoan: string
      /** @defaultValue `"Creditor Garnishment"` */
      creditorGarnishment: string
      /** @defaultValue `"Federal Loan"` */
      federalLoan: string
      /** @defaultValue `"Other Garnishment"` */
      otherGarnishment: string
      /** @defaultValue `"Custom deduction"` */
      custom: string
    }
    standard: {
      /** @defaultValue `"Description"` */
      descriptionLabel: string
      /** @defaultValue `"Description is required"` */
      descriptionRequired: string
      /** @defaultValue `"Deduction frequency"` */
      frequencyLabel: string
      /** @defaultValue `"Recurring (every payroll)"` */
      frequencyRecurring: string
      /** @defaultValue `"One-time (next payroll only)"` */
      frequencyOneTime: string
      /** @defaultValue `"Frequency is required"` */
      frequencyRequired: string
      /** @defaultValue `"Percentage or fixed"` */
      typeLabel: string
      /** @defaultValue `"Percentage of pay"` */
      typePercentage: string
      /** @defaultValue `"Fixed dollar amount"` */
      typeFixed: string
      /** @defaultValue `"Deduction type is required"` */
      typeRequired: string
      /** @defaultValue `"Amount to withhold"` */
      amountLabel: string
      /** @defaultValue `"Enter the percentage of your employee’s wages to withhold."` */
      amountPercentDescription: string
      /** @defaultValue `"Enter the amount of money to withhold each pay period from your employee’s wages."` */
      amountFixedDescription: string
      /** @defaultValue `"Amount is required"` */
      amountRequired: string
      /** @defaultValue `"Amount must be 0 or greater"` */
      amountNonNegative: string
      /** @defaultValue `"Total amount owed"` */
      totalAmountLabel: string
      /** @defaultValue `"We will adjust the amount of the last payment and stop collecting once the total amount is reached."` */
      totalAmountDescription: string
      /** @defaultValue `"Annual maximum"` */
      annualMaxLabel: string
      /** @defaultValue `"The maximum annual amount you deduct from the employee's pay for this specific deduction. Leave this field blank if there is no maximum."` */
      annualMaxDescription: string
    }
    childSupport: {
      /** @defaultValue `"Agency"` */
      agencyLabel: string
      /** @defaultValue `"Select state..."` */
      agencyPlaceholder: string
      /** @defaultValue `"Select the appropriate state agency."` */
      agencyDescription: string
      /** @defaultValue `"Agency is required"` */
      agencyRequired: string
      /** @defaultValue `"This agency doesn't support electronic payments. You are responsible for paying the agency yourself."` */
      manualPaymentRequired: string
      /** @defaultValue `"County"` */
      countyLabel: string
      /** @defaultValue `"Select county..."` */
      countyPlaceholder: string
      /** @defaultValue `"Select the appropriate state county"` */
      countyDescription: string
      /** @defaultValue `"All counties"` */
      allCounties: string
      /** @defaultValue `"County is required"` */
      countyRequired: string
      /** @defaultValue `"Carefully enter the Child Support Enforcement Case Number"` */
      caseNumberDescription: string
      /** @defaultValue `"Case number is required"` */
      caseNumberRequired: string
      /** @defaultValue `"Enter the unique Order Identifier or Order ID associated with this child support obligation."` */
      orderNumberDescription: string
      /** @defaultValue `"Order number is required"` */
      orderNumberRequired: string
      /** @defaultValue `"Carefully enter the Child Support Enforcement Remittance ID"` */
      remittanceNumberDescription: string
      /** @defaultValue `"Remittance number is required"` */
      remittanceNumberRequired: string
      /** @defaultValue `"Total amount withheld"` */
      totalAmountWithheld: string
      /** @defaultValue `"Enter the amount indicated in the letter from the child support agency"` */
      totalAmountWithheldDescription: string
      /** @defaultValue `"Pay period maximum is required"` */
      payPeriodMaximumRequired: string
      /** @defaultValue `"Max paycheck percentage"` */
      maxPaycheckPercentage: string
      /** @defaultValue `"Enter the maximum percentage of your employee's paycheck that we should withhold. You can find this info in the letter from the child support agency."` */
      maxPaycheckPercentageDescription: string
      /** @defaultValue `"Amount is required"` */
      amountRequired: string
      /** @defaultValue `"Amount must be 0 or greater"` */
      amountNonNegative: string
      /** @defaultValue `"Must be between 0 and 100"` */
      percentOutOfRange: string
      /** @defaultValue `"Per"` */
      paymentPeriodLabel: string
      /** @defaultValue `"Select payment period..."` */
      paymentPeriodPlaceholder: string
      /** @defaultValue `"Enter how often the agency collects the withholding amount"` */
      paymentPeriodDescription: string
      /** @defaultValue `"Payment period is required"` */
      paymentPeriodRequired: string
      paymentPeriod: {
        /** @defaultValue `"Every week"` */
        everyWeek: string
        /** @defaultValue `"Every other week"` */
        everyOtherWeek: string
        /** @defaultValue `"Twice per month"` */
        twicePerMonth: string
        /** @defaultValue `"Monthly"` */
        monthly: string
      }
    }
    actions: {
      /** @defaultValue `"Save"` */
      save: string
      /** @defaultValue `"Cancel"` */
      cancel: string
    }
  }
  /** Translation keys for the `Employee.DocumentManager` i18n namespace. */
  export interface EmployeeDocumentManager {
    /** @defaultValue `"View document"` */
    viewDocumentCta: string
    /** @defaultValue `"You can also <downloadLink>download this document</downloadLink>."` */
    downloadDocumentCta: string
    /** @defaultValue `"Back"` */
    backCta: string
  }
  /** Translation keys for the `Employee.DocumentSigner` i18n namespace. */
  export interface EmployeeDocumentSigner {
    /** @defaultValue `"Documents"` */
    documentListTitle: string
    /** @defaultValue `"Documents"` */
    documentListLabel: string
    /** @defaultValue `"Form"` */
    formColumnLabel: string
    /** @defaultValue `"Action"` */
    actionColumnLabel: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Sign document"` */
    signDocumentCta: string
    /** @defaultValue `"Complete"` */
    signDocumentComplete: string
    /** @defaultValue `"Not signed"` */
    notSigned: string
    /** @defaultValue `"Could not load your documents, try again later."` */
    documentListError: string
    /** @defaultValue `"No documents found"` */
    emptyTableTitle: string
    /** @defaultValue `"Signature required for {{formTitle}}"` */
    signatureFormTitle: string
    /** @defaultValue `"{{description}} You may also <downloadLink>download this document</downloadLink>."` */
    downloadPrompt: string
    /** @defaultValue `"Signature"` */
    signatureFieldLabel: string
    /** @defaultValue `"Type your full, legal name."` */
    signatureFieldDescription: string
    /** @defaultValue `"Signature is required"` */
    signatureFieldError: string
    /** @defaultValue `"I am the employee and I agree to sign electronically"` */
    confirmSignatureCheckboxLabel: string
    /** @defaultValue `"You must agree to sign electronically"` */
    confirmSignatureError: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Sign form"` */
    signFormCta: string
    /** @defaultValue `"View document"` */
    viewDocumentCta: string
    /** @defaultValue `"Please review the document. When you're finished, sign below."` */
    downloadAndReviewInstructions: string
  }
  /** Translation keys for the `Employee.EmployeeDocuments` i18n namespace. */
  export interface EmployeeEmployeeDocuments {
    selfOnboarding: {
      /** @defaultValue `"Employee documents"` */
      title: string
      /** @defaultValue `"During onboarding, employees will sign these documents."` */
      description: string
      /** @defaultValue `"We automatically include"` */
      documentsIncludedLabel: string
      /** @defaultValue `"Tax withholding (Form W-4)"` */
      taxWithholdingLabel: string
      /** @defaultValue `"Record employee federal withholding allowance"` */
      taxWithholdingDescription: string
      /** @defaultValue `"Direct deposit authorization"` */
      directDepositLabel: string
      /** @defaultValue `"Lets you pay the employee via direct deposit"` */
      directDepositDescription: string
      /** @defaultValue `"Include form I-9?"` */
      includeI9Heading: string
      /** @defaultValue `"Employment eligibility (Form I-9)"` */
      includeI9Label: string
      /** @defaultValue `"Verifies the employee's identity and work eligibility (must be completed onsite)."` */
      includeI9Description: string
      alerts: {
        /** @defaultValue `"Verifying Form I-9 remotely?"` */
        includedTitle: string
        /** @defaultValue `"The federal government requires remote verification through a video call. You'll need to complete the verification process outside this application. <Link href=\"https://support.gusto.com/article/100165395100000/Complete-Form-I-9-and-E-Verify-for-new-hires\" target=\"_blank\">Read the instructions</Link>."` */
        includedDescription: string
        /** @defaultValue `"To avoid duplicate I-9 forms, deselect “employment eligibility” above."` */
        includedDescription2: string
        /** @defaultValue `"Have you completed the form I-9 already?"` */
        notIncludedTitle: string
        /** @defaultValue `"If you are verifying the employee’s identity in person, select Form I-9 above. If verifying remotely, read <Link href=\"https://support.gusto.com/article/100165395100000/Complete-Form-I-9-and-E-Verify-for-new-hires\" target=\"_blank\">these instructions</Link> ."` */
        notIncludedDescription: string
      }
    }
    notSelfOnboarding: {
      /** @defaultValue `"Employee documents"` */
      title: string
      /** @defaultValue `"You will sign all these forms for your employee manually."` */
      description: string
      /** @defaultValue `"Employment eligibility (Form I-9)"` */
      employmentEligibilityLabel: string
      /** @defaultValue `"Verifies the employee's identity and work eligibility (must be completed onsite)."` */
      employmentEligibilityDescription: string
      /** @defaultValue `"Tax withholding (Form W-4)"` */
      taxWithholdingLabel: string
      /** @defaultValue `"Record employee federal withholding allowance"` */
      taxWithholdingDescription: string
      /** @defaultValue `"Direct deposit authorization"` */
      directDepositLabel: string
      /** @defaultValue `"Lets you pay the employee via direct deposit"` */
      directDepositDescription: string
      /** @defaultValue `"The government requires you to have Form I-9 and Form W-4 completed and signed."` */
      alertTitle: string
    }
    /** @defaultValue `"Continue"` */
    continueCta: string
  }
  /** Translation keys for the `Employee.EmployeeList` i18n namespace. */
  export interface EmployeeEmployeeList {
    /** @defaultValue `"Actions"` */
    actionLabel: string
    /** @defaultValue `"Add an employee"` */
    addEmployeeCta: string
    /** @defaultValue `"I'll do this later"` */
    skipCta: string
    /** @defaultValue `"Add another employee"` */
    addAnotherCta: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Addresses"` */
    addressesCta: string
    /** @defaultValue `"Deductions"` */
    deductionsCta: string
    /** @defaultValue `"Delete employee"` */
    deleteCta: string
    /** @defaultValue `"Edit employee"` */
    editCta: string
    /** @defaultValue `"Cancel self-onboarding"` */
    cancelSelfOnboardingCta: string
    /** @defaultValue `"Review"` */
    reviewCta: string
    /** @defaultValue `"List of current employees"` */
    employeeListLabel: string
    /** @defaultValue `"Once you’ve added employees they will appear here"` */
    emptyTableDescription: string
    /** @defaultValue `"No employees"` */
    emptyTableTitle: string
    /** @defaultValue `"Federal Taxes"` */
    federalTaxesCta: string
    /** @defaultValue `"Employee actions menu"` */
    hamburgerTitle: string
    /** @defaultValue `"Employee name"` */
    nameLabel: string
    /** @defaultValue `"Status"` */
    statusLabel: string
    /** @defaultValue `"Your employees"` */
    title: string
    deleteDialog: {
      /** @defaultValue `"Delete employee?"` */
      title: string
      /** @defaultValue `"This will permanently delete this employee from your account. This action cannot be undone."` */
      description: string
      /** @defaultValue `"Delete employee"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
  }
  /** Translation keys for the `Employee.EmploymentEligibility` i18n namespace. */
  export interface EmployeeEmploymentEligibility {
    /** @defaultValue `"Employment Eligibility"` */
    title: string
    /** @defaultValue `"The federal government requires verification of your eligibility to work in the United States. For more information, see <formI9Link href=\"https://www.uscis.gov/i-9\" target=\"_blank\">Form I-9</formI9Link>"` */
    subtitle: string
    select: {
      /** @defaultValue `"I am..."` */
      label: string
      /** @defaultValue `"This information will be used to fill out Form I-9, which proves you are legally allowed to work in the United States."` */
      description: string
      /** @defaultValue `"Select status..."` */
      placeholder: string
      options: {
        /** @defaultValue `"a citizen of the United States"` */
        citizen: string
        /** @defaultValue `"a lawful permanent resident"` */
        permanent_resident: string
        /** @defaultValue `"a noncitizen national"` */
        noncitizen: string
        /** @defaultValue `"a noncitizen authorized to work"` */
        alien: string
      }
    }
    statusDescriptions: {
      /** @defaultValue `"A citizen is someone who was born or naturalized in the U.S. or born abroad to U.S. citizen parent(s) or who has derived citizenship through a parent's naturalization based on legal requirements."` */
      citizen: string
      /** @defaultValue `"A lawful permanent resident is someone who is not a US citizen and who resides under legally recognized and lawfully recorded permanent residence as an immigrant."` */
      permanent_resident: string
      /** @defaultValue `"A noncitizen national is someone born in American Samoa, certain former citizens of the former Trust Territory of the Pacific Islands, and certain children of noncitizen nationals born abroad."` */
      noncitizen: string
      /** @defaultValue `"A noncitizen authorized to work includes anyone who is authorized to work in the United States but is not a US citizen, US national or lawful permanent resident."` */
      alien: string
    }
    expirationDate: {
      /** @defaultValue `"Authorized to work until"` */
      label: string
    }
    authorizationDocument: {
      /** @defaultValue `"Authorization document"` */
      label: string
      options: {
        /** @defaultValue `"USCIS or A-Number"` */
        uscis_alien_registration_number: string
        /** @defaultValue `"Form I-94"` */
        form_i94: string
        /** @defaultValue `"Foreign passport"` */
        foreign_passport: string
      }
    }
    documentNumber: {
      uscis_alien_registration_number: {
        /** @defaultValue `"USCIS or A-Number"` */
        label: string
        /** @defaultValue `"Fill in a 7-9 digit USCIS Number or A-Number (include the \"A\")"` */
        description: string
        /** @defaultValue `"********"` */
        placeholder: string
      }
      form_i94: {
        /** @defaultValue `"Form I-94 admission number"` */
        label: string
        /** @defaultValue `"Fill in your 11-character I-94 admission number"` */
        description: string
        /** @defaultValue `"***********"` */
        placeholder: string
      }
      foreign_passport: {
        /** @defaultValue `"Foreign passport number"` */
        label: string
        /** @defaultValue `"**********"` */
        placeholder: string
      }
    }
    country: {
      /** @defaultValue `"Country of Issuance"` */
      label: string
      /** @defaultValue `"The country that issues your passport"` */
      description: string
      /** @defaultValue `"Search or type a country..."` */
      placeholder: string
    }
    /** @defaultValue `"Continue"` */
    submit: string
  }
  /** Translation keys for the `Employee.FederalTaxes` i18n namespace. */
  export interface EmployeeFederalTaxes {
    /** @defaultValue `"Additional withholding"` */
    additionalWithholding: string
    /** @defaultValue `"Step 4b: Deductions"` */
    deductions: string
    /** @defaultValue `"Step 3: Dependents (if applicable)"` */
    dependentsTotalIfApplicable: string
    /** @defaultValue `"Step 4c: Extra withholding"` */
    extraWithholding: string
    /** @defaultValue `"Step 1c: Federal filing status (1c)"` */
    federalFilingStatus1c: string
    /** @defaultValue `"Select filing status..."` */
    federalFilingStatusPlaceholder: string
    /** @defaultValue `"Federal tax withholdings (Form W-4)"` */
    federalTaxesTitle: string
    /** @defaultValue `"Federal withholding allowance"` */
    federalWithholdingAllowance: string
    /** @defaultValue `"This field is a required field. Please enter a value."` */
    fieldIsRequired: string
    /** @defaultValue `"Filing status"` */
    filingStatus: string
    /** @defaultValue `"Exempt from withholding"` */
    filingStatusExemptFromWithholding: string
    /** @defaultValue `"Head of household"` */
    filingStatusHeadOfHousehold: string
    /** @defaultValue `"Married"` */
    filingStatusMarried: string
    /** @defaultValue `"Married, but withhold as Single"` */
    filingStatusMarriedWithholdAsSingle: string
    /** @defaultValue `"Single"` */
    filingStatusSingle: string
    /** @defaultValue `"Includes spouse, if applicable. Answering 2c will result in a higher withholding, but to preserve privacy, this can be left unchecked. <IrsLink href=\"https://www.irs.gov/newsroom/faqs-on-the-2020-form-w-4\" target=\"_blank\">Learn more on the IRS website</IrsLink>."` */
    includesSpouseExplanation: string
    /** @defaultValue `"We'll use this information to withhold the appropriate federal taxes from each paycheck. If you're unsure what to enter here, refer to <IrsCalculatorLink href=\"https://www.irs.gov/pub/irs-pdf/fw4.pdf\" target=\"_blank\">Form W-4</IrsCalculatorLink> to calculate the values, visit our <HelpCenterLink href=\"https://support.gusto.com\" target=\"_blank\">Help Center</HelpCenterLink>, or consult your tax advisor."` */
    irsCalculator: string
    /** @defaultValue `"Step 2c: Multiple jobs (2c)"` */
    multipleJobs2c: string
    /** @defaultValue `"Step 4a: Other income"` */
    otherIncome: string
    /** @defaultValue `"If you select Exempt from withholding, we won't withhold federal income taxes, but we'll still report taxable wages on a W-2. Keep in mind that anyone who claims exemption from withholding needs to submit a new W-4 each year."` */
    selectWithholdingDescription: string
    /** @defaultValue `"Continue"` */
    submitCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Successfully updated federal tax settings."` */
    successAlert: string
    /** @defaultValue `"No"` */
    twoJobNoLabel: string
    /** @defaultValue `"Yes"` */
    twoJobYesLabel: string
    validations: {
      /** @defaultValue `"Please select filing status"` */
      federalFilingStatus: string
      /** @defaultValue `"Please select an option"` */
      federalTwoJobs: string
    }
  }
  /** Translation keys for the `Employee.FederalTaxesView` i18n namespace. */
  export interface EmployeeFederalTaxesView {
    /** @defaultValue `"Federal tax withholdings (Form W-4)"` */
    federalTaxesTitle: string
    /** @defaultValue `"We'll use this information to withhold the appropriate federal taxes from each paycheck. If you're unsure what to enter here, refer to <IrsCalculatorLink href=\"https://www.irs.gov/pub/irs-pdf/fw4.pdf\" target=\"_blank\">Form W-4</IrsCalculatorLink> to calculate the values, visit our <HelpCenterLink href=\"https://support.gusto.com\" target=\"_blank\">Help Center</HelpCenterLink>, or consult your tax advisor."` */
    irsCalculator: string
    /** @defaultValue `"Step 1c: Federal filing status (1c)"` */
    federalFilingStatus1c: string
    /** @defaultValue `"Select filing status..."` */
    federalFilingStatusPlaceholder: string
    /** @defaultValue `"If you select Exempt from withholding, we won't withhold federal income taxes, but we'll still report taxable wages on a W-2. Keep in mind that anyone who claims exemption from withholding needs to submit a new W-4 each year."` */
    selectWithholdingDescription: string
    /** @defaultValue `"Single"` */
    filingStatusSingle: string
    /** @defaultValue `"Married"` */
    filingStatusMarried: string
    /** @defaultValue `"Head of household"` */
    filingStatusHeadOfHousehold: string
    /** @defaultValue `"Exempt from withholding"` */
    filingStatusExemptFromWithholding: string
    /** @defaultValue `"Step 2c: Multiple jobs (2c)"` */
    multipleJobs2c: string
    /** @defaultValue `"Includes spouse, if applicable. Answering 2c will result in a higher withholding, but to preserve privacy, this can be left unchecked. <IrsLink href=\"https://www.irs.gov/newsroom/faqs-on-the-2020-form-w-4\" target=\"_blank\">Learn more on the IRS website</IrsLink>."` */
    includesSpouseExplanation: string
    /** @defaultValue `"Yes"` */
    twoJobYesLabel: string
    /** @defaultValue `"No"` */
    twoJobNoLabel: string
    /** @defaultValue `"Step 3: Dependents (if applicable)"` */
    dependentsTotalIfApplicable: string
    /** @defaultValue `"Step 4a: Other income"` */
    otherIncome: string
    /** @defaultValue `"Step 4b: Deductions"` */
    deductions: string
    /** @defaultValue `"Step 4c: Extra withholding"` */
    extraWithholding: string
    /** @defaultValue `"This field is a required field. Please enter a value."` */
    fieldIsRequired: string
    validations: {
      /** @defaultValue `"Please select filing status"` */
      federalFilingStatus: string
      /** @defaultValue `"Please select an option"` */
      federalTwoJobs: string
    }
  }
  /** Translation keys for the `Employee.HomeAddress` i18n namespace. */
  export interface EmployeeHomeAddress {
    /** @defaultValue `"Home address"` */
    formTitle: string
    /** @defaultValue `"Employee’s home mailing address, within the United States."` */
    desc: string
    /** @defaultValue `"Street 1"` */
    street1: string
    /** @defaultValue `"Street 2"` */
    street2: string
    /** @defaultValue `"City"` */
    city: string
    /** @defaultValue `"State"` */
    state: string
    /** @defaultValue `"Select state..."` */
    statePlaceholder: string
    /** @defaultValue `"Zip"` */
    zip: string
    /** @defaultValue `"Effective date"` */
    effectiveDate: string
    /** @defaultValue `"Include courtesy withholding"` */
    courtesyWithholdingLabel: string
    /** @defaultValue `"Withhold and pay local income taxes for employees who live and work in different states. "` */
    courtesyWithholdingDescription: string
    /** @defaultValue `"<LearnMoreLink href=\"https://support.gusto.com/article/101365481100000/Reciprocal-agreements-and-courtesy-withholding\" target=\"_blank\">Learn more about courtesy withholdings.</LearnMoreLink>"` */
    learnMoreCta: string
    /** @defaultValue `"Courtesy withholding"` */
    withholdingTitle: string
    /** @defaultValue `"<p>Withholding on an employee's behalf will require your company to register with any corresponding agencies.</p><p>Also, if this employee's home address will change your company's state tax requirements, you may need to complete your company's state tax setup again.</p>"` */
    withholdingNote: string
    /** @defaultValue `"Edit Address"` */
    editAddress: string
    /** @defaultValue `"Cancel"` */
    cancel: string
    /** @defaultValue `"Submit"` */
    submit: string
    validations: {
      /** @defaultValue `"Street address is required"` */
      street1: string
      /** @defaultValue `"Please provide valid city name"` */
      city: string
      /** @defaultValue `"Please select a state"` */
      state: string
      /** @defaultValue `"Please provide valid zip code"` */
      zip: string
    }
  }
  /** Translation keys for the `Employee.I9SignatureForm` i18n namespace. */
  export interface EmployeeI9SignatureForm {
    /** @defaultValue `"Sign I-9 document"` */
    title: string
    /** @defaultValue `"Form I-9 records your authorization to work in the United States. <viewFormLink>View form.</viewFormLink>"` */
    description: string
    /** @defaultValue `"View document"` */
    viewDocumentCta: string
    /** @defaultValue `"Download document"` */
    downloadInstructions: string
    /** @defaultValue `"Signature"` */
    signatureLabel: string
    /** @defaultValue `"Type your full, legal name."` */
    signatureDescription: string
    /** @defaultValue `"Signature is required"` */
    signatureError: string
    /** @defaultValue `"I agree to electronically sign this form and confirm all information entered is correct to the best of my knowledge. I am aware that federal law provides for imprisonment and/or fines for false statements or use of false documents in connection with the completion of this form."` */
    confirmationLabel: string
    /** @defaultValue `"You must agree to sign electronically"` */
    confirmationError: string
    /** @defaultValue `"Did you use a preparer/translator?"` */
    preparerQuestion: string
    /** @defaultValue `"Please indicate whether you used a preparer/translator"` */
    preparerQuestionError: string
    /** @defaultValue `"No, I completed this myself"` */
    preparerNo: string
    /** @defaultValue `"Yes, I used a preparer/translator"` */
    preparerYes: string
    /** @defaultValue `"Your preparer/translator needs to complete the section below."` */
    preparerNote: string
    /** @defaultValue `"Preparer and/or translator certification"` */
    preparerSectionTitle: string
    /** @defaultValue `"First name"` */
    preparerFirstNameLabel: string
    /** @defaultValue `"First name is required"` */
    preparerFirstNameError: string
    /** @defaultValue `"Last name"` */
    preparerLastNameLabel: string
    /** @defaultValue `"Last name is required"` */
    preparerLastNameError: string
    /** @defaultValue `"Street 1"` */
    preparerStreet1Label: string
    /** @defaultValue `"Street address is required"` */
    preparerStreet1Error: string
    /** @defaultValue `"Street 2 (optional)"` */
    preparerStreet2Label: string
    /** @defaultValue `"City"` */
    preparerCityLabel: string
    /** @defaultValue `"City is required"` */
    preparerCityError: string
    /** @defaultValue `"State"` */
    preparerStateLabel: string
    /** @defaultValue `"Select a state..."` */
    preparerStatePlaceholder: string
    /** @defaultValue `"State is required"` */
    preparerStateError: string
    /** @defaultValue `"Zip"` */
    preparerZipLabel: string
    /** @defaultValue `"Zip is required"` */
    preparerZipError: string
    /** @defaultValue `"Signature"` */
    preparerSignatureLabel: string
    /** @defaultValue `"Type your full, legal name."` */
    preparerSignatureDescription: string
    /** @defaultValue `"Signature is required"` */
    preparerSignatureError: string
    /** @defaultValue `"I agree to electronically sign this form and confirm all information entered is correct to the best of my knowledge. I am aware that federal law provides for imprisonment and/or fines for false statements or use of false documents in connection with the completion of this form."` */
    preparerConfirmationLabel: string
    /** @defaultValue `"You must agree to sign electronically"` */
    preparerConfirmationError: string
    /** @defaultValue `"Add preparer"` */
    addPreparerCta: string
    /** @defaultValue `"Remove preparer"` */
    removePreparerCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Sign"` */
    signCta: string
    /** @defaultValue `"You indicated that you are a citizen of the United States. If this is not accurate, make sure you have selected the correct eligibility status."` */
    eligibilityAlertLabel_citizen: string
    /** @defaultValue `"You indicated that you are a lawful permanent resident. If this is not accurate, make sure you have selected the correct eligibility status."` */
    eligibilityAlertLabel_permanent_resident: string
    /** @defaultValue `"You indicated that you are a noncitizen national. If this is not accurate, make sure you have selected the correct eligibility status."` */
    eligibilityAlertLabel_noncitizen: string
    /** @defaultValue `"You indicated that you are a noncitizen authorized to work. If this is not accurate, make sure you have selected the correct eligibility status."` */
    eligibilityAlertLabel_alien: string
    /** @defaultValue `"A citizen is someone who was born or naturalized in the U.S. or born abroad to U.S. citizen parent(s) or who has derived citizenship through a parent's naturalization based on legal requirements."` */
    eligibilityAlertDescription_citizen: string
    /** @defaultValue `"A lawful permanent resident is someone who is not a US citizen and who resides under legally recognized and lawfully recorded permanent residence as an immigrant."` */
    eligibilityAlertDescription_permanent_resident: string
    /** @defaultValue `"A noncitizen national is someone born in American Samoa, certain former citizens of the former Trust Territory of the Pacific Islands, and certain children of noncitizen nationals born abroad."` */
    eligibilityAlertDescription_noncitizen: string
    /** @defaultValue `"A noncitizen authorized to work includes anyone who is authorized to work in the United States but is not a US citizen, US national or lawful permanent resident."` */
    eligibilityAlertDescription_alien: string
    /** @defaultValue `"Change eligibility status"` */
    eligibilityAlertChangeStatusCta: string
  }
  /** Translation keys for the `Employee.Landing` i18n namespace. */
  export interface EmployeeLanding {
    /** @defaultValue `"Hi, {{firstName}}. Welcome to {{companyName}}!"` */
    landingSubtitle: string
    /** @defaultValue `"Let's get you onboarded so you can get paid. This process usually takes about 8 minutes."` */
    landingDescription: string
    /** @defaultValue `"What you'll need:"` */
    stepsSubtitle: string
    steps: {
      /** @defaultValue `"Your personal information (home address, SSN)"` */
      personalInfo: string
      /** @defaultValue `"Tax withholding information (we'll help you figure this one out)"` */
      taxInfo: string
      /** @defaultValue `"Your bank information (account and routing numbers)"` */
      bankInfo: string
    }
    /** @defaultValue `"Let's get started"` */
    getStartedCta: string
  }
  /** Translation keys for the `Employee.Management.Compensation` i18n namespace. */
  export interface EmployeeManagementCompensation {
    /** @defaultValue `"Edit compensation"` */
    editCompensationTitle: string
    /** @defaultValue `"Job title"` */
    jobTitleLabel: string
    /** @defaultValue `"Hire date"` */
    hireDateLabel: string
    /** @defaultValue `"This employee is a 2% shareholder"` */
    twoPercentShareholderLabel: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Add a job"` */
    addJobTitle: string
    /** @defaultValue `"Add another job"` */
    addAnotherJobTitle: string
    /** @defaultValue `"Save job"` */
    saveNewJobCta: string
    /** @defaultValue `"Cancel"` */
    cancelNewJobCta: string
    card: {
      /** @defaultValue `"Compensation"` */
      title: string
      /** @defaultValue `"Edit"` */
      editCta: string
      /** @defaultValue `"Job title"` */
      jobTitle: string
      /** @defaultValue `"Type"` */
      type: string
      types: {
        /** @defaultValue `"Hourly/Overtime eligible"` */
        hourly: string
        /** @defaultValue `"Salary/No overtime"` */
        salary: string
      }
      /** @defaultValue `"Wage"` */
      wage: string
      /** @defaultValue `"Effective date"` */
      effectiveDate: string
      /** @defaultValue `"Add job"` */
      addJobCta: string
      /** @defaultValue `"Add another job"` */
      addAnotherJobCta: string
      /** @defaultValue `"List of jobs"` */
      tableLabel: string
      /** @defaultValue `"Job actions"` */
      hamburgerTitle: string
      /** @defaultValue `"Edit"` */
      editJobCta: string
      /** @defaultValue `"Delete"` */
      deleteJobCta: string
      /** @defaultValue `"No value"` */
      listEmptyPlaceholder: string
      columns: {
        /** @defaultValue `"Job title"` */
        jobTitle: string
        /** @defaultValue `"Pay type"` */
        payType: string
        /** @defaultValue `"Effective date"` */
        effectiveDate: string
        /** @defaultValue `"Status"` */
        status: string
      }
      /** @defaultValue `"Pending"` */
      pendingStatus: string
      deleteJobDialog: {
        /** @defaultValue `"Delete job?"` */
        title: string
        /** @defaultValue `"{{jobTitle}} will be permanently removed."` */
        description: string
        /** @defaultValue `"Delete"` */
        confirmCta: string
        /** @defaultValue `"Cancel"` */
        cancelCta: string
      }
      emptyState: {
        /** @defaultValue `"No compensation"` */
        title: string
        /** @defaultValue `"Compensation will appear here once added"` */
        description: string
      }
      pendingChange: {
        /** @defaultValue `"Compensation will change on {{date}}."` */
        alertLabel: string
        /** @defaultValue `"Compensation for {{jobTitle}} will change on {{date}}."` */
        alertLabelWithJob: string
        /** @defaultValue `"There are multiple pending changes to {{name}}'s compensation."` */
        summaryLabel: string
        /** @defaultValue `"Review"` */
        reviewCta: string
        /** @defaultValue `"Cancel change"` */
        cancelCta: string
        modal: {
          /** @defaultValue `"Review pending changes"` */
          title: string
          /** @defaultValue `"These compensation changes are scheduled to take effect on the dates below. Cancel any change to remove it."` */
          description: string
          /** @defaultValue `"Close"` */
          closeCta: string
        }
        details: {
          /** @defaultValue `"Job title will change to {{title}}"` */
          titleChange: string
          /** @defaultValue `"Pay will change to {{formattedRate}}"` */
          payChange: string
          /** @defaultValue `"Employee type will change to {{flsaLabel}}"` */
          flsaChange: string
          /** @defaultValue `"{{name}} will start an additional job as {{title}} at {{formattedRate}}"` */
          newJob: string
          /** @defaultValue `"{{name}} will start an additional job at {{formattedRate}}"` */
          newJobNoTitle: string
          /** @defaultValue `"{{name}} will start an additional job as {{title}}"` */
          newJobNoRate: string
          /** @defaultValue `"{{name}} will start an additional job"` */
          newJobMinimal: string
          /** @defaultValue `"Minimum wage adjustment will be enabled at {{formattedWage}}"` */
          minWageEnabled: string
          /** @defaultValue `"Minimum wage adjustment will be enabled"` */
          minWageEnabledNoRate: string
          /** @defaultValue `"Minimum wage adjustment will be removed"` */
          minWageDisabled: string
          /** @defaultValue `"Minimum wage adjustment rate will change to {{formattedWage}}"` */
          minWageChanged: string
          /** @defaultValue `"Minimum wage adjustment will change"` */
          minWageChangedNoRate: string
        }
      }
    }
    alerts: {
      /** @defaultValue `"Job successfully added."` */
      jobAdded: string
    }
    /** @defaultValue `"Job Title"` */
    jobTitle: string
    /** @defaultValue `"Start date"` */
    hireDate: string
    /** @defaultValue `"Effective date"` */
    effectiveDate: string
    /** @defaultValue `"Select if employee is a 2% shareholder"` */
    twoPercentStakeholderLabel: string
    /** @defaultValue `"Employee type"` */
    employeeClassification: string
    /** @defaultValue `"Select classification..."` */
    flsaStatusPlaceholder: string
    /** @defaultValue `"Select wage frequency..."` */
    paymentUnitPlaceholder: string
    /** @defaultValue `"Select minimum wage..."` */
    minimumWagePlaceholder: string
    /** @defaultValue `"<ClassificationLink href=\"https://support.gusto.com/team-management/team-payments/pay-rates/1001671771/Employee-classification-options.htm\" target=\"_blank\">Learn more about employee classifications.</ClassificationLink>"` */
    classificationLink: string
    /** @defaultValue `"Wage"` */
    wageLabel: string
    /** @defaultValue `"Wage frequency"` */
    wageFrequencyLabel: string
    /** @defaultValue `"The period over which the compensation amount is tracked (e.g., hourly, daily, weekly, monthly, annually)."` */
    paymentUnitDescription: string
    /** @defaultValue `"Effective date"` */
    effectiveDateLabel: string
    /** @defaultValue `"Adjust for minimum wage"` */
    adjustForMinimumWage: string
    /** @defaultValue `"Determines whether the compensation should be adjusted for minimum wage. Only applies to Nonexempt employees."` */
    adjustForMinimumWageDescription: string
    /** @defaultValue `"Minimum wage"` */
    minimumWageLabel: string
    /** @defaultValue `"What minimum wage requirement should compensation be adjusted to"` */
    minimumWageDescription: string
    /** @defaultValue `"Workers' compensation coverage"` */
    stateWcCoveredLabel: string
    /** @defaultValue `"Washington administers <wcLink>workers’ compensation insurance</wcLink> to protect workers and employers from the financial impact of a work-related injury. Indicate here if this employee is exempt from the workers’ comp tax."` */
    stateWcCoveredDescription: string
    stateWcCoveredOptions: {
      /** @defaultValue `"Yes, this employee is covered"` */
      yes: string
      /** @defaultValue `"No, this employee is not covered"` */
      no: string
    }
    /** @defaultValue `"Risk class code"` */
    stateWcClassCodeLabel: string
    /** @defaultValue `"The risk class code associated with this employee’s job function. We need this to pay and file your taxes correctly."` */
    stateWcClassCodeDescription: string
    flsaStatusLabels: {
      /** @defaultValue `"Commission Only/No Overtime"` */
      'Commission Only Exempt': string
      /** @defaultValue `"Commission Only/Eligible for overtime"` */
      'Commission Only Nonexempt': string
      /** @defaultValue `"Salary/No overtime"` */
      Exempt: string
      /** @defaultValue `"Paid by the hour"` */
      Nonexempt: string
      /** @defaultValue `"Owner's draw"` */
      Owner: string
      /** @defaultValue `"Salary/Eligible for overtime"` */
      'Salaried Nonexempt': string
    }
    paymentUnitOptions: {
      /** @defaultValue `"Hour"` */
      Hour: string
      /** @defaultValue `"Month"` */
      Month: string
      /** @defaultValue `"Paycheck"` */
      Paycheck: string
      /** @defaultValue `"Week"` */
      Week: string
      /** @defaultValue `"Year"` */
      Year: string
    }
    commissionAlerts: {
      federalMinimumPay: {
        /** @defaultValue `"Commission-only employees must earn the federal minimum pay"` */
        label: string
        /** @defaultValue `"Federal laws say that employees not eligible for overtime should be paid at least $684 per week ($35,568 per year). You’ve classified this employee as not eligible for overtime—make sure they meet the Department of Labor’s definition of an exempt employee."` */
        body: string
      }
      minimumWage: {
        /** @defaultValue `"Commission-only employees must earn at least the minimum wage"` */
        label: string
        /** @defaultValue `"To stay compliant, <minimumWageLink>check your local regulations</minimumWageLink>."` */
        body: string
      }
      ownerSalary: {
        /** @defaultValue `"The IRS requires owners of S corps to pay themselves a reasonable salary similar to others in the same role, before taking any distributions."` */
        label: string
      }
    }
    validations: {
      /** @defaultValue `"Job title is a required field"` */
      jobTitleSentence: string
      /** @defaultValue `"Title is a required field"` */
      title: string
      /** @defaultValue `"Start date is required"` */
      hireDate: string
      /** @defaultValue `"Most employees who make under {{limit}}/year should be eligible for overtime."` */
      exemptThreshold: string
      /** @defaultValue `"Changing this employee's classification will immediately delete their additional jobs."` */
      classificationChangeNotification: string
      /** @defaultValue `"Only employees that are paid by the hour can have multiple jobs."` */
      classificationChangeRemovesSecondaryJobs: string
      /** @defaultValue `"Amount is a required field"` */
      rate: string
      /** @defaultValue `"Amount must be at least $1.00"` */
      nonZeroRate: string
      /** @defaultValue `"FLSA Exempt employees must meet salary threshold of {{limit}}/year"` */
      rateExemptThreshold: string
      /** @defaultValue `"Payment unit must be one of Hour, Week, Month, or Year"` */
      paymentUnit: string
      /** @defaultValue `"Please select minimum wage for adjustment"` */
      minimumWage: string
      /** @defaultValue `"Please select a risk class code"` */
      stateWcClassCode: string
      /** @defaultValue `"Effective date is a required field"` */
      effectiveDate: string
      /** @defaultValue `"Effective date cannot be before the employee's hire date."` */
      effectiveDateBeforeHire: string
      /** @defaultValue `"Effective date must be in the future"` */
      effectiveDateBeforeMin: string
    }
  }
  /** Translation keys for the `Employee.Management.Deductions` i18n namespace. */
  export interface EmployeeManagementDeductions {
    /** @defaultValue `"Deductions"` */
    title: string
    /** @defaultValue `"Add deduction"` */
    addDeductionCta: string
    /** @defaultValue `"List of deductions"` */
    listLabel: string
    columns: {
      /** @defaultValue `"Deduction"` */
      deduction: string
      /** @defaultValue `"Frequency"` */
      frequency: string
      /** @defaultValue `"Withheld"` */
      withhold: string
    }
    /** @defaultValue `"Recurring"` */
    recurring: string
    /** @defaultValue `"One-time"` */
    oneTime: string
    /** @defaultValue `"{{value}} per paycheck"` */
    amountPerPaycheck: string
    emptyState: {
      /** @defaultValue `"No deductions"` */
      title: string
      /** @defaultValue `"Employee deductions will appear here"` */
      description: string
    }
    /** @defaultValue `"Deduction actions menu"` */
    hamburgerTitle: string
    /** @defaultValue `"Edit deduction"` */
    editCta: string
    /** @defaultValue `"Delete deduction"` */
    deleteCta: string
    deleteDeductionDialog: {
      /** @defaultValue `"Delete this deduction?"` */
      title: string
      /** @defaultValue `"{{deduction}} will no longer be deducted from this employee's paycheck."` */
      description: string
      /** @defaultValue `"Delete"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    alerts: {
      /** @defaultValue `"Deduction successfully added."` */
      deductionAdded: string
      /** @defaultValue `"Deduction successfully updated."` */
      deductionUpdated: string
      /** @defaultValue `"Deduction successfully deleted."` */
      deductionDeleted: string
    }
    form: {
      /** @defaultValue `"Add Deduction"` */
      addTitle: string
      /** @defaultValue `"Edit Deduction"` */
      editTitle: string
      /** @defaultValue `"Amounts are withheld from the net pay of an employee and reported on the paystub and within payroll receipts. Note that these deductions are always post-tax and will start with the next pay period."` */
      description: string
      /** @defaultValue `"Deduction type"` */
      variantLabel: string
      /** @defaultValue `"A garnishment is a court-ordered wage reduction—we’ll handle the tax calculations based on what you select. For other post-tax deductions, choose Custom Deduction."` */
      variantDescription: string
      /** @defaultValue `"Garnishment (a court-ordered deduction)"` */
      garnishmentOption: string
      /** @defaultValue `"Custom deduction (post-tax)"` */
      customOption: string
      /** @defaultValue `"Garnishment type"` */
      garnishmentTypeLabel: string
      types: {
        /** @defaultValue `"Child Support"` */
        childSupport: string
        /** @defaultValue `"Federal Tax Lien"` */
        federalTaxLien: string
        /** @defaultValue `"State Tax Lien"` */
        stateTaxLien: string
        /** @defaultValue `"Student Loan"` */
        studentLoan: string
        /** @defaultValue `"Creditor Garnishment"` */
        creditorGarnishment: string
        /** @defaultValue `"Federal Loan"` */
        federalLoan: string
        /** @defaultValue `"Other Garnishment"` */
        otherGarnishment: string
        /** @defaultValue `"Custom deduction"` */
        custom: string
      }
      standard: {
        /** @defaultValue `"Description"` */
        descriptionLabel: string
        /** @defaultValue `"Description is required"` */
        descriptionRequired: string
        /** @defaultValue `"Deduction frequency"` */
        frequencyLabel: string
        /** @defaultValue `"Recurring (every payroll)"` */
        frequencyRecurring: string
        /** @defaultValue `"One-time (next payroll only)"` */
        frequencyOneTime: string
        /** @defaultValue `"Frequency is required"` */
        frequencyRequired: string
        /** @defaultValue `"Percentage or fixed"` */
        typeLabel: string
        /** @defaultValue `"Percentage of pay"` */
        typePercentage: string
        /** @defaultValue `"Fixed dollar amount"` */
        typeFixed: string
        /** @defaultValue `"Deduction type is required"` */
        typeRequired: string
        /** @defaultValue `"Amount to withhold"` */
        amountLabel: string
        /** @defaultValue `"Enter the percentage of the employee’s wages to withhold."` */
        amountPercentDescription: string
        /** @defaultValue `"Enter the amount of money to withhold each pay period from the employee’s wages."` */
        amountFixedDescription: string
        /** @defaultValue `"Amount is required"` */
        amountRequired: string
        /** @defaultValue `"Amount must be 0 or greater"` */
        amountNonNegative: string
        /** @defaultValue `"Total amount owed"` */
        totalAmountLabel: string
        /** @defaultValue `"We will adjust the amount of the last payment and stop collecting once the total amount is reached."` */
        totalAmountDescription: string
        /** @defaultValue `"Annual maximum"` */
        annualMaxLabel: string
        /** @defaultValue `"The maximum annual amount you deduct from the employee's pay for this specific deduction. Leave this field blank if there is no maximum."` */
        annualMaxDescription: string
      }
      childSupport: {
        /** @defaultValue `"Agency"` */
        agencyLabel: string
        /** @defaultValue `"Select the appropriate state agency."` */
        agencyDescription: string
        /** @defaultValue `"Agency is required"` */
        agencyRequired: string
        /** @defaultValue `"This agency doesn't support electronic payments. You are responsible for paying the agency yourself."` */
        manualPaymentRequired: string
        /** @defaultValue `"County"` */
        countyLabel: string
        /** @defaultValue `"Select the appropriate state county."` */
        countyDescription: string
        /** @defaultValue `"All counties"` */
        allCounties: string
        /** @defaultValue `"County is required"` */
        countyRequired: string
        /** @defaultValue `"Carefully enter the Child Support Enforcement Case Number."` */
        caseNumberDescription: string
        /** @defaultValue `"Case number is required"` */
        caseNumberRequired: string
        /** @defaultValue `"Enter the unique Order Identifier or Order ID associated with this child support obligation."` */
        orderNumberDescription: string
        /** @defaultValue `"Order number is required"` */
        orderNumberRequired: string
        /** @defaultValue `"Carefully enter the Child Support Enforcement Remittance ID."` */
        remittanceNumberDescription: string
        /** @defaultValue `"Remittance number is required"` */
        remittanceNumberRequired: string
        /** @defaultValue `"Total amount withheld"` */
        totalAmountWithheld: string
        /** @defaultValue `"Enter the amount indicated in the letter from the child support agency."` */
        totalAmountWithheldDescription: string
        /** @defaultValue `"Pay period maximum is required"` */
        payPeriodMaximumRequired: string
        /** @defaultValue `"Max paycheck percentage"` */
        maxPaycheckPercentage: string
        /** @defaultValue `"Enter the maximum percentage of the employee's paycheck that we should withhold. You can find this info in the letter from the child support agency."` */
        maxPaycheckPercentageDescription: string
        /** @defaultValue `"Amount is required"` */
        amountRequired: string
        /** @defaultValue `"Amount must be 0 or greater"` */
        amountNonNegative: string
        /** @defaultValue `"Must be between 0 and 100"` */
        percentOutOfRange: string
        /** @defaultValue `"Per"` */
        paymentPeriodLabel: string
        /** @defaultValue `"Enter how often the agency collects the withholding amount."` */
        paymentPeriodDescription: string
        /** @defaultValue `"Payment period is required"` */
        paymentPeriodRequired: string
        paymentPeriod: {
          /** @defaultValue `"Every week"` */
          everyWeek: string
          /** @defaultValue `"Every other week"` */
          everyOtherWeek: string
          /** @defaultValue `"Twice per month"` */
          twicePerMonth: string
          /** @defaultValue `"Monthly"` */
          monthly: string
        }
      }
      actions: {
        /** @defaultValue `"Save"` */
        save: string
        /** @defaultValue `"Cancel"` */
        cancel: string
      }
    }
  }
  /** Translation keys for the `Employee.Management.Documents` i18n namespace. */
  export interface EmployeeManagementDocuments {
    /** @defaultValue `"Forms"` */
    title: string
    /** @defaultValue `"List of employee forms"` */
    listLabel: string
    /** @defaultValue `"View"` */
    viewCta: string
    columns: {
      /** @defaultValue `"Form"` */
      title: string
      /** @defaultValue `"Year"` */
      year: string
      /** @defaultValue `"Status"` */
      status: string
      /** @defaultValue `"Signing status"` */
      requiresSigning: string
      /** @defaultValue `"Actions"` */
      actions: string
    }
    signingStatus: {
      /** @defaultValue `"Signed"` */
      signed: string
      /** @defaultValue `"Not signed"` */
      notSigned: string
    }
    status: {
      /** @defaultValue `"Draft"` */
      draft: string
      /** @defaultValue `"Final"` */
      final: string
    }
    emptyState: {
      /** @defaultValue `"No forms"` */
      title: string
      /** @defaultValue `"Employee forms will appear here once available"` */
      description: string
    }
  }
  /** Translation keys for the `Employee.Management.FederalTaxes` i18n namespace. */
  export interface EmployeeManagementFederalTaxes {
    card: {
      /** @defaultValue `"Federal taxes"` */
      title: string
      /** @defaultValue `"Edit"` */
      editCta: string
      /** @defaultValue `"Filing status"` */
      filingStatus: string
      /** @defaultValue `"Multiple jobs"` */
      multipleJobs: string
      /** @defaultValue `"Dependents and other credits"` */
      dependentsAndOtherCredits: string
      /** @defaultValue `"Other income"` */
      otherIncome: string
      /** @defaultValue `"Deductions"` */
      deductions: string
      /** @defaultValue `"Extra withholding"` */
      extraWithholding: string
      /** @defaultValue `"No value"` */
      listEmptyPlaceholder: string
    }
    common: {
      /** @defaultValue `"Yes"` */
      yes: string
      /** @defaultValue `"No"` */
      no: string
    }
    /** @defaultValue `"Federal tax withholdings (Form W-4)"` */
    federalTaxesTitle: string
    /** @defaultValue `"We'll use this information to withhold the appropriate federal taxes from each paycheck. If you're unsure what to enter here, refer to <IrsCalculatorLink href=\"https://www.irs.gov/pub/irs-pdf/fw4.pdf\" target=\"_blank\">Form W-4</IrsCalculatorLink> to calculate the values, visit our <HelpCenterLink href=\"https://support.gusto.com\" target=\"_blank\">Help Center</HelpCenterLink>, or consult your tax advisor."` */
    irsCalculator: string
    /** @defaultValue `"Step 1c: Federal filing status (1c)"` */
    federalFilingStatus1c: string
    /** @defaultValue `"Select filing status..."` */
    federalFilingStatusPlaceholder: string
    /** @defaultValue `"If you select Exempt from withholding, we won't withhold federal income taxes, but we'll still report taxable wages on a W-2. Keep in mind that anyone who claims exemption from withholding needs to submit a new W-4 each year."` */
    selectWithholdingDescription: string
    /** @defaultValue `"Single"` */
    filingStatusSingle: string
    /** @defaultValue `"Married"` */
    filingStatusMarried: string
    /** @defaultValue `"Head of household"` */
    filingStatusHeadOfHousehold: string
    /** @defaultValue `"Exempt from withholding"` */
    filingStatusExemptFromWithholding: string
    /** @defaultValue `"Step 2c: Multiple jobs (2c)"` */
    multipleJobs2c: string
    /** @defaultValue `"Includes spouse, if applicable. Answering 2c will result in a higher withholding, but to preserve privacy, this can be left unchecked. <IrsLink href=\"https://www.irs.gov/newsroom/faqs-on-the-2020-form-w-4\" target=\"_blank\">Learn more on the IRS website</IrsLink>."` */
    includesSpouseExplanation: string
    /** @defaultValue `"Yes"` */
    twoJobYesLabel: string
    /** @defaultValue `"No"` */
    twoJobNoLabel: string
    /** @defaultValue `"Step 3: Dependents (if applicable)"` */
    dependentsTotalIfApplicable: string
    /** @defaultValue `"Step 4a: Other income"` */
    otherIncome: string
    /** @defaultValue `"Step 4b: Deductions"` */
    deductions: string
    /** @defaultValue `"Step 4c: Extra withholding"` */
    extraWithholding: string
    /** @defaultValue `"This field is a required field. Please enter a value."` */
    fieldIsRequired: string
    validations: {
      /** @defaultValue `"Please select filing status"` */
      federalFilingStatus: string
      /** @defaultValue `"Please select an option"` */
      federalTwoJobs: string
    }
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    alerts: {
      /** @defaultValue `"Federal tax settings successfully updated."` */
      federalTaxesUpdated: string
    }
  }
  /** Translation keys for the `Employee.Management.HomeAddress` i18n namespace. */
  export interface EmployeeManagementHomeAddress {
    card: {
      /** @defaultValue `"Home address"` */
      title: string
      /** @defaultValue `"Manage"` */
      manageCta: string
      /** @defaultValue `"No home address on file"` */
      noAddress: string
    }
    /** @defaultValue `"Manage home address"` */
    title: string
    /** @defaultValue `"An employee's home address is used to calculate their taxes and determine eligibility for certain benefits. Make sure to keep it up-to-date."` */
    description: string
    /** @defaultValue `"Open address row actions"` */
    rowMenuAriaLabel: string
    /** @defaultValue `"Edit"` */
    rowEdit: string
    /** @defaultValue `"Delete"` */
    rowDelete: string
    /** @defaultValue `"Current home address"` */
    currentSectionTitle: string
    /** @defaultValue `"Since {{date}}"` */
    currentSince: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Change address"` */
    changeCta: string
    /** @defaultValue `"Change pending"` */
    changePendingTitle: string
    /** @defaultValue `"This employee's"` */
    changePendingPossessiveFallback: string
    /** @defaultValue `"{{possessiveLabel}} home address will change to {{newAddress}} on {{effectiveDate}}."` */
    changePendingDescription: string
    /** @defaultValue `"Home address history"` */
    historySectionTitle: string
    /** @defaultValue `"No address history yet"` */
    historyEmptyTitle: string
    /** @defaultValue `"Once this employee has had more than one home address, their previous addresses will appear in this list."` */
    historyEmptyDescription: string
    columns: {
      /** @defaultValue `"Address"` */
      address: string
      /** @defaultValue `"Start date"` */
      startDate: string
      /** @defaultValue `"End date"` */
      endDate: string
    }
    /** @defaultValue `"Edit home address"` */
    editModalTitle: string
    /** @defaultValue `"Update this employee's current home address. This information is used for payroll, benefits, and HR, so please make sure it's accurate."` */
    editModalDescription: string
    /** @defaultValue `"Add a new home address"` */
    createModalTitle: string
    /** @defaultValue `"Enter the new address and when it takes effect. This information is used for payroll, benefits, and HR, so please make sure it's accurate."` */
    createModalDescription: string
    /** @defaultValue `"The date the employee started living at this address."` */
    startDateHelper: string
    /** @defaultValue `"Save"` */
    submitCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"We couldn't save this address"` */
    submitErrorAlertTitle: string
    /** @defaultValue `"The address couldn't be verified."` */
    submitErrorAlertFallback: string
    /** @defaultValue `"Double-check the street, city, state, and ZIP. If they look right, the address may not be recognized by USPS — try a nearby valid address or contact your administrator."` */
    submitErrorAlertHelp: string
    /** @defaultValue `"Delete address?"` */
    deleteModalTitle: string
    /** @defaultValue `"Deleting an address can't be undone. <strong>{{address}}</strong> will be deleted. This can have implications on your tax calculations & withholdings."` */
    deleteModalDescription: string
    /** @defaultValue `"Delete address"` */
    deleteModalConfirmCta: string
    form: {
      /** @defaultValue `"Street 1"` */
      street1: string
      /** @defaultValue `"Street 2"` */
      street2: string
      /** @defaultValue `"City"` */
      city: string
      /** @defaultValue `"State"` */
      state: string
      /** @defaultValue `"Select state..."` */
      statePlaceholder: string
      /** @defaultValue `"Zip"` */
      zip: string
      /** @defaultValue `"Home address"` */
      noCurrentAddress: string
      /** @defaultValue `"Include courtesy withholding"` */
      courtesyWithholdingLabel: string
      /** @defaultValue `"Withhold and pay local income taxes for employees who live and work in different states. "` */
      courtesyWithholdingDescription: string
      /** @defaultValue `"<LearnMoreLink href=\"https://support.gusto.com/article/101365481100000/Reciprocal-agreements-and-courtesy-withholding\" target=\"_blank\">Learn more about courtesy withholdings.</LearnMoreLink>"` */
      learnMoreCta: string
      /** @defaultValue `"Courtesy withholding"` */
      withholdingTitle: string
      /** @defaultValue `"<p>Withholding on an employee's behalf will require your company to register with any corresponding agencies.</p><p>Also, if this employee's home address will change your company's state tax requirements, you may need to complete your company's state tax setup again.</p>"` */
      withholdingNote: string
      /** @defaultValue `"Start date is required"` */
      startDateRequired: string
      validations: {
        /** @defaultValue `"Street address is required"` */
        street1: string
        /** @defaultValue `"Please provide valid city name"` */
        city: string
        /** @defaultValue `"Please select a state"` */
        state: string
        /** @defaultValue `"Please provide valid zip code"` */
        zip: string
      }
    }
  }
  /** Translation keys for the `Employee.Management.PaymentMethod` i18n namespace. */
  export interface EmployeeManagementPaymentMethod {
    /** @defaultValue `"Payment"` */
    title: string
    /** @defaultValue `"Split paycheck"` */
    splitCta: string
    /** @defaultValue `"Add bank account"` */
    addBankAccountCta: string
    /** @defaultValue `"Add another bank account"` */
    addAnotherCta: string
    /** @defaultValue `"List of employee bank accounts"` */
    bankAccountsListLabel: string
    /** @defaultValue `"Nickname"` */
    nicknameColumn: string
    /** @defaultValue `"Routing number"` */
    routingNumberColumn: string
    /** @defaultValue `"Account type"` */
    accountTypeColumn: string
    /** @defaultValue `"Delete"` */
    deleteBankAccountCta: string
    deleteBankAccountDialog: {
      /** @defaultValue `"Delete bank account"` */
      title: string
      /** @defaultValue `"Are you sure you want to delete the bank account {{account}}?"` */
      description: string
      /** @defaultValue `"Delete"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    /** @defaultValue `"Bank account actions"` */
    hamburgerTitle: string
    /** @defaultValue `"Payment method"` */
    paymentMethodLabel: string
    /** @defaultValue `"Direct Deposit"` */
    directDepositLabel: string
    /** @defaultValue `"Check"` */
    checkLabel: string
    alerts: {
      /** @defaultValue `"Bank account successfully added."` */
      bankAccountAdded: string
      /** @defaultValue `"Bank account successfully deleted."` */
      bankAccountDeleted: string
      /** @defaultValue `"Split payment successfully updated."` */
      splitUpdated: string
    }
  }
  /** Translation keys for the `Employee.Management.PaymentMethodBankForm` i18n namespace. */
  export interface EmployeeManagementPaymentMethodBankForm {
    /** @defaultValue `"Add bank account"` */
    title: string
    /** @defaultValue `"Account nickname"` */
    nameLabel: string
    /** @defaultValue `"Routing number"` */
    routingNumberLabel: string
    /** @defaultValue `"(9 digits)"` */
    routingNumberDescription: string
    /** @defaultValue `"Account number"` */
    accountNumberLabel: string
    /** @defaultValue `"Account type"` */
    accountTypeLabel: string
    /** @defaultValue `"Checking"` */
    accountTypeChecking: string
    /** @defaultValue `"Savings"` */
    accountTypeSavings: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    validations: {
      /** @defaultValue `"Account name is required"` */
      accountName: string
      /** @defaultValue `"Routing number should be a number (9 digits)"` */
      routingNumber: string
      /** @defaultValue `"Account number is a required field"` */
      accountNumber: string
      /** @defaultValue `"Account number should contain only digits (up to 17)"` */
      accountNumberFormat: string
    }
  }
  /** Translation keys for the `Employee.Management.PaymentMethodSplitForm` i18n namespace. */
  export interface EmployeeManagementPaymentMethodSplitForm {
    /** @defaultValue `"Split employee paycheck"` */
    title: string
    /** @defaultValue `"You can split your paycheck into different accounts by percentage or flat dollar amounts. When splitting by amount, you can reorder the priority of the bank accounts by clicking and dragging the list icon to the left of the bank account field. We’ll pay the amounts in the order specified until your salary has been fully distributed to the last “remainder” account."` */
    splitDescription: string
    /** @defaultValue `"Split by"` */
    splitByLabel: string
    /** @defaultValue `"Percentage"` */
    percentageLabel: string
    /** @defaultValue `"Fixed amount"` */
    amountLabel: string
    /** @defaultValue `"{{name}} ({{account_number}})"` */
    splitAmountLabel: string
    /** @defaultValue `"Reorderable list of bank accounts"` */
    draggableListLabel: string
    /** @defaultValue `"Remainder"` */
    remainderLabel: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    validations: {
      /** @defaultValue `"Splits must total 100%. Currently {{total}}%."` */
      percentageErrorWithTotal: string
      /** @defaultValue `"Please enter valid amount"` */
      amountError: string
      /** @defaultValue `"Percentage should be a whole number between 0 and 100"` */
      percentageAmountError: string
    }
  }
  /** Translation keys for the `Employee.Management.Paystubs` i18n namespace. */
  export interface EmployeeManagementPaystubs {
    /** @defaultValue `"Paystubs"` */
    title: string
    /** @defaultValue `"List of paystubs"` */
    listLabel: string
    /** @defaultValue `"Payday"` */
    payday: string
    /** @defaultValue `"Check amount"` */
    checkAmount: string
    /** @defaultValue `"Gross pay"` */
    grossPay: string
    /** @defaultValue `"Payment method"` */
    paymentMethod: string
    /** @defaultValue `"Not available"` */
    noPaymentMethod: string
    /** @defaultValue `"Download paystub"` */
    downloadCta: string
    /** @defaultValue `"Unable to download paystub"` */
    downloadError: string
    /** @defaultValue `"Generating paystub…"` */
    downloadLoadingMessage: string
    /** @defaultValue `"No value"` */
    listEmptyPlaceholder: string
    emptyState: {
      /** @defaultValue `"No paystubs"` */
      title: string
      /** @defaultValue `"Paystubs will appear here after payroll is run"` */
      description: string
    }
  }
  /** Translation keys for the `Employee.Management.Profile` i18n namespace. */
  export interface EmployeeManagementProfile {
    /** @defaultValue `"Basic details"` */
    title: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Legal name"` */
    legalName: string
    /** @defaultValue `"Start date"` */
    startDate: string
    /** @defaultValue `"Social security number"` */
    socialSecurityNumber: string
    /** @defaultValue `"Date of birth"` */
    dateOfBirth: string
    /** @defaultValue `"Personal email"` */
    personalEmail: string
    /** @defaultValue `"No value"` */
    listEmptyPlaceholder: string
    alerts: {
      /** @defaultValue `"Profile updated"` */
      profileUpdated: string
    }
    form: {
      /** @defaultValue `"Basics"` */
      title: string
      /** @defaultValue `"Legal first name"` */
      firstName: string
      /** @defaultValue `"Middle initial"` */
      middleInitial: string
      /** @defaultValue `"Legal last name"` */
      lastName: string
      /** @defaultValue `"Personal email"` */
      email: string
      /** @defaultValue `"Use an email that's not associated with your company."` */
      emailDescription: string
      /** @defaultValue `"Social Security Number (9 digit)"` */
      ssnLabel: string
      /** @defaultValue `"Date of birth"` */
      dobLabel: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
      /** @defaultValue `"Save"` */
      saveCta: string
      /** @defaultValue `"Successfully updated profile"` */
      successAlert: string
      validations: {
        /** @defaultValue `"Valid email is required"` */
        email: string
        /** @defaultValue `"Please enter valid first name"` */
        firstName: string
        /** @defaultValue `"Please enter valid last name"` */
        lastName: string
      }
    }
  }
  /** Translation keys for the `Employee.Management.StateTaxes` i18n namespace. */
  export interface EmployeeManagementStateTaxes {
    card: {
      /** @defaultValue `"State taxes"` */
      title: string
      /** @defaultValue `"Edit"` */
      editCta: string
      /** @defaultValue `"No state taxes on file"` */
      noStateTaxes: string
      /** @defaultValue `"No state income tax withholding required."` */
      noWithholdingForState: string
      /** @defaultValue `"No value"` */
      listEmptyPlaceholder: string
      /** @defaultValue `"Yes"` */
      yes: string
      /** @defaultValue `"No"` */
      no: string
    }
    /** @defaultValue `"{{state}} Tax Requirements"` */
    stateTaxesTitle: string
    /** @defaultValue `"No state income tax withholding is required for this employee's work state."` */
    noWithholding: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    alerts: {
      /** @defaultValue `"Successfully updated state tax settings."` */
      stateTaxesUpdated: string
    }
    validations: {
      /** @defaultValue `"This field is a required field. Please enter a value."` */
      required: string
    }
  }
  /** Translation keys for the `Employee.Management.WorkAddress` i18n namespace. */
  export interface EmployeeManagementWorkAddress {
    /** @defaultValue `"Work address"` */
    cardTitle: string
    /** @defaultValue `"Manage"` */
    cardManageCta: string
    /** @defaultValue `"No work address on file"` */
    cardNoAddress: string
    /** @defaultValue `"Work address"` */
    title: string
    /** @defaultValue `"An employee's work address is the primary location where they perform their job. Keep it accurate for payroll and tax purposes."` */
    description: string
    /** @defaultValue `"Open work address row actions"` */
    rowMenuAriaLabel: string
    /** @defaultValue `"Edit"` */
    rowEdit: string
    /** @defaultValue `"Delete"` */
    rowDelete: string
    /** @defaultValue `"Current work address"` */
    currentSectionTitle: string
    /** @defaultValue `"Since {{date}}"` */
    currentSince: string
    /** @defaultValue `"No work address on file."` */
    currentEmpty: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Change address"` */
    changeCta: string
    /** @defaultValue `"Change pending"` */
    changePendingTitle: string
    /** @defaultValue `"This employee's"` */
    changePendingPossessiveFallback: string
    /** @defaultValue `"{{possessiveLabel}} work address will change to {{newAddress}} on {{effectiveDate}}."` */
    changePendingDescription: string
    /** @defaultValue `"Work address history"` */
    historySectionTitle: string
    /** @defaultValue `"No address history yet"` */
    historyEmptyTitle: string
    /** @defaultValue `"When this employee has more than one work address on record, previous locations will appear here."` */
    historyEmptyDescription: string
    columns: {
      /** @defaultValue `"Location"` */
      location: string
      /** @defaultValue `"Start date"` */
      startDate: string
      /** @defaultValue `"End date"` */
      endDate: string
    }
    /** @defaultValue `"Review or update the work location on file. This information is used for payroll, benefits, and HR, so keep it accurate."` */
    editModalDescription: string
    /** @defaultValue `"This information is used for payroll, benefits, and HR, so please make sure it's accurate."` */
    changeModalDescription: string
    /** @defaultValue `"Editing a work address can have implications on your tax calculations & withholdings."` */
    editPastAddressAlertTitle: string
    /** @defaultValue `"Edit work address"` */
    editModalTitle: string
    /** @defaultValue `"Change work address"` */
    changeModalTitle: string
    form: {
      /** @defaultValue `"Work address"` */
      editLocationLabel: string
      /** @defaultValue `"Corrected work address"` */
      editInactiveLocationLabel: string
      /** @defaultValue `"If you don't see the address in the dropdown, you need to add a new company location first."` */
      editLocationDescription: string
      /** @defaultValue `"New work address"` */
      newWorkAddressLabel: string
      /** @defaultValue `"If you don't see the address in the dropdown, you need to add a new company location first."` */
      newWorkAddressDescription: string
      /** @defaultValue `"Select a work address..."` */
      selectPlaceholder: string
      /** @defaultValue `"Select a work location"` */
      locationRequired: string
      /** @defaultValue `"Start date"` */
      startDateLabel: string
      /** @defaultValue `"When did the employee start working at this address"` */
      startDateDescription: string
      /** @defaultValue `"The date this work address took effect for this employee."` */
      editInactiveStartDateDescription: string
      /** @defaultValue `"Start date is required"` */
      startDateRequired: string
    }
    /** @defaultValue `"Submit"` */
    submitCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Delete address?"` */
    deleteModalTitle: string
    /** @defaultValue `"Deleting an address can't be undone. <strong>{{address}}</strong> will be removed from history."` */
    deleteModalDescription: string
    /** @defaultValue `"Delete address"` */
    deleteModalConfirmCta: string
  }
  /** Translation keys for the `Employee.ManagementEmployeeList` i18n namespace. */
  export interface EmployeeManagementEmployeeList {
    /** @defaultValue `"Employees"` */
    title: string
    /** @defaultValue `"Add employee"` */
    addEmployeeCta: string
    /** @defaultValue `"Back to employees"` */
    backToListCta: string
    /** @defaultValue `"Employee status tabs"` */
    tabsLabel: string
    tabs: {
      /** @defaultValue `"Active"` */
      active: string
      /** @defaultValue `"Onboarding"` */
      onboarding: string
      /** @defaultValue `"Dismissed"` */
      dismissed: string
    }
    /** @defaultValue `"Employee name"` */
    nameLabel: string
    /** @defaultValue `"Job title"` */
    jobTitleLabel: string
    /** @defaultValue `"Start date"` */
    startDateLabel: string
    /** @defaultValue `"Status"` */
    statusLabel: string
    /** @defaultValue `"Last day"` */
    lastDayLabel: string
    /** @defaultValue `"Edit employee"` */
    editCta: string
    /** @defaultValue `"Dismiss employee"` */
    dismissCta: string
    /** @defaultValue `"Cancel onboarding"` */
    cancelCta: string
    /** @defaultValue `"Rehire employee"` */
    rehireCta: string
    /** @defaultValue `"Employee actions menu"` */
    hamburgerTitle: string
    /** @defaultValue `"List of employees"` */
    employeeListLabel: string
    emptyState: {
      active: {
        /** @defaultValue `"There are no active employees"` */
        title: string
        /** @defaultValue `"Employees who have completed onboarding will appear here"` */
        description: string
      }
      onboarding: {
        /** @defaultValue `"There are no employees onboarding"` */
        title: string
        /** @defaultValue `"Employees currently being onboarded will appear here"` */
        description: string
      }
      dismissed: {
        /** @defaultValue `"There are no dismissed employees"` */
        title: string
        /** @defaultValue `"Terminated employees will appear here"` */
        description: string
      }
    }
    deleteDialog: {
      /** @defaultValue `"Cancel employee onboarding?"` */
      title: string
      /** @defaultValue `"This will permanently delete this employee from your account. This action cannot be undone."` */
      description: string
      /** @defaultValue `"Delete employee"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
  }
  /** Translation keys for the `Employee.OnboardingSummary` i18n namespace. */
  export interface EmployeeOnboardingSummary {
    /** @defaultValue `"Missing Requirements"` */
    subTitle: string
    /** @defaultValue `"Please complete the following steps in order to continue."` */
    description: string
    /** @defaultValue `"That's it! {{name}} is ready to get paid!"` */
    onboardedAdminSubtitle: string
    /** @defaultValue `"We'll begin withholding and reporting their taxes."` */
    onboardedAdminDescription: string
    /** @defaultValue `"{{name}}'s invite is on its way"` */
    handedOffAdminSubtitle: string
    /** @defaultValue `"They'll complete the remaining setup steps on their own."` */
    handedOffAdminDescription: string
    /** @defaultValue `"You've completed setup!"` */
    onboardedSelfSubtitle: string
    /** @defaultValue `"Your account will now be reviewed by your company admin."` */
    onboardedSelfDescription: string
    /** @defaultValue `"Done"` */
    doneCta: string
    /** @defaultValue `"New Hire report"` */
    newHireReportCta: string
    /** @defaultValue `"Missing requirements"` */
    missingRequirementsSubtitle: string
    /** @defaultValue `"Please complete the following steps in order to continue."` */
    missingRequirementsDescription: string
    steps: {
      /** @defaultValue `"Personal details"` */
      personal_details: string
      /** @defaultValue `"Job and compensation"` */
      compensation_details: string
      /** @defaultValue `"Work address"` */
      add_work_address: string
      /** @defaultValue `"Home address"` */
      add_home_address: string
      /** @defaultValue `"Federal tax details"` */
      federal_tax_setup: string
      /** @defaultValue `"State tax details"` */
      state_tax_setup: string
      /** @defaultValue `"Payment method"` */
      direct_deposit_setup: string
      /** @defaultValue `"Employee form signing"` */
      employee_form_signing: string
      /** @defaultValue `"File new hire report"` */
      file_new_hire_report: string
      /** @defaultValue `"Admin review"` */
      admin_review: string
    }
    stepsDescriptions: {
      /** @defaultValue `"Basic employee information and their home address."` */
      personal_details: string
      /** @defaultValue `" What the employee’s role(s) will be and their compensation."` */
      compensation_details: string
      /** @defaultValue `"Work address"` */
      add_work_address: string
      /** @defaultValue `"Home address"` */
      add_home_address: string
      /** @defaultValue `"Federal tax withholdings."` */
      federal_tax_setup: string
      /** @defaultValue `"State tax withholdings"` */
      state_tax_setup: string
      /** @defaultValue `"How the employee will be paid."` */
      direct_deposit_setup: string
      /** @defaultValue `"Documents requiring employee signature"` */
      employee_form_signing: string
      /** @defaultValue `"Indicate if new hire report needs to be filed"` */
      file_new_hire_report: string
      /** @defaultValue `"Admin review"` */
      admin_review: string
    }
  }
  /** Translation keys for the `Employee.PaySchedules` i18n namespace. */
  export interface EmployeePaySchedules {
    /** @defaultValue `"Set up pay schedule"` */
    pageTitle: string
    /** @defaultValue `"Save & continue"` */
    saveAndContinueCta: string
    /** @defaultValue `"Please make sure to verify this information is accurate. If this information isn't correct, it can delay when your team will be paid."` */
    pleaseVerify: string
    labels: {
      /** @defaultValue `"Pay frequency"` */
      frequency: string
      /** @defaultValue `"First pay date"` */
      firstPayDate: string
      /** @defaultValue `"Deadline to run payroll"` */
      deadline: string
    }
  }
  /** Translation keys for the `Employee.PaymentMethod` i18n namespace. */
  export interface EmployeePaymentMethod {
    /** @defaultValue `"Account number"` */
    accountNumberLabel: string
    /** @defaultValue `"Account type"` */
    accountTypeColumn: string
    /** @defaultValue `"Account type"` */
    accountTypeLabel: string
    /** @defaultValue `"Checking"` */
    accountTypeChecking: string
    /** @defaultValue `"Savings"` */
    accountTypeSavings: string
    /** @defaultValue `"Allocation"` */
    allocationColumn: string
    /** @defaultValue `"Bank account actions"` */
    actionColumn: string
    /** @defaultValue `"+ Add another bank account"` */
    addAnotherCta: string
    /** @defaultValue `"Add bank account"` */
    addBankAccountCta: string
    /** @defaultValue `"Add bank account"` */
    addBankAccountFormTitle: string
    /** @defaultValue `"List of employee bank accounts"` */
    bankAccountsListLabel: string
    /** @defaultValue `"Back"` */
    cancelCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelAddCta: string
    /** @defaultValue `"If you select check as the payment method, you'll need to write a physical check to this employee each payday."` */
    checkDescription: string
    /** @defaultValue `"If you select check as the payment method, you'll receive a physical check."` */
    checkDescriptionSelf: string
    /** @defaultValue `"Check"` */
    checkLabel: string
    /** @defaultValue `"Delete"` */
    deleteBankAccountCta: string
    deleteBankAccountDialog: {
      /** @defaultValue `"Delete bank account"` */
      title: string
      /** @defaultValue `"Are you sure you want to delete the bank account {{account}}?"` */
      description: string
      /** @defaultValue `"Delete"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
    /** @defaultValue `"Bank account {{account}} was removed"` */
    deleteBankAccountSuccessAlert: string
    /** @defaultValue `"We recommend direct deposit — we can deposit paychecks directly into your employees' bank accounts."` */
    directDepositDescription: string
    /** @defaultValue `"We recommend direct deposit — we can deposit paychecks directly into your account."` */
    directDepositDescriptionSelf: string
    /** @defaultValue `"Direct Deposit"` */
    directDepositLabel: string
    /** @defaultValue `"Bank account actions"` */
    hamburgerTitle: string
    /** @defaultValue `"Account nickname"` */
    nameLabel: string
    /** @defaultValue `"Nickname"` */
    nicknameColumn: string
    /** @defaultValue `"Select payment method"` */
    paymentFieldsetLegend: string
    /** @defaultValue `"Routing number"` */
    routingNumberColumn: string
    /** @defaultValue `"Routing number"` */
    routingNumberLabel: string
    /** @defaultValue `"(9 digits)"` */
    routingNumberDescription: string
    /** @defaultValue `"Split paycheck"` */
    splitCta: string
    /** @defaultValue `"Continue"` */
    submitCta: string
    /** @defaultValue `"Payment method"` */
    title: string
    /** @defaultValue `"Fixed amount"` */
    amountLabel: string
    /** @defaultValue `"You can split your paycheck into different accounts by percentage or flat dollar amounts. When splitting by amount, you can reorder the priority of the bank accounts by clicking and dragging the list icon to the left of the bank account field. We’ll pay the amounts in the order specified until your salary has been fully distributed to the last “remainder” account."` */
    splitDescription: string
    /** @defaultValue `"Reorderable list of bank accounts"` */
    draggableListLabel: string
    /** @defaultValue `"Percentage"` */
    percentageLabel: string
    /** @defaultValue `"{{name}} ({{account_number}})"` */
    splitAmountLabel: string
    /** @defaultValue `"Split by"` */
    splitByLabel: string
    /** @defaultValue `"Priority"` */
    priorityLabel: string
    /** @defaultValue `"Remainder"` */
    remainderLabel: string
    /** @defaultValue `"Split employee paycheck"` */
    splitTitle: string
    /** @defaultValue `"{{count}}st"` */
    priority_one: string
    /** @defaultValue `"{{count}}nd"` */
    priority_two: string
    /** @defaultValue `"{{count}}rd"` */
    priority_few: string
    /** @defaultValue `"{{count}}th"` */
    priority_other: string
    /** @defaultValue `"Payment"` */
    managementTitle: string
    /** @defaultValue `"Payment method"` */
    paymentMethodLabel: string
    /** @defaultValue `"Bank account was successfully added"` */
    bankFormSuccessAlert: string
    /** @defaultValue `"Payment split was successfully updated"` */
    splitViewSuccessAlert: string
    validations: {
      /** @defaultValue `"If payment method amount is split by Percentage, all split amounts must add up to exactly 100."` */
      percentageError: string
      /** @defaultValue `"Splits must total 100%. Currently {{total}}%."` */
      percentageErrorWithTotal: string
      /** @defaultValue `"Percentage should be a whole number between 0 and 100"` */
      percentageAmountError: string
      /** @defaultValue `"Please enter valid amount"` */
      amountError: string
      /** @defaultValue `"Account name is required"` */
      accountName: string
      /** @defaultValue `"Routing number should be a number (9 digits)"` */
      routingNumber: string
      /** @defaultValue `"Account number is a required field"` */
      accountNumber: string
      /** @defaultValue `"Account number should contain only digits (up to 17)"` */
      accountNumberFormat: string
    }
  }
  /** Translation keys for the `Employee.Profile` i18n namespace. */
  export interface EmployeeProfile {
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"This information will be used for payroll and taxes, so double-check that it's accurate."` */
    description: string
    /** @defaultValue `"Date of birth"` */
    dobLabel: string
    /** @defaultValue `"Personal email"` */
    email: string
    /** @defaultValue `"Use an email that's not associated with your company."` */
    emailDescription: string
    /** @defaultValue `"Legal first name"` */
    firstName: string
    /** @defaultValue `"Employee Form"` */
    formTitle: string
    /** @defaultValue `"Legal last name"` */
    lastName: string
    /** @defaultValue `"Middle initial"` */
    middleInitial: string
    /** @defaultValue `"Preferred first name"` */
    preferredFirstName: string
    /** @defaultValue `"Invite this employee to enter their own details online."` */
    selfOnboardingLabel: string
    /** @defaultValue `"Your employee will be invited to enter their information on their own time."` */
    selfOnboardingDescription: string
    /** @defaultValue `"Social Security Number (9 digit)"` */
    ssnLabel: string
    /** @defaultValue `"***-**-****"` */
    ssnMask: string
    /** @defaultValue `"Your employee’s first day of work at your company."` */
    startDateDescription: string
    /** @defaultValue `"Start date"` */
    startDateLabel: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Continue"` */
    submitCta: string
    /** @defaultValue `"Successfully updated profile"` */
    successAlert: string
    /** @defaultValue `"Basics"` */
    title: string
    validations: {
      /** @defaultValue `"Valid email is required"` */
      email: string
      /** @defaultValue `"Please enter valid first name"` */
      firstName: string
      /** @defaultValue `"Please enter valid last name"` */
      lastName: string
      /** @defaultValue `"Please select valid start date"` */
      startDate: string
      /** @defaultValue `"An employee must have a start date that is within six months from today."` */
      startDateOutOfRange: string
    }
    /** @defaultValue `"Work address"` */
    workAddress: string
    /** @defaultValue `"The primary location where the employee will be working."` */
    workAddressDescription: string
    /** @defaultValue `"Select work address..."` */
    workAddressPlaceholder: string
    /** @defaultValue `"Work Address"` */
    workAddressSectionTitle: string
    /** @defaultValue `"This is where you primarily work. We need this information to make sure your pay is accurate. Please reach out to your employer if this looks incorrect."` */
    workAddressSectionDescription: string
  }
  /** Translation keys for the `Employee.SplitPaycheck` i18n namespace. */
  export interface EmployeeSplitPaycheck {
    /** @defaultValue `"Amount"` */
    amountLabel: string
    /** @defaultValue `"Select the amount to split to this account."` */
    bankDescription: string
    /** @defaultValue `"Here you can split the employee’s paycheck. Select between a whole value or percentage split. Then set the priority and amount to complete the setup."` */
    description: string
    /** @defaultValue `"Percentage"` */
    percentageLabel: string
    /** @defaultValue `"Split amount"` */
    splitAmountLabel: string
    /** @defaultValue `"Split by"` */
    splitByLabel: string
    /** @defaultValue `"Priority"` */
    priorityLabel: string
    /** @defaultValue `"Split employee paycheck"` */
    title: string
    /** @defaultValue `"Select if this account will recieve any remaining payments"` */
    remainderLabel: string
    /** @defaultValue `"{{count}}st"` */
    priority_one: string
    /** @defaultValue `"{{count}}nd"` */
    priority_two: string
    /** @defaultValue `"{{count}}rd"` */
    priority_few: string
    /** @defaultValue `"{{count}}th"` */
    priority_other: string
    validations: {
      /** @defaultValue `"If payment method amount is split by Percentage, all split amounts must add up to exactly 100."` */
      percentageError: string
      /** @defaultValue `"Priorities must be sequential"` */
      priorityError: string
      /** @defaultValue `"Please enter valid amount"` */
      amountError: string
    }
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Submit"` */
    submitCta: string
  }
  /** Translation keys for the `Employee.SplitPaymentsFormBody` i18n namespace. */
  export interface EmployeeSplitPaymentsFormBody {
    /** @defaultValue `"Split employee paycheck"` */
    title: string
    /** @defaultValue `"You can split your paycheck into different accounts by percentage or flat dollar amounts. When splitting by amount, you can reorder the priority of the bank accounts by clicking and dragging the list icon to the left of the bank account field. We’ll pay the amounts in the order specified until your salary has been fully distributed to the last “remainder” account."` */
    splitDescription: string
    /** @defaultValue `"Split by"` */
    splitByLabel: string
    /** @defaultValue `"Percentage"` */
    percentageLabel: string
    /** @defaultValue `"Fixed amount"` */
    amountLabel: string
    /** @defaultValue `"{{name}} ({{account_number}})"` */
    splitAmountLabel: string
    /** @defaultValue `"Reorderable list of bank accounts"` */
    draggableListLabel: string
    /** @defaultValue `"Remainder"` */
    remainderLabel: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    validations: {
      /** @defaultValue `"Splits must total 100%. Currently {{total}}%."` */
      percentageErrorWithTotal: string
      /** @defaultValue `"Please enter valid amount"` */
      amountError: string
      /** @defaultValue `"Percentage should be a whole number between 0 and 100"` */
      percentageAmountError: string
    }
  }
  /** Translation keys for the `Employee.StateTaxes` i18n namespace. */
  export interface EmployeeStateTaxes {
    /** @defaultValue `"{{state}} Tax Requirements"` */
    stateTaxesTitle: string
    /** @defaultValue `"No state income tax withholding is required for this employee's work state."` */
    noWithholding: string
    /** @defaultValue `"Continue"` */
    submitCta: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Successfully updated state tax settings."` */
    successAlert: string
    validations: {
      /** @defaultValue `"This field is a required field. Please enter a value."` */
      required: string
    }
  }
  /** Translation keys for the `Employee.StateTaxesView` i18n namespace. */
  export interface EmployeeStateTaxesView {
    /** @defaultValue `"{{state}} Tax Requirements"` */
    stateTaxesTitle: string
    /** @defaultValue `"No state income tax withholding is required for this employee's work state."` */
    noWithholding: string
    validations: {
      /** @defaultValue `"This field is a required field. Please enter a value."` */
      required: string
    }
  }
  /** Translation keys for the `Employee.Terminations.TerminateEmployee` i18n namespace. */
  export interface EmployeeTerminationsTerminateEmployee {
    /** @defaultValue `"Terminate {{employeeName}}"` */
    title: string
    /** @defaultValue `"Set their last day of work and choose how to handle their final payroll."` */
    subtitle: string
    form: {
      lastDayOfEmployment: {
        /** @defaultValue `"Last day of work"` */
        label: string
        /** @defaultValue `"The last day must be after their most recent regular payroll period (even if they were paid $0). If they never started working, the dismissal date has to be on or after the hire date."` */
        description: string
      }
      payrollOption: {
        /** @defaultValue `"How should the final payroll be handled?"` */
        label: string
        /** @defaultValue `"We recommend running a dismissal payroll. Make sure you meet your state's requirements about employees' final payment (deadline to pay, unused PTO, etc)."` */
        description: string
        options: {
          dismissalPayroll: {
            /** @defaultValue `"Dismissal payroll"` */
            label: string
            /** @defaultValue `"Runs a final payroll that automatically pays out unused PTO, lets you include severance, keep a separate record for audits, and choose a custom payday based on when you run it."` */
            description: string
          }
          regularPayroll: {
            /** @defaultValue `"Regular payroll"` */
            label: string
            /** @defaultValue `"Same as dismissal payrolls, except there won’t be a separate record of final payment and you can’t customize the final payday (they’ll be paid on the regular payday)."` */
            description: string
          }
          anotherWay: {
            /** @defaultValue `"Another way"` */
            label: string
            /** @defaultValue `"You can run an off-cycle payroll to manually calculate final amounts, or you can pay them outside of the app (but make sure to report this payroll so the amounts are recorded on tax forms). You can also select this option if you’ve already paid them."` */
            description: string
          }
        }
      }
    }
    alert: {
      dismissalPayroll: {
        /** @defaultValue `"After submitting, you won't be able to undo this dismissal"` */
        label: string
        /** @defaultValue `"Make sure you want to end {{employeeName}}'s employment. You won't be able to cancel this dismissal and you'll need to rehire them if they return."` */
        text: string
      }
      regularPayroll: {
        /** @defaultValue `"After their last day, you won't be able to undo this dismissal"` */
        label: string
        /** @defaultValue `"Make sure you want to end {{employeeName}}'s employment. You will be able to cancel the dismissal or make changes until their last day. After their last working day, you won't be able to cancel this dismissal and you'll need to rehire them if they return."` */
        text: string
      }
      anotherWay: {
        /** @defaultValue `"After their last day, you won't be able to undo this dismissal"` */
        label: string
        /** @defaultValue `"Make sure you want to end {{employeeName}}'s employment. You will be able to cancel the dismissal or make changes until their last day. After their last working day, you won't be able to cancel this dismissal and you'll need to rehire them if they return."` */
        text: string
      }
    }
    actions: {
      /** @defaultValue `"Terminate employee"` */
      submit: string
      /** @defaultValue `"Cancel"` */
      cancel: string
    }
    validation: {
      /** @defaultValue `"Last day of work is required"` */
      lastDayRequired: string
      /** @defaultValue `"Please select how to handle the final payroll"` */
      payrollOptionRequired: string
    }
  }
  /** Translation keys for the `Employee.Terminations.TerminationFlow` i18n namespace. */
  export interface EmployeeTerminationsTerminationFlow {
    /** @defaultValue `"Termination has been cancelled successfully"` */
    cancelSuccess: string
    breadcrumbs: {
      /** @defaultValue `"Terminate employee"` */
      form: string
      /** @defaultValue `"Summary"` */
      summary: string
      /** @defaultValue `"Run payroll"` */
      dismissal: string
    }
    offCycleCreation: {
      /** @defaultValue `"Creating off-cycle payroll…"` */
      loading: string
      /** @defaultValue `"Failed to create off-cycle payroll. Please try again."` */
      error: string
      /** @defaultValue `"No unprocessed termination pay periods found for this employee."` */
      noPeriodsError: string
      /** @defaultValue `"Retry"` */
      retry: string
    }
  }
  /** Translation keys for the `Employee.Terminations.TerminationSummary` i18n namespace. */
  export interface EmployeeTerminationsTerminationSummary {
    /** @defaultValue `"Termination summary"` */
    title: string
    /** @defaultValue `"The termination has been submitted. Here's the timeline and what to expect."` */
    subtitle: string
    alert: {
      success: {
        /** @defaultValue `"{{employeeName}} has been successfully terminated"` */
        label: string
      }
    }
    dates: {
      /** @defaultValue `"Today"` */
      today: string
      /** @defaultValue `"You dismissed {{employeeName}}."` */
      dismissed: string
      /** @defaultValue `"Last day of work"` */
      lastDayOfWork: string
      /** @defaultValue `"Last pay day"` */
      lastPayDay: string
    }
    offboarding: {
      /** @defaultValue `"Offboarding checklist:"` */
      title: string
      runPayroll: {
        /** @defaultValue `"Run payroll"` */
        title: string
        /** @defaultValue `"We recommend running the payroll 2-4 days before the last day of employment so there's enough time for direct deposit to process. Make sure you <StateRequirementsLink href=\"https://support.gusto.com/article/100895878100000/Final-paychecks\" target=\"_blank\">meet your state's requirements</StateRequirementsLink> about employees' final payment (deadline to pay, unused PTO, etc). If you're going to pay an employee outside of payroll (like via a physical check), make sure you record it for tax purposes."` */
        description: string
      }
      taxForms: {
        /** @defaultValue `"Tax forms and documents"` */
        title: string
        /** @defaultValue `"Ensure that former employees have a way to access their paystubs and tax documents."` */
        description: string
      }
      disconnectAccounts: {
        /** @defaultValue `"Disconnect accounts and services"` */
        title: string
        /** @defaultValue `"If there are any company email addresses or other accounts associated with the dismissed employees, we recommend disconnecting those before the employees' last day."` */
        description: string
      }
    }
    actions: {
      /** @defaultValue `"Cancel termination"` */
      cancelTermination: string
      /** @defaultValue `"Edit termination"` */
      editDismissal: string
      /** @defaultValue `"Run termination payroll"` */
      runDismissalPayroll: string
      /** @defaultValue `"Run off-cycle payroll"` */
      runOffCyclePayroll: string
    }
    cancelDialog: {
      /** @defaultValue `"Cancel termination?"` */
      title: string
      /** @defaultValue `"Are you sure you want to cancel this termination? The employee will remain active."` */
      body: string
      /** @defaultValue `"Yes, cancel termination"` */
      confirm: string
      /** @defaultValue `"No, go back"` */
      cancel: string
    }
  }
  /** Translation keys for the `InformationRequests.InformationRequestForm` i18n namespace. */
  export interface InformationRequestsInformationRequestForm {
    /** @defaultValue `"Request for information"` */
    title: string
    blockingAlert: {
      /** @defaultValue `"This is a payroll blocking request"` */
      title: string
      /** @defaultValue `"You will not be able to run payroll until we receive the information requested below."` */
      description: string
    }
    questionTypes: {
      /** @defaultValue `"Document required"` */
      document: string
      /** @defaultValue `"Answer required"` */
      answer: string
    }
    fields: {
      textAnswer: {
        /** @defaultValue `"Answer the above question"` */
        label: string
        /** @defaultValue `"Your answer"` */
        placeholder: string
      }
      fileUpload: {
        /** @defaultValue `"Upload document"` */
        label: string
      }
    }
    validation: {
      /** @defaultValue `"This field is required"` */
      required: string
      /** @defaultValue `"Please upload a file"` */
      fileRequired: string
    }
    cta: {
      /** @defaultValue `"Cancel"` */
      cancel: string
      /** @defaultValue `"Submit response"` */
      submit: string
      /** @defaultValue `"Close"` */
      close: string
    }
    unsupported: {
      persona: {
        /** @defaultValue `"Verify identity"` */
        title: string
        /** @defaultValue `"In order to ensure the security of your account, we need some additional information to help verify your signatory's identity."` */
        description: string
      }
      generic: {
        /** @defaultValue `"Additional information required"` */
        title: string
        /** @defaultValue `"We need some more information that we are unable to collect here."` */
        description: string
      }
      /** @defaultValue `"Please contact support"` */
      contactSupport: string
    }
  }
  /** Translation keys for the `InformationRequests.InformationRequestList` i18n namespace. */
  export interface InformationRequestsInformationRequestList {
    /** @defaultValue `"Information requests"` */
    title: string
    /** @defaultValue `"We need you to share some more information about your business. This info will be used to keep your business secure and ensure that our data about your company is correct. Please make sure to do this soon since it may otherwise impact your ability to run payroll."` */
    description: string
    /** @defaultValue `"No information requests"` */
    emptyTableTitle: string
    /** @defaultValue `"There are no information requests that need your response at this time."` */
    emptyTableDescription: string
    columns: {
      /** @defaultValue `"Type"` */
      type: string
      /** @defaultValue `"Status"` */
      status: string
    }
    types: {
      /** @defaultValue `"Company Onboarding"` */
      companyOnboarding: string
      /** @defaultValue `"Account Protection"` */
      accountProtection: string
      /** @defaultValue `"Payment Request"` */
      paymentRequest: string
      /** @defaultValue `"Payment Error"` */
      paymentError: string
      /** @defaultValue `"Information Request"` */
      unknown: string
    }
    status: {
      /** @defaultValue `"Incomplete"` */
      incomplete: string
      /** @defaultValue `"Under review"` */
      underReview: string
      /** @defaultValue `"Payroll blocking"` */
      payrollBlocking: string
    }
    cta: {
      /** @defaultValue `"Respond"` */
      respond: string
    }
  }
  /** Translation keys for the `InformationRequests` i18n namespace. */
  export interface InformationRequests {
    alerts: {
      informationRequestResponded: {
        /** @defaultValue `"We've received your response"` */
        title: string
        /** @defaultValue `"We are reviewing your submission and will reach out if we need any additional information."` */
        description: string
      }
    }
  }
  /** Translation keys for the `Payroll.Common` i18n namespace. */
  export interface PayrollCommon {
    status: {
      /** @defaultValue `"Processed"` */
      processed: string
      /** @defaultValue `"Unprocessed"` */
      unprocessed: string
      /** @defaultValue `"Calculating..."` */
      calculating: string
      /** @defaultValue `"Ready to submit"` */
      readyToSubmit: string
      /** @defaultValue `"Processing"` */
      processing: string
      /** @defaultValue `"Failed"` */
      failed: string
      /** @defaultValue `"Waiting for wire in"` */
      waitingForWireIn: string
      /** @defaultValue `"Pending approval"` */
      pendingApproval: string
      /** @defaultValue `"Due in {{count}} hour"` */
      dueInHours_one: string
      /** @defaultValue `"Due in {{count}} hours"` */
      dueInHours_other: string
      /** @defaultValue `"Due in {{count}} day"` */
      dueInDays_one: string
      /** @defaultValue `"Due in {{count}} days"` */
      dueInDays_other: string
      /** @defaultValue `"{{count}} day late"` */
      daysLate_one: string
      /** @defaultValue `"{{count}} days late"` */
      daysLate_other: string
      /** @defaultValue `"Pending"` */
      pending: string
      /** @defaultValue `"Paid"` */
      paid: string
      /** @defaultValue `"Complete"` */
      complete: string
      /** @defaultValue `"Submitted"` */
      submitted: string
      /** @defaultValue `"In progress"` */
      inProgress: string
    }
  }
  /** Translation keys for the `Payroll.ConfirmWireDetailsBanner` i18n namespace. */
  export interface PayrollConfirmWireDetailsBanner {
    /** @defaultValue `"Confirm Wire Details"` */
    breadcrumbLabel: string
    banner: {
      /** @defaultValue `"To make payroll, wire funds by {{time}} on {{date}}"` */
      title: string
      /** @defaultValue `"Wire transfer details required for {{payrollRange}} payroll"` */
      titleWithPayroll: string
      /** @defaultValue `"Wire transfer details required for {{count}} payrolls or payments"` */
      titleMultiple: string
      /** @defaultValue `"We can't pay your team until we get your wire. It may take time to get from your bank to ours, so we recommend sending it as soon as you can."` */
      description: string
      /** @defaultValue `"Payroll for {{payrollRange}}"` */
      requestLabelPayroll: string
      /** @defaultValue `"Contractor payment for {{requestedAmount}}"` */
      requestLabelContractorPaymentGroup: string
    }
    cta: {
      /** @defaultValue `"Start your wire transfer"` */
      startWireTransfer: string
    }
    modal: {
      /** @defaultValue `"Wire instructions"` */
      title: string
      /** @defaultValue `"Close"` */
      close: string
      /** @defaultValue `"I've made a wire transfer"` */
      submitCta: string
    }
  }
  /** Translation keys for the `Payroll.ConfirmWireDetailsForm` i18n namespace. */
  export interface PayrollConfirmWireDetailsForm {
    /** @defaultValue `"Confirm wire details for {{payrollRange}}"` */
    title: string
    /** @defaultValue `"Enter the details of your wire transfer submitted through your bank."` */
    description: string
    /** @defaultValue `"Total amount sent"` */
    amountLabel: string
    /** @defaultValue `"Date sent"` */
    dateLabel: string
    /** @defaultValue `"Initiating bank"` */
    bankNameLabel: string
    /** @defaultValue `"The bank where you initiated the wire transfer"` */
    bankNameDescription: string
    /** @defaultValue `"Enter a financial institution"` */
    bankNamePlaceholder: string
    /** @defaultValue `"Additional notes"` */
    notesLabel: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Submit"` */
    submitCta: string
    validations: {
      /** @defaultValue `"Total amount sent is required"` */
      amount: string
      /** @defaultValue `"Date sent is required"` */
      date: string
      /** @defaultValue `"Initiating bank is required"` */
      bankName: string
    }
    confirmationAlert: {
      /** @defaultValue `"Wire details submitted for {{payrollRange}} payroll"` */
      title: string
      /** @defaultValue `"Wire details submitted"` */
      emptyTitle: string
      /** @defaultValue `"Once we receive the funds from your bank we'll pay your team on {{checkDate}}."` */
      content: string
    }
  }
  /** Translation keys for the `Payroll.Dismissal` i18n namespace. */
  export interface PayrollDismissal {
    /** @defaultValue `"Pay Period"` */
    breadcrumbLabel: string
    /** @defaultValue `"Run dismissal payroll"` */
    pageTitle: string
    /** @defaultValue `"Select the pay period for the terminated employee's final payroll."` */
    pageDescription: string
    /** @defaultValue `"Pay period"` */
    selectLabel: string
    /** @defaultValue `"Select a pay period"` */
    selectPlaceholder: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"There are no unprocessed termination pay periods available."` */
    emptyState: string
    errors: {
      /** @defaultValue `"Dismissal payroll was created but no payroll ID was returned"` */
      missingPayrollId: string
      /** @defaultValue `"Please select a pay period before continuing"` */
      noPayPeriodSelected: string
      /** @defaultValue `"The selected pay period is no longer available"` */
      invalidPayPeriod: string
    }
  }
  /** Translation keys for the `Payroll.EmployeeSelection` i18n namespace. */
  export interface PayrollEmployeeSelection {
    /** @defaultValue `"Employees"` */
    sectionTitle: string
    /** @defaultValue `"Search by name"` */
    searchPlaceholder: string
  }
  /** Translation keys for the `Payroll.GrossUpModal` i18n namespace. */
  export interface PayrollGrossUpModal {
    /** @defaultValue `"Enter a net amount"` */
    title: string
    /** @defaultValue `"Please enter the net amount you want this employee to receive. We'll then automatically calculate the gross amount you need to pay, including taxes and deductions."` */
    description: string
    /** @defaultValue `"This will override any previously entered amounts."` */
    warning: string
    /** @defaultValue `"Net amount"` */
    netPayLabel: string
    /** @defaultValue `"Calculate"` */
    calculateCta: string
    /** @defaultValue `"Calculating..."` */
    calculatingCta: string
    /** @defaultValue `"Apply"` */
    applyCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    /** @defaultValue `"Calculated gross pay"` */
    grossPayResult: string
    /** @defaultValue `"Unable to calculate gross up. Please try again."` */
    errorMessage: string
    validations: {
      /** @defaultValue `"Net amount must be greater than zero cents"` */
      netPay: string
    }
  }
  /** Translation keys for the `Payroll.OffCycle` i18n namespace. */
  export interface PayrollOffCycle {
    createOffCyclePayroll: {
      /** @defaultValue `"Run Off-Cycle Payroll"` */
      breadcrumbLabel: string
    }
  }
  /** Translation keys for the `Payroll.OffCycleCreation` i18n namespace. */
  export interface PayrollOffCycleCreation {
    /** @defaultValue `"New Off-Cycle Payroll"` */
    pageTitle: string
    /** @defaultValue `"Configure your off-cycle payroll details below."` */
    pageDescription: string
    /** @defaultValue `"Pay period and payment date"` */
    payPeriodSectionTitle: string
    /** @defaultValue `"Enter a work period to show on your employees' pay stubs."` */
    payPeriodSectionDescription: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    /** @defaultValue `"Include all employees in this payroll"` */
    includeAllEmployeesLabel: string
    errors: {
      /** @defaultValue `"Off-cycle payroll was created but no payroll ID was returned"` */
      missingPayrollId: string
      /** @defaultValue `"At least one employee must be selected"` */
      noEmployeesSelected: string
    }
  }
  /** Translation keys for the `Payroll.OffCycleDeductionsSetting` i18n namespace. */
  export interface PayrollOffCycleDeductionsSetting {
    /** @defaultValue `"Deductions and contributions"` */
    title: string
    /** @defaultValue `"Employers often block deductions and contributions from certain checks (i.e. bonus checks), since they are calculated on a per-pay-period basis. Deductions and contributions include loan repayments, garnishments, benefits, etc. Taxes will be included regardless of what is chosen."` */
    description: string
    options: {
      include: {
        /** @defaultValue `"Make all the regular deductions and contributions."` */
        label: string
      }
      skip: {
        /** @defaultValue `"Block all deductions and contributions, except 401(k). Taxes will be included."` */
        label: string
      }
    }
  }
  /** Translation keys for the `Payroll.OffCyclePayPeriodDateForm` i18n namespace. */
  export interface PayrollOffCyclePayPeriodDateForm {
    /** @defaultValue `"Check-only payroll"` */
    checkOnlyLabel: string
    /** @defaultValue `"Select this option if all employees will be paid by check only. This allows you to set the check date to today or any future date."` */
    checkOnlyDescription: string
    /** @defaultValue `"Start date"` */
    startDateLabel: string
    /** @defaultValue `"End date"` */
    endDateLabel: string
    /** @defaultValue `"Payment date"` */
    checkDateLabel: string
    validations: {
      /** @defaultValue `"Start date is required"` */
      startDateRequired: string
      /** @defaultValue `"End date is required"` */
      endDateRequired: string
      /** @defaultValue `"Payment date is required"` */
      checkDateRequired: string
      /** @defaultValue `"End date must be on or after start date"` */
      endDateAfterStart: string
      /** @defaultValue `"Start date cannot be in the future for correction payrolls"` */
      startDateNotFuture: string
      /** @defaultValue `"Payment date must be at least {{count}} business day from today for direct deposit"` */
      checkDateAchLeadTime_one: string
      /** @defaultValue `"Payment date must be at least {{count}} business days from today for direct deposit"` */
      checkDateAchLeadTime_other: string
      /** @defaultValue `"Payment date must be today or a future date"` */
      checkDateNotPast: string
    }
  }
  /** Translation keys for the `Payroll.OffCycleReasonSelection` i18n namespace. */
  export interface PayrollOffCycleReasonSelection {
    /** @defaultValue `"Reason"` */
    title: string
    options: {
      correction: {
        /** @defaultValue `"Correction payment"` */
        label: string
        /** @defaultValue `"Run a payroll outside of your regular pay schedule"` */
        description: string
      }
      bonus: {
        /** @defaultValue `"Bonus"` */
        label: string
        /** @defaultValue `"Pay a bonus, gift, or commission."` */
        description: string
      }
    }
    aria: {
      /** @defaultValue `"Select off-cycle payroll reason"` */
      reasonSelection: string
    }
  }
  /** Translation keys for the `Payroll.OffCycleTaxWithholding` i18n namespace. */
  export interface PayrollOffCycleTaxWithholding {
    /** @defaultValue `"Tax withholding rates"` */
    title: string
    /** @defaultValue `"We've set the recommended withholding rate for each earning type below. Regular wages should be taxed at a rate that matches your employees' normal pay schedule"` */
    description: string
    /** @defaultValue `"Edit"` */
    editButton: string
    table: {
      headers: {
        /** @defaultValue `"Wage types"` */
        wageTypes: string
        /** @defaultValue `"Taxed as"` */
        taxedAs: string
      }
      /** @defaultValue `"Regular wages, paid {{frequency}}"` */
      taxedAsRegular: string
      /** @defaultValue `"Supplemental 22% for federal income taxes and at the state's supplemental tax rate for state income taxes."` */
      taxedAsSupplemental: string
      /** @defaultValue `"Not taxed"` */
      taxedAsNotTaxed: string
    }
    wageTypeGroups: {
      regular: {
        /** @defaultValue `"Regular hours, regular wages, and tips"` */
        label: string
        /** @defaultValue `"These earnings should be taxed at a rate that matches your employees' regular pay schedule."` */
        description: string
      }
      supplemental: {
        /** @defaultValue `"Supplemental wages, bonus wages, commission"` */
        label: string
        /** @defaultValue `"These are typically taxed at the rate required by the IRS for federal income taxes and by the state for state income taxes."` */
        description: string
      }
      reimbursement: {
        /** @defaultValue `"Reimbursements"` */
        label: string
      }
    }
    modal: {
      /** @defaultValue `"Tax withholding rates"` */
      title: string
      regularSection: {
        /** @defaultValue `"Rate for regular wages and earnings"` */
        title: string
        /** @defaultValue `"Includes regular hours, regular wages, and tips"` */
        subtitle: string
        /** @defaultValue `"Rate is based on payment schedule of"` */
        rateLabel: string
        /** @defaultValue `"Select pay schedule..."` */
        ratePlaceholder: string
        /** @defaultValue `"Regular wages should be taxed at a rate matching your employees' regular pay schedule. Select the most accurate pay schedule."` */
        rateDescription: string
      }
      supplementalSection: {
        /** @defaultValue `"Rate for supplemental wages and earnings"` */
        title: string
        /** @defaultValue `"Use supplemental tax rate"` */
        useSupplementalRate: string
        /** @defaultValue `"Taxes will be withheld at the IRS's required rate of 22% for federal income taxes. State income taxes will be taxed at the state's supplemental tax rate"` */
        useSupplementalRateDescription: string
        /** @defaultValue `"Use rate for regular wages (based on payment schedule of {{frequency}})"` */
        useRegularRate: string
        /** @defaultValue `"We'll sum the entirety of the employee's wages and withhold taxes on the entire amount at the rate for regular wages"` */
        useRegularRateDescription: string
      }
      /** @defaultValue `"Cancel"` */
      cancelButton: string
      /** @defaultValue `"Done"` */
      doneButton: string
    }
    payPeriodFrequency: {
      /** @defaultValue `"Every week"` */
      everyWeek: string
      /** @defaultValue `"Every other week"` */
      everyOtherWeek: string
      /** @defaultValue `"Twice per month"` */
      twicePerMonth: string
      /** @defaultValue `"Monthly"` */
      monthly: string
      /** @defaultValue `"Quarterly"` */
      quarterly: string
      /** @defaultValue `"Semiannually"` */
      semiannually: string
      /** @defaultValue `"Annually"` */
      annually: string
    }
  }
  /** Translation keys for the `Payroll.PayrollBlocker` i18n namespace. */
  export interface PayrollPayrollBlocker {
    /** @defaultValue `"Payroll Blockers"` */
    breadcrumbLabel: string
    alerts: {
      recoveryCaseResubmitted: {
        /** @defaultValue `"Payment resubmitted"` */
        title: string
        /** @defaultValue `"We are reviewing your submission and will reach out if we need any additional information."` */
        description: string
      }
      informationRequestResponded: {
        /** @defaultValue `"We've received your response"` */
        title: string
        /** @defaultValue `"We are reviewing your submission and will reach out if we need any additional information."` */
        description: string
      }
    }
    /** @defaultValue `"Exit"` */
    exitFlowCta: string
    /** @defaultValue `"{{count}} issues are preventing you from running payroll"` */
    multipleIssuesTitle: string
    /** @defaultValue `"View All Blockers"` */
    viewAllBlockers: string
    /** @defaultValue `"View Blocker"` */
    viewBlocker: string
    /** @defaultValue `"Payroll blockers"` */
    blockersListTitle: string
    /** @defaultValue `"Blocker"` */
    blockerSectionLabel: string
    /** @defaultValue `"The following issues must be resolved before you can run payroll:"` */
    blockersListDescription: string
    /** @defaultValue `"No payroll blockers at this time."` */
    noBlockersMessage: string
    /** @defaultValue `"Estimated time"` */
    estimatedTime: string
    /** @defaultValue `"Unknown blocker"` */
    defaultBlockerDescription: string
    /** @defaultValue `"Please contact support."` */
    defaultBlockerHelp: string
    blockers: {
      geocode_error: {
        /** @defaultValue `"Address Verification Failed"` */
        title: string
        /** @defaultValue `"Company or employee address could not be verified."` */
        description: string
        /** @defaultValue `"Please ensure all addresses are valid and complete."` */
        help: string
      }
      geocode_needed: {
        /** @defaultValue `"Address Verification Missing"` */
        title: string
        /** @defaultValue `"Company or employee address verification is missing."` */
        description: string
        /** @defaultValue `"Please ensure all addresses are entered correctly."` */
        help: string
      }
      missing_signatory: {
        /** @defaultValue `"Signatory Required"` */
        title: string
        /** @defaultValue `"A signatory who is authorized to sign documents on behalf of your company is required."` */
        description: string
        /** @defaultValue `"Designate an authorized company signatory to continue."` */
        help: string
        /** @defaultValue `"Set Up Signatory"` */
        defaultAction: string
      }
      invalid_signatory: {
        /** @defaultValue `"Signatory Verification Failed"` */
        title: string
        /** @defaultValue `"Please ensure that the identity verification of the company signatory is successful."` */
        description: string
        /** @defaultValue `"The signatory must complete identity verification."` */
        help: string
      }
      suspended: {
        /** @defaultValue `"Company Suspended"` */
        title: string
        /** @defaultValue `"Company is suspended and cannot run payroll."` */
        description: string
        /** @defaultValue `"Contact support if you believe this is an error."` */
        help: string
        /** @defaultValue `"Contact Support"` */
        defaultAction: string
      }
      needs_onboarding: {
        /** @defaultValue `"Onboarding Incomplete"` */
        title: string
        /** @defaultValue `"Company must complete all onboarding requirements in order to run payroll."` */
        description: string
        /** @defaultValue `"Complete all required onboarding steps."` */
        help: string
        /** @defaultValue `"Complete Onboarding"` */
        defaultAction: string
      }
      missing_bank_info: {
        /** @defaultValue `"Bank Account Required"` */
        title: string
        /** @defaultValue `"Company must have a bank account in order to run payroll."` */
        description: string
        /** @defaultValue `"Add and verify a company bank account."` */
        help: string
        /** @defaultValue `"Add Bank Account"` */
        defaultAction: string
      }
      missing_bank_verification: {
        /** @defaultValue `"Bank Account Verification Required"` */
        title: string
        /** @defaultValue `"Company bank account must be verified in order to run payroll."` */
        description: string
        /** @defaultValue `"Complete bank account verification process."` */
        help: string
        /** @defaultValue `"Verify Account"` */
        defaultAction: string
      }
      missing_employee_setup: {
        /** @defaultValue `"Employees Required"` */
        title: string
        /** @defaultValue `"Company must add employees in order to run payroll."` */
        description: string
        /** @defaultValue `"Add at least one employee to continue."` */
        help: string
      }
      missing_pay_schedule: {
        /** @defaultValue `"Pay Schedule Required"` */
        title: string
        /** @defaultValue `"Company must have a pay schedule in order to run payroll."` */
        description: string
        /** @defaultValue `"Set up a pay schedule for your employees."` */
        help: string
      }
      pay_schedule_setup_not_complete: {
        /** @defaultValue `"Employee Pay Schedule Missing"` */
        title: string
        /** @defaultValue `"Some employees don't have a pay schedule set up yet."` */
        description: string
        /** @defaultValue `"Assign all employees to a pay schedule."` */
        help: string
      }
      missing_forms: {
        /** @defaultValue `"Forms Require Signature"` */
        title: string
        /** @defaultValue `"Company forms must be signed in order to run payroll."` */
        description: string
        /** @defaultValue `"Complete and sign all required forms."` */
        help: string
      }
      needs_approval: {
        /** @defaultValue `"Pending Approval"` */
        title: string
        /** @defaultValue `"Company needs to be approved to run payroll."` */
        description: string
        /** @defaultValue `"Your account is under review. Please wait for approval."` */
        help: string
      }
      pending_payroll_review: {
        /** @defaultValue `"Payroll Under Review"` */
        title: string
        /** @defaultValue `"Payroll is blocked. We are reviewing payroll information in your account."` */
        description: string
        /** @defaultValue `"Contact support if you believe this is an error."` */
        help: string
        /** @defaultValue `"Contact Support"` */
        defaultAction: string
      }
      contractor_only_company: {
        /** @defaultValue `"Contractor-Only Company"` */
        title: string
        /** @defaultValue `"Company is set to contractor_only and cannot run payroll."` */
        description: string
        /** @defaultValue `"Update company type and complete additional onboarding."` */
        help: string
      }
      eftps_ic_error: {
        /** @defaultValue `"Electronic Federal Tax Payment System Error"` */
        title: string
        /** @defaultValue `"We could not make payments to the Electronic Federal Tax Payment System."` */
        description: string
        /** @defaultValue `"Please contact support."` */
        help: string
        /** @defaultValue `"Contact Support"` */
        defaultAction: string
      }
      wc_pending_approval: {
        /** @defaultValue `"Worker's Compensation Pending Approval"` */
        title: string
        /** @defaultValue `"Worker's compensation policy needs to be approved."` */
        description: string
        /** @defaultValue `"The worker's compensation process has not been completed and may require more information. Please contact support."` */
        help: string
        /** @defaultValue `"Contact Support"` */
        defaultAction: string
      }
      pending_recovery_case: {
        /** @defaultValue `"Recovery Case Pending"` */
        title: string
        /** @defaultValue `"Company has an open recovery case that must be resolved in order to unblock payroll."` */
        description: string
        /** @defaultValue `"Please contact support."` */
        help: string
        /** @defaultValue `"Contact Support"` */
        defaultAction: string
      }
      pending_information_request: {
        /** @defaultValue `"Information Request Pending"` */
        title: string
        /** @defaultValue `"Company has an open information request that must be submitted and approved in order to unblock payroll."` */
        description: string
        /** @defaultValue `"Please contact support."` */
        help: string
        /** @defaultValue `"Contact Support"` */
        defaultAction: string
      }
      missing_addresses: {
        /** @defaultValue `"Company Addresses Missing"` */
        title: string
        /** @defaultValue `"Company must add addresses in order to run payroll."` */
        description: string
        /** @defaultValue `"Missing company onboarding requirement. Confirm that company addresses have been added."` */
        help: string
        /** @defaultValue `"Add Addresses"` */
        defaultAction: string
      }
      missing_federal_tax_setup: {
        /** @defaultValue `"Federal Tax Setup Missing"` */
        title: string
        /** @defaultValue `"Company must complete federal tax setup in order to run payroll."` */
        description: string
        /** @defaultValue `"Missing company onboarding requirement. Confirm that company federal tax setup has been completed."` */
        help: string
        /** @defaultValue `"Complete Federal Tax Setup"` */
        defaultAction: string
      }
      missing_industry_selection: {
        /** @defaultValue `"Industry Selection Missing"` */
        title: string
        /** @defaultValue `"Company must complete industry selection in order to run payroll."` */
        description: string
        /** @defaultValue `"Missing company onboarding requirement. Confirm that a company industry has been selected."` */
        help: string
        /** @defaultValue `"Select Industry"` */
        defaultAction: string
      }
      missing_state_tax_setup: {
        /** @defaultValue `"State Tax Setup Missing"` */
        title: string
        /** @defaultValue `"Company must complete state tax setup in order to run payroll."` */
        description: string
        /** @defaultValue `"Missing company onboarding requirement. Confirm that the company has completed state tax setup."` */
        help: string
        /** @defaultValue `"Complete State Tax Setup"` */
        defaultAction: string
      }
    }
  }
  /** Translation keys for the `Payroll.PayrollConfiguration` i18n namespace. */
  export interface PayrollPayrollConfiguration {
    /** @defaultValue `"Edit Payroll"` */
    pageTitle: string
    /** @defaultValue `"{{payrollType}} payroll for <dateWrapper>{{startDate}} - {{endDate}}</dateWrapper>"` */
    description: string
    /** @defaultValue `"{{payrollType}} payroll"` */
    descriptionDismissal: string
    /** @defaultValue `"Edit Payroll for {{startDate}} - {{endDate}}"` */
    breadcrumbLabel: string
    /** @defaultValue `"Edit Dismissal Payroll"` */
    breadcrumbLabelDismissal: string
    /** @defaultValue `"Save and exit"` */
    exitFlowCta: string
    /** @defaultValue `"Calculate and review"` */
    calculatePayroll: string
    /** @defaultValue `"Calculate and review"` */
    calculatePayrollTitle: string
    /** @defaultValue `"Hours and additional earnings"` */
    hoursAndEarningsTitle: string
    /** @defaultValue `"Review and update your employee's hours, reimbursements, and additional earnings below."` */
    hoursAndEarningsDescription: string
    /** @defaultValue `"Employee compensations"` */
    employeeCompensationsTitle: string
    tableColumns: {
      /** @defaultValue `"Employees"` */
      employees: string
      /** @defaultValue `"Hours"` */
      hours: string
      /** @defaultValue `"Time off"` */
      timeOff: string
      /** @defaultValue `"Additional earnings"` */
      additionalEarnings: string
      /** @defaultValue `"Reimbursements"` */
      reimbursements: string
      /** @defaultValue `"Total pay"` */
      totalPay: string
    }
    /** @defaultValue `"Skipped"` */
    skippedBadge: string
    editMenu: {
      /** @defaultValue `"Edit"` */
      edit: string
      /** @defaultValue `"Skip employee"` */
      skip: string
      /** @defaultValue `"Unskip employee"` */
      unskip: string
      /** @defaultValue `"Set employee net earnings"` */
      setNetEarnings: string
    }
    alerts: {
      /** @defaultValue `"Your progress has been saved"` */
      progressSaved: string
      /** @defaultValue `"To pay your employees with direct deposit by {{payDate}}, you'll need to run payroll by {{time}} on {{date}}."` */
      directDepositDeadline: string
      /** @defaultValue `"Make sure to submit before the deadline to ensure timely payments."` */
      directDepositDeadlineText: string
      /** @defaultValue `"Your original pay date was {{initialCheckDate}}"` */
      payrollLate: string
      /** @defaultValue `"Run payroll before {{time}} on {{date}} to pay your employees on {{newCheckDate}}."` */
      payrollLateText: string
      payrollDeadline: {
        /** @defaultValue `"Payroll Deadline"` */
        label: string
        /** @defaultValue `"To pay your employees with direct deposit by the check date, you'll need to run payroll by the deadline."` */
        message: string
      }
      skippedEmployees: {
        /** @defaultValue `"Skipped Employees"` */
        label: string
        /** @defaultValue `"Employee address not verified"` */
        employeeAddressNotVerified: string
      }
      employeeUpdated: {
        /** @defaultValue `"{{employeeName}} updated successfully"` */
        label: string
      }
    }
    /** @defaultValue `"Preparing payroll..."` */
    loadingTitle: string
    /** @defaultValue `"This may take a minute or two. You can navigate away while this happens."` */
    loadingDescription: string
    /** @defaultValue `"Calculating payroll..."` */
    calculatingTitle: string
    /** @defaultValue `"This may take a minute or two. You can navigate away while this happens."` */
    calculatingDescription: string
    /** @defaultValue `"Calculating payroll..."` */
    calculatingPayroll: string
  }
  /** Translation keys for the `Payroll.PayrollEditEmployee` i18n namespace. */
  export interface PayrollPayrollEditEmployee {
    /** @defaultValue `"Edit payroll for {{employeeName}}"` */
    pageTitle: string
    /** @defaultValue `"{{firstName}} {{lastName}}"` */
    breadcrumbLabel: string
    /** @defaultValue `"Gross pay (excluding reimbursements)"` */
    grossPayLabel: string
    /** @defaultValue `"Gross pay: {{grossPay}} (excluding reimbursements)"` */
    grossPayLabelMobile: string
    /** @defaultValue `"Regular hours"` */
    regularHoursTitle: string
    /** @defaultValue `"Hours"` */
    hoursUnit: string
    /** @defaultValue `"Save"` */
    saveCta: string
    /** @defaultValue `"Cancel"` */
    cancelCta: string
    compensationNames: {
      /** @defaultValue `"Regular Hours"` */
      regularHours: string
      /** @defaultValue `"Overtime"` */
      overtime: string
      /** @defaultValue `"Double overtime"` */
      doubleOvertime: string
    }
    /** @defaultValue `"Time off"` */
    timeOffTitle: string
    /** @defaultValue `"Time off hours used this pay period"` */
    timeOffTitleDismissal: string
    /** @defaultValue `"Unused time off payout"` */
    finalPayoutTitle: string
    /** @defaultValue `"Enter the unused hours to pay out on the final paycheck. This is separate from time off hours used during the pay period."` */
    finalPayoutDescription: string
    timeOffBalance: {
      /** @defaultValue `"{{balance}} remaining"` */
      remaining: string
    }
    /** @defaultValue `"Additional earnings"` */
    additionalEarningsTitle: string
    /** @defaultValue `"Reimbursements"` */
    reimbursementTitle: string
    /** @defaultValue `"Description"` */
    reimbursementDescriptionLabel: string
    /** @defaultValue `"e.g., Office supplies"` */
    reimbursementDescriptionPlaceholder: string
    /** @defaultValue `"Amount"` */
    reimbursementAmountLabel: string
    /** @defaultValue `"Reimbursement"` */
    reimbursementUnnamedFallback: string
    /** @defaultValue `"Reimbursements"` */
    reimbursementsTableLabel: string
    /** @defaultValue `"Description"` */
    reimbursementDescriptionColumn: string
    /** @defaultValue `"Amount"` */
    reimbursementAmountColumn: string
    /** @defaultValue `"Type"` */
    reimbursementTypeColumn: string
    /** @defaultValue `"Recurring"` */
    reimbursementTypeRecurring: string
    /** @defaultValue `"One-time"` */
    reimbursementTypeOneTime: string
    /** @defaultValue `"No reimbursements"` */
    reimbursementEmptyTitle: string
    /** @defaultValue `"Add one-time reimbursement"` */
    addReimbursementCta: string
    /** @defaultValue `"Add one-time reimbursement"` */
    addReimbursementLink: string
    /** @defaultValue `"Save reimbursement"` */
    saveReimbursementCta: string
    /** @defaultValue `"Cancel reimbursement"` */
    cancelReimbursementCta: string
    /** @defaultValue `"Remove {{description}} reimbursement"` */
    removeReimbursementLabel: string
    /** @defaultValue `"{{description}} (recurring reimbursement)"` */
    recurringReimbursementLabel: string
    /** @defaultValue `"Recurring reimbursements are managed outside of payroll."` */
    recurringReimbursementTooltip: string
    fixedCompensationNames: {
      /** @defaultValue `"Bonus"` */
      bonus: string
      /** @defaultValue `"Paycheck tips"` */
      paycheckTips: string
      /** @defaultValue `"Correction payment"` */
      correctionPayment: string
      /** @defaultValue `"Commission"` */
      commission: string
      /** @defaultValue `"Cash tips"` */
      cashTips: string
      /** @defaultValue `"Reimbursement"` */
      reimbursement: string
    }
    /** @defaultValue `"Payment"` */
    paymentMethodTitle: string
    /** @defaultValue `"Payment method"` */
    paymentMethodLabel: string
    /** @defaultValue `"Changing the default payment method will only apply to this payroll."` */
    paymentMethodDescription: string
    paymentMethodOptions: {
      /** @defaultValue `"Direct deposit"` */
      directDeposit: string
      /** @defaultValue `"Check"` */
      check: string
    }
  }
  /** Translation keys for the `Payroll.PayrollFlow` i18n namespace. */
  export interface PayrollPayrollFlow {
    /** @defaultValue `"Save and exit"` */
    exitFlowCta: string
  }
  /** Translation keys for the `Payroll.PayrollHistory` i18n namespace. */
  export interface PayrollPayrollHistory {
    /** @defaultValue `"Payroll history"` */
    title: string
    dateFilter: {
      /** @defaultValue `"From"` */
      startDate: string
      /** @defaultValue `"To"` */
      endDate: string
      /** @defaultValue `"Apply"` */
      apply: string
      /** @defaultValue `"Cancel"` */
      cancel: string
      /** @defaultValue `"Reset"` */
      reset: string
      /** @defaultValue `"Select dates"` */
      selectDates: string
      /** @defaultValue `"Filter by date"` */
      trigger: string
    }
    dataView: {
      /** @defaultValue `"Payroll history"` */
      label: string
    }
    columns: {
      /** @defaultValue `"Pay period"` */
      payPeriod: string
      /** @defaultValue `"Type"` */
      type: string
      /** @defaultValue `"Pay date"` */
      payDate: string
      /** @defaultValue `"Status"` */
      status: string
      /** @defaultValue `"Total payroll"` */
      totalPayroll: string
    }
    menu: {
      /** @defaultValue `"View payroll summary"` */
      viewSummary: string
      /** @defaultValue `"View payroll receipt"` */
      viewReceipt: string
      /** @defaultValue `"Cancel payroll"` */
      cancelPayroll: string
    }
    emptyState: {
      default: {
        /** @defaultValue `"No payroll history"` */
        title: string
        /** @defaultValue `"When you run payrolls, they'll appear here for easy reference."` */
        description: string
      }
      filtered: {
        /** @defaultValue `"No payrolls in this date range"` */
        title: string
        /** @defaultValue `"Try adjusting the date filter to see more payrolls."` */
        description: string
      }
    }
    labels: {
      /** @defaultValue `"—"` */
      noAmount: string
    }
    cancelDialog: {
      /** @defaultValue `"Cancel {{payPeriod}} payroll?"` */
      title: string
      /** @defaultValue `"Any changes you have made to this payroll will be saved."` */
      body: string
      /** @defaultValue `"Run this payroll by {{deadline}} to ensure your employees are paid on time."` */
      deadline: string
      /** @defaultValue `"Yes, cancel payroll"` */
      primaryAction: string
      /** @defaultValue `"No, go back"` */
      secondaryAction: string
    }
  }
  /** Translation keys for the `Payroll.PayrollLanding` i18n namespace. */
  export interface PayrollPayrollLanding {
    tabs: {
      /** @defaultValue `"Run payroll"` */
      runPayroll: string
      /** @defaultValue `"Payroll history"` */
      payrollHistory: string
    }
    aria: {
      /** @defaultValue `"Payroll navigation"` */
      tabNavigation: string
    }
    labels: {
      /** @defaultValue `"Loading payroll data..."` */
      loading: string
    }
    breadcrumbs: {
      /** @defaultValue `"Run payroll"` */
      landing: string
      /** @defaultValue `"Summary for {{startDate}}-{{endDate}}"` */
      overview: string
      /** @defaultValue `"Payroll receipt for {{startDate}}-{{endDate}}"` */
      receipt: string
    }
    alerts: {
      /** @defaultValue `"Payroll cancelled"` */
      payrollCancelled: string
    }
  }
  /** Translation keys for the `Payroll.PayrollList` i18n namespace. */
  export interface PayrollPayrollList {
    emptyState: {
      default: {
        /** @defaultValue `"All payrolls have been processed"` */
        title: string
      }
      filtered: {
        /** @defaultValue `"No payrolls in this date range"` */
        title: string
        /** @defaultValue `"Try adjusting the date filter to see more payrolls."` */
        description: string
      }
    }
    /** @defaultValue `"Upcoming payroll"` */
    title: string
    dateFilter: {
      /** @defaultValue `"From"` */
      startDate: string
      /** @defaultValue `"To"` */
      endDate: string
      /** @defaultValue `"Apply"` */
      apply: string
      /** @defaultValue `"Cancel"` */
      cancel: string
      /** @defaultValue `"Reset"` */
      reset: string
      /** @defaultValue `"Select dates"` */
      selectDates: string
      /** @defaultValue `"Filter by date"` */
      trigger: string
    }
    /** @defaultValue `"Payrolls"` */
    payrollsListLabel: string
    /** @defaultValue `"Run Payroll"` */
    runPayrollTitle: string
    /** @defaultValue `"Review and submit"` */
    submitPayrollCta: string
    /** @defaultValue `"Skip payroll"` */
    skipPayrollCta: string
    /** @defaultValue `"Payroll actions"` */
    payrollMenuLabel: string
    skipPayrollDialog: {
      /** @defaultValue `"Skip payroll for {{payPeriod}}?"` */
      title: string
      /** @defaultValue `"Before skipping this payroll, check your state's pay frequency laws. If you don't pay the team on time, it could lead to penalties or fines."` */
      body: string
      /** @defaultValue `"Yes, skip payroll"` */
      confirmCta: string
      /** @defaultValue `"No, go back"` */
      cancelCta: string
    }
    /** @defaultValue `"Payroll skipped"` */
    skipSuccessAlert: string
    /** @defaultValue `"Cancel payroll"` */
    deletePayrollCta: string
    deletePayrollDialog: {
      /** @defaultValue `"Cancel {{payPeriod}} payroll?"` */
      title: string
      /** @defaultValue `"This will permanently remove this off-cycle payroll. Any changes you've made will be lost."` */
      body: string
      /** @defaultValue `"Yes, cancel payroll"` */
      confirmCta: string
      /** @defaultValue `"No, go back"` */
      cancelCta: string
    }
    /** @defaultValue `"Payroll cancelled"` */
    deleteSuccessAlert: string
    type: {
      /** @defaultValue `"External"` */
      External: string
      /** @defaultValue `"Off-Cycle"` */
      'Off-Cycle': string
      /** @defaultValue `"Regular"` */
      Regular: string
    }
    tableHeaders: {
      /** @defaultValue `"Pay period"` */
      '0': string
      /** @defaultValue `"Type"` */
      '1': string
      /** @defaultValue `"Pay date"` */
      '2': string
      /** @defaultValue `"Run by"` */
      '3': string
      /** @defaultValue `"Status"` */
      '4': string
    }
    offCycleCta: {
      /** @defaultValue `"Run an off-cycle payroll"` */
      title: string
      /** @defaultValue `"You can pay an employee outside of your normal payroll schedule by running an off-cycle payroll."` */
      description: string
      /** @defaultValue `"Run off-cycle payroll"` */
      button: string
    }
  }
  /** Translation keys for the `Payroll.PayrollOverview` i18n namespace. */
  export interface PayrollPayrollOverview {
    /** @defaultValue `"Submit"` */
    breadcrumbLabel: string
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Save and exit"` */
    exitFlowCta: string
    /** @defaultValue `"Review payroll"` */
    overviewTitle: string
    /** @defaultValue `"Payroll summary"` */
    summaryTitle: string
    /** @defaultValue `"{{payrollType}} payroll for <dateWrapper>{{startDate}} - {{endDate}}</dateWrapper>"` */
    pageSubtitle: string
    /** @defaultValue `"{{payrollType}} payroll"` */
    pageSubtitleDismissal: string
    /** @defaultValue `"Submit"` */
    submitCta: string
    /** @defaultValue `"Edit"` */
    editCta: string
    /** @defaultValue `"Cancel payroll"` */
    cancelCta: string
    /** @defaultValue `"Cancel {{startDate}} - {{endDate}} payroll?"` */
    cancelDialogTitle: string
    /** @defaultValue `"Cancel payroll?"` */
    cancelDialogTitleDismissal: string
    /** @defaultValue `"Any changes you have made to this payroll will be saved."` */
    cancelDialogDescription: string
    /** @defaultValue `"Run this payroll by {{deadline}} to ensure your employees are paid on time."` */
    cancelDialogDescriptionDeadline: string
    /** @defaultValue `" Yes, cancel payroll"` */
    confirmCancelCta: string
    /** @defaultValue `"No, go back"` */
    declineCancelCta: string
    /** @defaultValue `"View payroll receipt"` */
    payrollReceiptCta: string
    /** @defaultValue `"Download paystub pdf"` */
    downloadPaystubLabel: string
    /** @defaultValue `"Submitting payroll..."` */
    loadingTitle: string
    /** @defaultValue `"This may take a minute or two. You can navigate away while this happens."` */
    loadingDescription: string
    /** @defaultValue `"Loading payroll..."` */
    dataLoadingTitle: string
    /** @defaultValue `"Cancelling payroll..."` */
    cancellingTitle: string
    /** @defaultValue `"This payroll has been cancelled."` */
    cancelledEmptyState: string
    /** @defaultValue `"Skipped"` */
    skippedBadge: string
    alerts: {
      /** @defaultValue `"Payroll is not calculated"` */
      payrollNotCalculated: string
      /** @defaultValue `"To pay your employees with direct deposit by {{payDate}}, you'll need to run payroll by {{time}} on {{date}}"` */
      directDepositDeadline: string
      /** @defaultValue `"There was an issue generating the paystub PDF. Please try again later."` */
      paystubPdfError: string
      /** @defaultValue `"You noted {{count}} employee who should be paid by check."` */
      checkPaymentWarning_one: string
      /** @defaultValue `"You noted {{count}} employees who should be paid by check."` */
      checkPaymentWarning_other: string
      /** @defaultValue `"Employees with this payment method will need their checks delivered to them."` */
      checkPaymentWarningDescription: string
      /** @defaultValue `"Payroll submitted"` */
      payrollProcessedTitle: string
      /** @defaultValue `"{{amount}} will be debited on {{date}}. Make sure you have these funds available."` */
      payrollProcessedMessage: string
      /** @defaultValue `"There was an error submitting payroll"` */
      payrollProcessingFailedTitle: string
      /** @defaultValue `"Recalculate payroll"` */
      payrollProcessingFailedCtaLabel: string
      /** @defaultValue `"Wire details submitted"` */
      wireDetailsSubmittedTitle: string
      /** @defaultValue `"Once we receive the funds from your bank we'll pay your team on {{checkDate}}"` */
      wireDetailsSubmittedMessage: string
    }
    /** @defaultValue `"Payroll Summary"` */
    payrollSummaryTitle: string
    /** @defaultValue `"Payroll summary (Wire funds)"` */
    payrollSummaryTitleWire: string
    /** @defaultValue `"Payroll summary (4-day direct deposit)"` */
    payrollSummaryTitleFourDay: string
    /** @defaultValue `"Payroll summary table"` */
    payrollSummaryLabel: string
    dataViews: {
      /** @defaultValue `"Payroll details"` */
      label: string
      /** @defaultValue `"Company pays"` */
      companyPaysTab: string
      /** @defaultValue `"Company pays by employee"` */
      companyPaysTable: string
      /** @defaultValue `"Hours worked"` */
      hoursWorkedTab: string
      /** @defaultValue `"Hours worked by employee"` */
      hoursWorkedTable: string
      /** @defaultValue `"Employee take home"` */
      employeeTakeHomeTab: string
      /** @defaultValue `"Employee take home by employee"` */
      employeeTakeHomeTable: string
      /** @defaultValue `"Taxed and debited"` */
      taxesTab: string
      /** @defaultValue `"Taxes breakdown"` */
      taxesTable: string
      /** @defaultValue `"Debited totals by company"` */
      debitedTable: string
    }
    tableHeaders: {
      /** @defaultValue `"Total payroll"` */
      totalPayroll: string
      /** @defaultValue `"Debit amount"` */
      debitAmount: string
      /** @defaultValue `"Wire amount"` */
      wireAmount: string
      /** @defaultValue `"Wire transfer deadline"` */
      wireTransferDeadline: string
      /** @defaultValue `"Employees"` */
      employees: string
      /** @defaultValue `"Gross Pay"` */
      grossPay: string
      /** @defaultValue `"Reimbursements"` */
      reimbursements: string
      /** @defaultValue `"Debit account"` */
      debitAccount: string
      /** @defaultValue `"Debit date"` */
      debitDate: string
      /** @defaultValue `"Employee pay date"` */
      employeePayDate: string
      /** @defaultValue `"Company taxes"` */
      companyTaxes: string
      /** @defaultValue `"Company benefits"` */
      companyBenefits: string
      /** @defaultValue `"Company pays"` */
      companyPays: string
      /** @defaultValue `"Compensation type"` */
      compensationType: string
      /** @defaultValue `"Regular"` */
      regular: string
      /** @defaultValue `"Overtime (1.5x)"` */
      overtime: string
      /** @defaultValue `"Overtime (2x)"` */
      doubleOT: string
      /** @defaultValue `"Paid time off"` */
      timeOff: string
      /** @defaultValue `"Total hours"` */
      totalHours: string
      /** @defaultValue `"Payment type"` */
      paymentType: string
      /** @defaultValue `"Employee taxes"` */
      employeeTaxes: string
      /** @defaultValue `"Employee benefits"` */
      employeeBenefits: string
      /** @defaultValue `"Payment"` */
      payment: string
      /** @defaultValue `"Deductions"` */
      deductions: string
      /** @defaultValue `"Tax description"` */
      taxDescription: string
      /** @defaultValue `"By your employees"` */
      byYourEmployees: string
      /** @defaultValue `"By your company"` */
      byYourCompany: string
      /** @defaultValue `"Debited by Gusto"` */
      debitedByGusto: string
      /** @defaultValue `"Total"` */
      taxesTotal: string
      /** @defaultValue `"Paystub"` */
      paystub: string
      /** @defaultValue `"Totals"` */
      footerTotalsLabel: string
      /** @defaultValue `"All employees in this payroll"` */
      footerTotalsDescription: string
    }
    /** @defaultValue `"Totals"` */
    totalsLabel: string
    /** @defaultValue `"Direct deposits"` */
    directDepositLabel: string
    /** @defaultValue `"Reimbursements"` */
    reimbursementLabel: string
    /** @defaultValue `"Garnishments"` */
    garnishmentsLabel: string
    /** @defaultValue `"Taxes (Employees and Employers)"` */
    taxesLabel: string
    compensationTypeLabels: {
      /** @defaultValue `"Salaried / Exempt"` */
      exempt: string
      /** @defaultValue `"Hourly / Nonexempt"` */
      nonexempt: string
    }
    submissionBlockers: {
      /** @defaultValue `"Submission blocked"` */
      genericBlockerTitle: string
      /** @defaultValue `"This payroll cannot be submitted. Please contact support for assistance."` */
      genericBlockerMessage: string
      fastAchOptions: {
        /** @defaultValue `"Payroll can still be funded by selecting one of the options below. The selected funding method will only be used for this cycle and will not apply to future payroll."` */
        description: string
        /** @defaultValue `"Funding options"` */
        fundingOptionsLabel: string
        /** @defaultValue `"Wire funds"` */
        wireLabel: string
        /** @defaultValue `"Fastest"` */
        wireFastestBadge: string
        /** @defaultValue `"Pay your employees on time by sending a wire transfer. We'll provide instructions on the next step."` */
        wireDescription: string
        /** @defaultValue `"Switch to 4-day direct deposit"` */
        directDepositLabel: string
        /** @defaultValue `"Delay your employees pay date by four days and process using regular debits."` */
        directDepositDescription: string
        /** @defaultValue `"Employee pay date: {{date}}"` */
        employeePayDate: string
      }
      fast_ach_threshold_exceeded: {
        /** @defaultValue `"You have exceeded the limit at which you can process {{days}} payroll."` */
        title: string
      }
      needs_earned_access_for_fast_ach: {
        /** @defaultValue `"You have not yet earned access to faster payroll."` */
        title: string
      }
    }
  }
  /** Translation keys for the `Payroll.PayrollReceipts` i18n namespace. */
  export interface PayrollPayrollReceipts {
    /** @defaultValue `"Back"` */
    backCta: string
    /** @defaultValue `"Exit"` */
    exitFlowCta: string
    /** @defaultValue `"Receipts"` */
    breadcrumbLabel: string
    receipt: {
      /** @defaultValue `"Total"` */
      totalLabel: string
      /** @defaultValue `"Receipt Details"` */
      detailsLabel: string
      /** @defaultValue `"Receipt ID"` */
      receiptId: string
      /** @defaultValue `"From"` */
      from: string
      /** @defaultValue `"To"` */
      to: string
      /** @defaultValue `"Debit date"` */
      debitDate: string
      /** @defaultValue `"{{count}} employees"` */
      employeeCount: string
      /** @defaultValue `"{{count}} employee"` */
      employeeCount_one: string
      /** @defaultValue `"{{count}} employees"` */
      employeeCount_other: string
      /** @defaultValue `"These amounts may not represent all monies due to government tax authorities from you, and do not include any amounts transmitted outside the platform."` */
      disclaimer: string
      /** @defaultValue `"Your payroll provider partners with Gusto Inc. for payments processing. Gusto Inc. is a licensed money transmitter. Learn more on our license page."` */
      companyInfo: string
      /** @defaultValue `"525 20th St, San Francisco, CA 94107 | 415-777-8888"` */
      address: string
    }
    sections: {
      /** @defaultValue `"Debited"` */
      debitedLabel: string
      /** @defaultValue `"Tax"` */
      taxLabel: string
      /** @defaultValue `"Employees"` */
      employeesLabel: string
    }
    breakdown: {
      /** @defaultValue `"Direct deposits"` */
      directDeposits: string
      /** @defaultValue `"Reimbursements"` */
      reimbursements: string
      /** @defaultValue `"Garnishments"` */
      garnishments: string
      /** @defaultValue `"Taxes (Employee and Employer)"` */
      taxes: string
      /** @defaultValue `"Totals"` */
      totals: string
      /** @defaultValue `"Category"` */
      category: string
      /** @defaultValue `"Amount"` */
      amount: string
    }
    tax: {
      /** @defaultValue `"Tax Type"` */
      name: string
      /** @defaultValue `"Amount"` */
      amount: string
    }
    employee: {
      /** @defaultValue `"Employee"` */
      name: string
      /** @defaultValue `"Payment method"` */
      paymentMethod: string
      /** @defaultValue `"Child support"` */
      childSupport: string
      /** @defaultValue `"Reimbursement"` */
      reimbursement: string
      /** @defaultValue `"Total taxes"` */
      totalTaxes: string
      /** @defaultValue `"Net pay"` */
      netPay: string
      /** @defaultValue `"{{count}} employees in this payroll"` */
      totalEmployees: string
      /** @defaultValue `"{{count}} employee in this payroll"` */
      totalEmployees_one: string
      /** @defaultValue `"{{count}} employees in this payroll"` */
      totalEmployees_other: string
    }
  }
  /** Translation keys for the `Payroll.RecoveryCasesList` i18n namespace. */
  export interface PayrollRecoveryCasesList {
    /** @defaultValue `"Recovery cases"` */
    title: string
    /** @defaultValue `"One or more payments couldn't be processed due to a bank error. Resolve open recovery cases to continue running payroll."` */
    description: string
    /** @defaultValue `"No recovery cases"` */
    emptyTableTitle: string
    /** @defaultValue `"There are no recovery cases that need your response at this time."` */
    emptyTableDescription: string
    labels: {
      /** @defaultValue `"—"` */
      noLatestErrorCode: string
      /** @defaultValue `"No error code available"` */
      noLatestErrorCodeAriaLabel: string
    }
    columns: {
      /** @defaultValue `"Original debit date"` */
      originalDebitDate: string
      /** @defaultValue `"Total amount"` */
      totalAmount: string
      /** @defaultValue `"Amount outstanding"` */
      amountOutstanding: string
      /** @defaultValue `"Latest error code"` */
      latestErrorCode: string
      /** @defaultValue `"Status"` */
      status: string
    }
    status: {
      /** @defaultValue `"Open"` */
      open: string
      /** @defaultValue `"Redebit initiated"` */
      redebit_initiated: string
      /** @defaultValue `"Wire initiated"` */
      wire_initiated: string
      /** @defaultValue `"Recovered"` */
      recovered: string
      /** @defaultValue `"Lost"` */
      lost: string
    }
    cta: {
      /** @defaultValue `"Resolve"` */
      resolve: string
    }
  }
  /** Translation keys for the `Payroll.RecoveryCasesResubmit` i18n namespace. */
  export interface PayrollRecoveryCasesResubmit {
    cta: {
      /** @defaultValue `"Cancel"` */
      cancel: string
      /** @defaultValue `"Resubmit payment"` */
      resubmit: string
    }
    errorCodes: {
      R01: {
        /** @defaultValue `"R01: Insufficient funds"` */
        title: string
        /** @defaultValue `"Ensure sufficient funds to unblock your account"` */
        subtitle: string
        /** @defaultValue `"We attempted to debit from your bank account. Unfortunately, we received the following error: <medium>R01: Insufficient funds.</medium>"` */
        body: string
        /** @defaultValue `"This means your bank account did not have sufficient funds to cover the debits of your invoice. Transfer sufficient funds into your bank account. Then select <medium>Resubmit payment</medium> below."` */
        instruction: string
      }
      R02: {
        /** @defaultValue `"R02: Account closed"` */
        title: string
        /** @defaultValue `"Connect your new bank account to unblock your account"` */
        subtitle: string
        /** @defaultValue `"We attempted to debit from your bank account. Unfortunately, we received the following error: <medium>R02: Account closed.</medium>"` */
        body: string
        /** @defaultValue `"This error means that the account we tried to debit is closed. Please connect a new bank account. Then select <medium>Resubmit payment</medium> below."` */
        instruction: string
      }
      R16: {
        /** @defaultValue `"R16: Account frozen"` */
        title: string
        /** @defaultValue `"Unfreeze your bank account to unblock your account"` */
        subtitle: string
        /** @defaultValue `"We attempted to debit from your bank account. Unfortunately, we received the following error: <medium>R16: Account frozen.</medium>"` */
        body: string
        /** @defaultValue `"Banks can freeze an account for a number of reasons (i.e. to protect you and others from fraud, etc). Work with your bank to understand why your bank account was frozen and to unfreeze it. Once you've confirmed with your bank that your account is ready, select <medium>Resubmit payment</medium> below."` */
        instruction: string
      }
      R29: {
        /** @defaultValue `"R29: Corporate customer advises not authorized"` */
        title: string
        /** @defaultValue `"Contact your bank to authorize debits"` */
        subtitle: string
        /** @defaultValue `"We attempted to debit from your bank account. Unfortunately, we received the following error: <medium>R29: Corporate customer advises not authorized.</medium>"` */
        body: string
        /** @defaultValue `"Typically this error means your bank needs to authorize us to debit your account. Work with your bank to make sure the following ACH codes are authorized to debit your account:"` */
        listDescription: string
        achCodes: {
          /** @defaultValue `"1453942850"` */
          '0': string
          /** @defaultValue `"1800948598"` */
          '1': string
          /** @defaultValue `"2453942850"` */
          '2': string
          /** @defaultValue `"4270465600"` */
          '3': string
          /** @defaultValue `"9138864001"` */
          '4': string
          /** @defaultValue `"9138864004"` */
          '5': string
          /** @defaultValue `"9138864002"` */
          '6': string
          /** @defaultValue `"9138864003"` */
          '7': string
          /** @defaultValue `"9138864007"` */
          '8': string
        }
        /** @defaultValue `"Once you have done so, select <medium>Resubmit payment</medium> below."` */
        instruction: string
      }
      bankError: {
        /** @defaultValue `"Bank error"` */
        title: string
        /** @defaultValue `"Contact your bank"` */
        subtitle: string
        /** @defaultValue `"A bank error has occurred. Please contact support to unblock your account."` */
        body: string
        /** @defaultValue `""` */
        instruction: string
      }
    }
  }
  /** Translation keys for the `Payroll.Transition` i18n namespace. */
  export interface PayrollTransition {
    /** @defaultValue `"Transition Payroll"` */
    breadcrumbLabel: string
  }
  /** Translation keys for the `Payroll.TransitionCreation` i18n namespace. */
  export interface PayrollTransitionCreation {
    /** @defaultValue `"Transition Payroll"` */
    breadcrumbLabel: string
    /** @defaultValue `"Transition Payroll"` */
    pageTitle: string
    /** @defaultValue `"A transition payroll covers the gap between your old and new pay schedules."` */
    pageDescription: string
    /** @defaultValue `"When a pay schedule changes, there may be workdays that fall between the end of the old schedule and the start of the new one. This transition payroll ensures employees are paid for those days."` */
    transitionExplanation: string
    /** @defaultValue `"Transition Details"` */
    detailsHeading: string
    /** @defaultValue `"Pay period"` */
    payPeriodLabel: string
    /** @defaultValue `"Pay schedule"` */
    payScheduleLabel: string
    /** @defaultValue `"Check date"` */
    checkDateLabel: string
    /** @defaultValue `"Continue"` */
    continueCta: string
    errors: {
      /** @defaultValue `"Transition payroll was created but no payroll ID was returned"` */
      missingPayrollId: string
      /** @defaultValue `"Check date is required"` */
      checkDateRequired: string
      /** @defaultValue `"Check date must be at least {{count}} business day from today"` */
      checkDateAchLeadTime_one: string
      /** @defaultValue `"Check date must be at least {{count}} business days from today"` */
      checkDateAchLeadTime_other: string
    }
  }
  /** Translation keys for the `Payroll.TransitionPayrollAlert` i18n namespace. */
  export interface PayrollTransitionPayrollAlert {
    /** @defaultValue `"Transition payroll - {{dateRange}}"` */
    alertTitle: string
    /** @defaultValue `"Because you changed your pay schedule, you'll need to run some transition payrolls. Transition payrolls let you pay employees for any workdays during gaps between pay schedules. You may choose to skip them, but it is your responsibility to ensure employees are paid properly. Regular payroll functionality is blocked until you either run or skip transition payrolls."` */
    alertDescription: string
    /** @defaultValue `"Run Transition Payroll"` */
    runPayroll: string
    /** @defaultValue `"Skip this payroll"` */
    skipPayroll: string
    /** @defaultValue `"Transition payroll skipped"` */
    skipSuccessAlert: string
    skipDialog: {
      /** @defaultValue `"Skip {{dateRange}} transition payroll?"` */
      title: string
      /** @defaultValue `"Skipping this payroll means employees will not be paid for the transition period. It is your responsibility to ensure employees are paid properly."` */
      body: string
      /** @defaultValue `"Skip payroll"` */
      confirmCta: string
      /** @defaultValue `"Cancel"` */
      cancelCta: string
    }
  }
  /** Translation keys for the `Payroll.WireInstructions` i18n namespace. */
  export interface PayrollWireInstructions {
    /** @defaultValue `"Wire instructions"` */
    title: string
    /** @defaultValue `"This info tells your bank how much and where to send the wire transfer. Make sure to send a wire transfer—we will not accept ACH transfers. Afterwards, confirm below that you have completed this step."` */
    subtitle: string
    /** @defaultValue `"Wire transfer for payroll"` */
    selectLabel: string
    /** @defaultValue `"Payroll for {{payrollRange}}"` */
    selectLabelPayroll: string
    /** @defaultValue `"Contractor payment for {{requestedAmount}}"` */
    selectLabelContractorPaymentGroup: string
    /** @defaultValue `"Wire Transfer"` */
    selectFallback: string
    /** @defaultValue `"What to know when wiring funds"` */
    requirementsTitle: string
    requirements: {
      /** @defaultValue `"You must include the unique tracking code from the wire instructions"` */
      trackingCode: string
      /** @defaultValue `"The amount you send must exactly match the amount in the wire instructions"` */
      amountMatch: string
      /** @defaultValue `"The originating bank account must be based in the US"` */
      usBank: string
      /** @defaultValue `"You must be authorized to use the bank account on the company's behalf"` */
      authorized: string
    }
    fields: {
      /** @defaultValue `"Unique tracking code"` */
      trackingCode: string
      /** @defaultValue `"Amount to wire"` */
      amount: string
      /** @defaultValue `"Bank name"` */
      bankName: string
      /** @defaultValue `"Bank address"` */
      bankAddress: string
      /** @defaultValue `"Recipient name"` */
      recipientName: string
      /** @defaultValue `"Recipient address"` */
      recipientAddress: string
      /** @defaultValue `"Recipient account number"` */
      accountNumber: string
      /** @defaultValue `"Recipient routing number"` */
      routingNumber: string
    }
    ariaLabels: {
      /** @defaultValue `"Copy tracking code"` */
      copyTrackingCode: string
    }
    messages: {
      /** @defaultValue `"Copied to clipboard!"` */
      copied: string
      /** @defaultValue `"No wire instructions available at this time."` */
      noInstructions: string
      /** @defaultValue `"Unable to load wire instruction details."` */
      unableToLoad: string
    }
    cta: {
      /** @defaultValue `"Close"` */
      close: string
      /** @defaultValue `"I've made a wire transfer"` */
      confirm: string
    }
  }
  /** Translation keys for the `common` i18n namespace. */
  export interface common {
    status: {
      /** @defaultValue `"Loading component..."` */
      loading: string
      /** @defaultValue `"Loading options..."` */
      loadingOptions: string
      /** @defaultValue `"is a required field"` */
      requiredField: string
      /** @defaultValue `"There was a problem with your submission"` */
      errorEncountered: string
      /** @defaultValue `"There were multiple problems with your submission"` */
      multipleErrorsEncountered: string
    }
    /** @defaultValue `"(optional)"` */
    optionalLabel: string
    /** @defaultValue `"Select an option..."` */
    selectPlaceholder: string
    /** @defaultValue `"You are on step {{currentStep}} of {{totalSteps}}"` */
    progressBarLabel: string
    /** @defaultValue `"Back"` */
    back: string
    errors: {
      /** @defaultValue `"Error"` */
      errorHeading: string
      /** @defaultValue `"Error while rendering SDK component: {{error}}"` */
      globalReactError: string
      /** @defaultValue `"Try again"` */
      resetGlobalError: string
      /** @defaultValue `"Unknown Error"` */
      unknownError: string
      /** @defaultValue `"{{component}} is missing {{param}} parameter or is used outside {{provider}}"` */
      missingParamsOrContext: string
      /** @defaultValue `"Unhandled event type: {{event}}"` */
      unhandledEvent: string
      /** @defaultValue `"Unprocessed event type"` */
      unknownEventType: string
      /** @defaultValue `"Required prop is missing"` */
      ensureRequired: string
    }
    icons: {
      /** @defaultValue `"Show options"` */
      selectArrow: string
      /** @defaultValue `"Show calendar"` */
      calendarArrow: string
      /** @defaultValue `"Show next month"` */
      nextMonth: string
      /** @defaultValue `"Show previous month"` */
      previousMonth: string
      /** @defaultValue `"Navigate to first page"` */
      paginationFirst: string
      /** @defaultValue `"Navigate to previous page"` */
      paginationPrev: string
      /** @defaultValue `"Navigate to next page"` */
      paginationNext: string
      /** @defaultValue `"Navigate to last page"` */
      paginationLast: string
    }
    labels: {
      /** @defaultValue `"Items per page"` */
      paginationControlCountLabel: string
      /** @defaultValue `"Fetching data..."` */
      paginationFetchingLabel: string
      /** @defaultValue `"Open menu"` */
      openMenu: string
      /** @defaultValue `"Menu"` */
      menuLabel: string
      /** @defaultValue `"Tab navigation"` */
      tabNavigation: string
      /** @defaultValue `"Selected {{label}}"` */
      selectedItems: string
      /** @defaultValue `"Remove {{label}}"` */
      removeItem: string
    }
    onboardingStatus: {
      contractor: {
        /** @defaultValue `"N/A"` */
        undefined: string
        /** @defaultValue `"Admin-onboarding Incomplete"` */
        admin_onboarding_incomplete: string
        /** @defaultValue `"Admin-onboarding review"` */
        admin_onboarding_review: string
        /** @defaultValue `"Completed"` */
        onboarding_completed: string
        /** @defaultValue `"Self-onboarding: Not Invited"` */
        self_onboarding_not_invited: string
        /** @defaultValue `"Self-onboarding: Invited"` */
        self_onboarding_invited: string
        /** @defaultValue `"Self-onboarding started"` */
        self_onboarding_started: string
        /** @defaultValue `"Self-onboarding: Review"` */
        self_onboarding_review: string
      }
      employee: {
        /** @defaultValue `"N/A"` */
        undefined: string
        /** @defaultValue `"Admin-onboarding Incomplete"` */
        admin_onboarding_incomplete: string
        /** @defaultValue `"Completed"` */
        onboarding_completed: string
        /** @defaultValue `"Self-onboarding: Pending Invite"` */
        self_onboarding_pending_invite: string
        /** @defaultValue `"Self-onboarding: Invited"` */
        self_onboarding_invited: string
        /** @defaultValue `"Self-onboarding started"` */
        self_onboarding_invited_started: string
        /** @defaultValue `"Self-onboarding: Overdue"` */
        self_onboarding_invited_overdue: string
        /** @defaultValue `"Self-onboarding: Completed"` */
        self_onboarding_completed_by_employee: string
        /** @defaultValue `"Self-onboarding: Admin review"` */
        self_onboarding_awaiting_admin_review: string
      }
    }
    inputs: {
      ssn: {
        /** @defaultValue `"***-**-****"` */
        placeholder: string
      }
      ein: {
        /** @defaultValue `"**-*******"` */
        placeholder: string
      }
      /** @defaultValue `"/hr"` */
      workersCompensationRatePerHourAdornment: string
    }
    payRateFormats: {
      /** @defaultValue `"{{amount}}/hr"` */
      hourly: string
      /** @defaultValue `"{{amount}}/yr"` */
      weekly: string
      /** @defaultValue `"{{amount}}/yr"` */
      monthly: string
      /** @defaultValue `"{{amount}}/yr"` */
      yearly: string
      /** @defaultValue `"{{amount}}/paycheck"` */
      paycheck: string
    }
    compensationRateFormats: {
      /** @defaultValue `"{{amount}} per hour"` */
      hourly: string
      /** @defaultValue `"{{amount}} per week"` */
      weekly: string
      /** @defaultValue `"{{amount}} per month"` */
      monthly: string
      /** @defaultValue `"{{amount}} per year"` */
      yearly: string
      /** @defaultValue `"{{amount}} per paycheck"` */
      paycheck: string
    }
    validations: {
      /** @defaultValue `"Account name is required"` */
      accountName: string
      /** @defaultValue `"Routing number should be a number (9 digits)"` */
      routingNumber: string
      /** @defaultValue `"Account number is a required field"` */
      accountNumber: string
      /** @defaultValue `"The SSN must be exactly 9 digits long, cannot contain all zeros in any group, and the first three digits cannot be '666' or in the range 900–999."` */
      ssn: string
      /** @defaultValue `"Social Security Number is required"` */
      ssnRequired: string
      /** @defaultValue `"Location is not valid"` */
      location: string
      /** @defaultValue `"Date of birth is required"` */
      dob: string
      /** @defaultValue `"Minimum value is {{min}}%"` */
      percentageMin: string
      /** @defaultValue `"Maximum value is {{max}}%"` */
      percentageMax: string
    }
    statesHash: {
      /** @defaultValue `"Alabama"` */
      AL: string
      /** @defaultValue `"Alaska"` */
      AK: string
      /** @defaultValue `"Arizona"` */
      AZ: string
      /** @defaultValue `"Arkansas"` */
      AR: string
      /** @defaultValue `"California"` */
      CA: string
      /** @defaultValue `"Colorado"` */
      CO: string
      /** @defaultValue `"Connecticut"` */
      CT: string
      /** @defaultValue `"Delaware"` */
      DE: string
      /** @defaultValue `"District of Columbia"` */
      DC: string
      /** @defaultValue `"Florida"` */
      FL: string
      /** @defaultValue `"Georgia"` */
      GA: string
      /** @defaultValue `"Hawaii"` */
      HI: string
      /** @defaultValue `"Idaho"` */
      ID: string
      /** @defaultValue `"Illinois"` */
      IL: string
      /** @defaultValue `"Indiana"` */
      IN: string
      /** @defaultValue `"Iowa"` */
      IA: string
      /** @defaultValue `"Kansas"` */
      KS: string
      /** @defaultValue `"Kentucky"` */
      KY: string
      /** @defaultValue `"Louisiana"` */
      LA: string
      /** @defaultValue `"Maine"` */
      ME: string
      /** @defaultValue `"Maryland"` */
      MD: string
      /** @defaultValue `"Massachusetts"` */
      MA: string
      /** @defaultValue `"Michigan"` */
      MI: string
      /** @defaultValue `"Minnesota"` */
      MN: string
      /** @defaultValue `"Mississippi"` */
      MS: string
      /** @defaultValue `"Missouri"` */
      MO: string
      /** @defaultValue `"Montana"` */
      MT: string
      /** @defaultValue `"Nebraska"` */
      NE: string
      /** @defaultValue `"Nevada"` */
      NV: string
      /** @defaultValue `"New Hampshire"` */
      NH: string
      /** @defaultValue `"New Jersey"` */
      NJ: string
      /** @defaultValue `"New Mexico"` */
      NM: string
      /** @defaultValue `"New York"` */
      NY: string
      /** @defaultValue `"North Carolina"` */
      NC: string
      /** @defaultValue `"North Dakota"` */
      ND: string
      /** @defaultValue `"Ohio"` */
      OH: string
      /** @defaultValue `"Oklahoma"` */
      OK: string
      /** @defaultValue `"Oregon"` */
      OR: string
      /** @defaultValue `"Pennsylvania"` */
      PA: string
      /** @defaultValue `"Rhode Island"` */
      RI: string
      /** @defaultValue `"South Carolina"` */
      SC: string
      /** @defaultValue `"South Dakota"` */
      SD: string
      /** @defaultValue `"Tennessee"` */
      TN: string
      /** @defaultValue `"Texas"` */
      TX: string
      /** @defaultValue `"Utah"` */
      UT: string
      /** @defaultValue `"Vermont"` */
      VT: string
      /** @defaultValue `"Virginia"` */
      VA: string
      /** @defaultValue `"Washington"` */
      WA: string
      /** @defaultValue `"West Virginia"` */
      WV: string
      /** @defaultValue `"Wisconsin"` */
      WI: string
      /** @defaultValue `"Wyoming"` */
      WY: string
    }
    table: {
      /** @defaultValue `"Select row"` */
      selectRowHeader: string
      /** @defaultValue `"Select row"` */
      selectRowLabel: string
      /** @defaultValue `"Select all rows"` */
      selectAllRowsLabel: string
      /** @defaultValue `"Actions"` */
      actionsColumnHeader: string
    }
    card: {
      /** @defaultValue `"Select row"` */
      selectRowLabel: string
      /** @defaultValue `"Select all rows"` */
      selectAllRowsLabel: string
    }
    signatoryTitles: {
      /** @defaultValue `"Owner"` */
      owner: string
      /** @defaultValue `"President"` */
      president: string
      /** @defaultValue `"Vice President"` */
      vice_president: string
      /** @defaultValue `"Treasurer"` */
      treasurer: string
      /** @defaultValue `"Corporate Officer"` */
      corporate_officer: string
      /** @defaultValue `"Partner"` */
      partner: string
      /** @defaultValue `"Member"` */
      member: string
    }
    fileInput: {
      /** @defaultValue `"<clickToUpload>Click to upload</clickToUpload> or drag and drop"` */
      uploadInstructions: string
      /** @defaultValue `"Only {{types}} files are permitted"` */
      acceptedTypes: string
      /** @defaultValue `"Remove file"` */
      removeFile: string
    }
    reorderableList: {
      /** @defaultValue `"Item {{item}} moved down to position {{position}} of {{total}}"` */
      itemMovedDown: string
      /** @defaultValue `"Item {{item}} moved up to position {{position}} of {{total}}"` */
      itemMovedUp: string
      /** @defaultValue `"Started dragging item {{item}}"` */
      dragStarted: string
      /** @defaultValue `"Finished dragging item {{item}}"` */
      dragEnded: string
      /** @defaultValue `"Item {{item}} moved up to position {{position}} of {{total}}"` */
      movedUp: string
      /** @defaultValue `"Item {{item}} moved down to position {{position}} of {{total}}"` */
      movedDown: string
      /** @defaultValue `"Reordering mode canceled for item {{item}}"` */
      reorderingCanceled: string
      /** @defaultValue `"Exited reordering mode for item {{item}}"` */
      reorderingExited: string
      /** @defaultValue `"Entered reordering mode for item {{item}}. Use arrow keys to move, Escape to cancel, or Space to finish."` */
      reorderingStarted: string
      /** @defaultValue `"Reordering complete for item {{item}}"` */
      reorderingComplete: string
      /** @defaultValue `"Press Space or Enter to start reordering item {{item}}"` */
      draggableLabel: string
      /** @defaultValue `"Press arrow keys to move item {{item}}, or press Space/Enter to finish reordering"` */
      draggableLabelActive: string
      /** @defaultValue `"Item {{item}} at position {{position}} of {{total}}"` */
      draggablePosition: string
      /** @defaultValue `"Item {{position}}"` */
      item: string
      /** @defaultValue `"Drop item here"` */
      dropItemHere: string
      /** @defaultValue `"Draggable item"` */
      draggableItem: string
    }
  }
}
